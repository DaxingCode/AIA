# 产品技术蓝图：截图感知 · 食物卡路里 · 健康数据 原生 App

> 版本：v1（2026-07-16） ｜ 平台：iOS 优先，后续跨端 ｜ 状态：设计整合阶段

---

## 1. 产品定位（一句话）

**一个"系统级感知 + AI 结构化"的个人助理**：截图自动识别记账/待办/留存，拍照记录食物卡路里，打通 HealthKit 运动/睡眠/身体指标，最终形成「摄入—消耗」闭环与统一的知识库。

---

## 2. 市场与竞品（已验证存在，组合仍稀缺）

| 品类 | 代表 | 覆盖能力 |
|---|---|---|
| 截图自动记账 | 咔皮记账（商汤，2026.1 用户破 500 万）、MONO 记账 AI、蜜蜂记账 | 仅账单 |
| 截图整理 / AI 收件箱 | RecallBox、CaptainShot、Screenshot OS、Sorti、SnapSense | 整理+待办+提醒，国内支付弱 |
| 纯 OCR 复制 | 全能图片转文字、Copy Text On Screen、Screenie | 仅取字 |

**结论**：单点产品成熟，但「截图识别 + 食物卡路里 + 健康数据 + 闭环」四合一仍稀缺，是切入空间。

---

## 3. 总体架构

```
[截图]──┐
[拍照食物]─┼─→ 采集与预处理层(iOS原生桥接) ─→ AI识别层(云端多模态LLM) ─→ 结构化数据层(本地优先) ─→ 应用功能层
[健康数据]─┘   PhotoKit·Camera·HealthKit             OCR+视觉→JSON          账单/提醒/食物/健康/知识卡   记账/提醒/卡路里/看板/检索
```

- 财务/健康属敏感数据：**默认本地存储**，上云前脱敏，Info.plist 声明权限。

---

## 4. 核心能力拆解

### 4.1 截图自动识别（账单 / 提醒 / 复制内容）
- **自动入口**：iOS 快捷指令「截屏」个人自动化 + `App Intents`（`openAppWhenRun=false` 后台运行），关闭「运行前询问」实现无感。
- **兜底入口**：分享扩展（手动把截图发进 App）。
- **处理**：图片 → 云端多模态 LLM，prompt 要求返回结构化 JSON：
  - `{type:"bill", amount, merchant, category, date, paymentMethod}`
  - `{type:"todo", title, dueDate}`
  - `{type:"note", text}`

### 4.2 拍照识别食物卡路里
- 相机拍照 → 云端多模态 LLM 返回 `{foods:[{name, portion, calories, protein, carb, fat}], mealType}`。
- **精度增强**：纯 LLM 估卡路里误差大（±30%），接**营养数据库**校正——海外 USDA/Edamam，国内薄荷健康/Keep，把菜名映射到标准热量。

### 4.3 健康数据（HealthKit）
- `HKHealthStore` 申请读取权限，三类数据类型：
  - 运动：`StepCount`、`DistanceWalkingRunning`、`ActiveEnergyBurned`、`Workout`
  - 睡眠：`SleepAnalysis`
  - 身体：`BodyMass`、`Height`、`BodyFatPercentage`、`HeartRate`、`BMI`
- 拉取后落本地，做健康看板与趋势图。

### 4.4 闭环：净热量
- 由运动/身体指标计算 **TDEE（每日总消耗）**，减去食物卡路里得**净热量**。
- 这是差异化卖点：截图记账 + 食物 + 健康在同一个个人数据库里联动。

---

## 5. 数据模型（本地优先）

| 实体 | 关键字段 |
|---|---|
| **Capture**（原始采集） | id, kind(screenshot/food/health), assetLocalRef, processed, createdAt |
| **Bill**（账单） | id, source, rawText, imagePath, amount, currency, merchant, category, transactionTime, paymentMethod, confidence, status(pending/confirmed) |
| **Reminder**（提醒待办） | id, title, dueDate, done, priority, sourceCaptureId, note |
| **FoodEntry**（食物卡路里） | id, imagePath, mealType, eatenAt, items[{name,portion,calories,protein,carb,fat,source}], totalCalories, totalProtein, totalCarb, totalFat |
| **HealthMetric**（健康指标·日快照） | date, steps, distanceKm, activeEnergyKcal, sleepHours, weightKg, heightCm, bmi, bodyFatPct, restingHr, tdee(计算), netCalories(计算) |
| **KnowledgeCard**（知识卡片） | id, title, tags[], contentText, sourceCaptureId, type, starred, archived |

---

## 6. 技术栈建议（待确认）

| 选项 | 说明 | 适合度 |
|---|---|---|
| **React Native (Expo)** | 复用你的 JS 经验，跨端成本低；HealthKit 用 react-native-health，相册/相机用 expo 模块，原生桥接包成 TurboModule | ⭐ 推荐（JS 背景） |
| **Flutter** | `health` 插件对 HealthKit+Google Fit 开箱即用；需学 Dart | 跨端健康最省心 |
| **纯 Swift + SwiftUI** | iOS 最干净、桥接最少；但 Android 要重写 | 仅 iOS 时最优 |

> 无论哪种，**HealthKit、相册截屏读取都避不开原生 Swift 桥接**。

---

## 7. 截图自动化实现要点（原生 Swift）

```swift
import AppIntents

struct ProcessScreenshotIntent: AppIntent {
    static var title: LocalizedStringResource = "处理截屏"
    static var description = IntentDescription("识别截图中的账单、待办与内容并自动记录")
    static var openAppWhenRun: Bool = false   // 后台运行，不打开 App

    @Parameter(title: "图片", default: nil)
    var image: IntentImage?

    func perform() async throws -> some IntentResult {
        let data = try await resolveImageData()          // 优先传入图，否则读相册最新截屏
        let compressed = compress(data, maxDim: 1280)     // 压缩省流量
        let result = try await CloudRecognizer.recognize(compressed) // 云端 LLM → JSON
        try Database.save(result)                         // 落本地 DB
        Notification.schedule(summary: result.summary)    // 本地通知
        return .result()
    }
}

struct AppShortcuts: AppShortcutsProvider {
    static var appShortcuts: [AppShortcut] {
        AppShortcut(intent: ProcessScreenshotIntent(),
                     phrases: ["用 \(.applicationName) 处理截屏"])
    }
}
```

**用户一次性配置**：快捷指令 → 自动化 → 个人自动化 → 「截屏」→ 添加操作「[App] 处理截屏」→ 关闭「运行前询问」。引导页提供 deep link 降低门槛。

**三个坑**：① 部分 iOS 版本「运行前询问」仍弹确认（真机验证，最坏退化为点一下）；② 需完整照片读取权限（`NSPhotoLibraryUsageDescription`），限定照片模式未必能稳拿截图；③ Intent 自己读相册最新截屏最稳，不依赖上游传参。

---

## 8. MVP 里程碑

| 阶段 | 内容 | 价值 |
|---|---|---|
| M1 | 分享扩展导入截图 → 云端 LLM → 账单/提醒/知识卡 | 跑通核心闭环，成本最低 |
| M2 | 快捷指令「截屏」自动化对接（替代 PhotoKit 监听） | 体验从手动变无感 |
| M3 | 拍照食物识别卡路里 + 营养库校正 | 第二个差异化能力 |
| M4 | HealthKit 接入运动/睡眠/身体指标看板 | 第三个能力 |
| M5 | 净热量联动 + 云同步 + 本地通知提醒 | 形成闭环，可上线 | ✅ **2026-07-17 真机验证通过**（净热量卡片 + 云同步 push/pull 双向通，详见 §13） |

---

## 9. 隐私与审核

- 财务/健康敏感，**默认本地存储**；上云仅传结构化字段，不传原图；隐私政策写清。
- HealthKit 是 Apple 审查重点：功能须真实、非纯展示；Info.plist 写清 `NSHealthShareUsageDescription` 等理由文案，否则上架被拒。

---

## 10. 风险与开放问题

- iOS 无法真后台截屏，自动化体验依赖用户配置 + 系统版本。
- 云端 LLM 账单/卡路里识别精度与隐私权衡。
- HealthKit 权限获取率、营养库国内数据版权。
- **已决**：技术栈定为**纯 Swift + SwiftUI**（2026-07-16 拍板，已落地 MVP 全量功能，详见 `原生代码脚手架/`）。RN / Flutter 不再考虑。
- 云端多模态 LLM API Key 必须走 CloudBase 云函数代理（不在前端硬编码）；健康/财务敏感数据默认本地存储，上云仅传结构化字段。
- 未来如需云同步可平滑切 iCloud（详见 §12）。

---

## 11. 下一步

确认技术栈后，直接 scaffold M1+M2 骨架：工程结构、`ProcessScreenshotIntent` 完整实现、云端 LLM 封装（含结构化 JSON prompt）、本地数据模型、快捷指令引导页。

## 12. 未来演进：切换到 iCloud 同步（迁移方案）

**现状**：MVP 云同步已采用 **CloudBase 云函数**（`aia-sync` + `aia_records` 集合），原因是 iCloud / CloudKit 整库同步属 **Apple Developer Program（付费 $99/年）专属能力**，免费账号无法开启 iCloud 容器、部署 CloudKit schema；而 CloudBase 只依赖普通网络请求，免费账号即可验。

**结论**：未来可平滑切到 iCloud，且当前架构已为此铺好路。

### 12.1 三种演进姿态

- **A. 继续用 CloudBase**：不做改动。服务端可控、跨平台（未来安卓/网页版可复用）。
- **B. 双方案并存**：保留 CloudBase，另开 iCloud 通道给纯 Apple 生态用户零配置同步。
- **C. 完全切 iCloud（CloudKit）**：删手写同步代码，SwiftData + CloudKit 一行接管（`ModelConfiguration(cloudKitDatabase: .automatic)`）。

### 12.2 切到 iCloud（姿态 C）的改动清单

| 环节 | 现状（CloudBase） | 切 iCloud 后 |
|---|---|---|
| 能力 | 无（普通网络） | 付费会员 → 主 App 开 iCloud + CloudKit，建容器 |
| 同步引擎 | `CloudSyncManager.swift`（手写 push/pull/冲突合并） | 整文件删除，改 `AIAApp.swift` 容器配置 |
| 设置页 | `SettingsView` 手填同步账号 | 删除账号逻辑，自动用登录的 Apple ID |
| 云函数 | `aia-sync` | 不再需要（可留给非 Apple 端） |
| 数据模型 | 已有 `syncId / syncUpdatedAt / syncDeleted` | 微调：CloudKit 要求所有非可选属性有默认值（现基本满足） |

### 12.3 为什么现在就好切（设计铺垫）

给四个模型加 `syncId / syncUpdatedAt / syncDeleted` 的额外价值：**迁移时本地已有记录会被 CloudKit 首次上传，一般不丢**；但 **CloudBase 历史与 iCloud 历史不会自动合并**，两套云端各自独立。

### 12.4 老数据迁移时机（唯一坑）

- 最佳时机：早期用户少时直接切；或
- 一次性迁移脚本：先把 CloudBase 历史 `pull` 回本地，再让 CloudKit 接管上传，避免两朵云各存一份历史。

### 12.5 建议

现在**别切**，先用 CloudBase 跑通验证（免费账号唯一可行路径）。待决定上架 App Store（本身需付 $99）且用户集中在 Apple 生态时再切 iCloud 最划算——手写同步代码可全删。切换时提供「`CloudSyncManager` → CloudKit」改造 + 一次性迁移脚本。

---

## 13. M5 云同步 · 实际落地与验证（2026-07-17）

**状态**：✅ 已端到端真机验证通过（免费 Apple ID + 真机）。

### 13.1 实际技术实现
- **客户端**（`AIA/AIA/CloudSyncManager.swift`，`@MainActor` 的 `ObservableObject` 单例）：
  - push：每次同步把本地全部四条记录（账单/待办/饮食/健康）按 `syncId` 打包成 `{id,type,updatedAt,deleted,payload}` 数组上传。
  - pull：传 `since`（上次同步时间），拉取其它设备更新的记录，按 `syncId` 在本地 upsert，冲突按 `updatedAt` 后写胜出。
  - 端点派生：同步地址 = `RecognizeService.endpoint` 把 `/recognize` 换成 `/sync`，**写死在 `RecognizeService.swift` 两处**（主 App + ShareExtension），换环境须改代码重跑。
- **服务端**（`云函数/aia-sync/index.js`，CloudBase 云函数 + `wx-server-sdk`）：
  - 集合 `aia_records`，`_id` = 客户端生成的 `syncId`(UUID 字符串)；`userId` 隔离多设备账号。
  - `action=push`：逐条 `doc().get()` 判存在 → 存在且本地更新时间更新则 `update`，不存在则 `set`；`action=pull`：按 `userId + updatedAt>since` 查询返回。
  - HTTP 触发（`/sync`）解析 `event.body` JSON，直接返回 `{ok,...}`（与 `recognize` 一致）。

### 13.2 验证结果
- 真机 App 点「立即同步」显示「已同步 · 上传 6 条 / 更新 0 条」。
- curl 直接 push 一条测试记录，远端返回 `{"ok":true,"upserted":1,"total":1}`；再 pull 同账号能取到该记录。
- **结论**：本地→云端、云端→本地双向链路均通，多设备共享数据可行。

### 13.3 踩坑记录（已修复）
- **`CloudSyncManager` 编译崩溃**：原 `ObservableObject` 类未加 `@MainActor`，与 SwiftUI `@StateObject` 并发推断冲突，报 `Failed to produce diagnostic for expression`；同时 `ContentView` 对同步属性误用 `await`。修复：类加 `@MainActor`，去掉 `await CloudSyncManager.shared/autoSync`，各方法 `@MainActor` 收敛到类级。
- **云函数 `doc().set()` 报 `-501007 不能更新_id的值`**：早期代码在 `set({data:{_id:r.id,...base}})` 里多写了 `_id`。CloudBase / 微信云开发的 `doc().set()` **不允许 data 里再写 `_id`**（与部分 MongoDB 驱动不同），`doc(r.id)` 已指定 id。修复：改为 `set({data: base})`。
  - **排查方法**：函数调用返回 `ok:true` 但 `upserted:0` 时，必须展开 CloudBase 函数日志看单条 `console.error`，仅靠 App 端返回看不出原因。
- **环境一致性**：`recognize` 与 `aia-sync` 必须同 CloudBase 环境；`/sync` 触发器的「集成响应」开关须与 `/recognize` 一致，否则 App 解析失败。

### 13.4 已知限制（后续迭代）
- 本地删除是即时硬删，**删除暂不跨设备传播**（墓碑同步待做）。
- 同步账号靠 UUID 不可猜性隔离，无 Token 鉴权（MVP 简化）。
