# AI 助理 · 真机验证指南

把这一整套（分享扩展 / 无感截图 / 营养校正 / HealthKit / 净热量）在 iPhone 上跑通验证的步骤与清单。

## 0. 前置条件与重要提醒（务必先看）

- 需要：Mac + Xcode 16+ + iPhone + 数据线。
- ⚠️ **付费 Apple Developer 会员（$99/年）是硬门槛**。HealthKit 与 App Groups 属于「付费能力」：
  - 免费 Personal Team 只能把 App 装到真机，**无法启用 HealthKit / App Groups**。
  - 因此 **分享扩展、无感截图、HealthKit、净热量「消耗」** 这四项必须付费账号才能验证。
  - 免费账号只能验证【阶段一 A】：App 内「测试识别」+ 营养校正 + 待办时间 + 摄入/宏量营养素。
- 若暂无付费账号：先按【5-A】验证能跑的，其余等开通会员后做【5-B/C/D】。
- 免费账号签名的 App 每 **7 天**需重装一次（付费账号证书有效期 1 年）。

## 1. 打开工程并签名

1. 双击 `AIA/AIA.xcodeproj`。
2. 选中主 App target `AIA` → **Signing & Capabilities**：
   - Team 选你的 Apple ID（付费账号）。
   - Bundle Identifier 保持 `com.daxing.aia.AIA`（或改成你自己的，但全文要一致）。
   - 勾选 **Automatically manage signing**。
3. 选中 `ShareExtension` target → 同样选同一个 Team，Bundle Identifier 为 `com.daxing.aia.AIA.ShareExtension`。

## 2. 开能力（关键，付费账号才有）

**主 App target（`AIA`）：**
- 加 **App Groups** → 勾选/新建组 `group.com.daxing.aia`（必须和 `ScreenshotStore.swift` 里的 `id` 完全一致）。
- 加 **HealthKit**。

**ShareExtension target：**
- 加 **App Groups** → 勾选**同一个** `group.com.daxing.aia`。

> 想换 App Group id：需同步改主 App 与 ShareExtension 里的 `ScreenshotStore.swift` 两处（搜索 `group.com.daxing.aia`）。

## 3. 运行到真机

1. 数据线连 iPhone；Xcode 顶部 Scheme 选 `AIA` + 你的设备（**不要选模拟器**）。
2. 首次：iPhone 弹「信任此电脑」；到 `设置 → 通用 → VPN与设备管理 → 信任开发者`。
3. 点 Run（▶）。

## 4. 首次打开授予权限

- **HealthKit**：弹窗点「允许」（读步数/活动消耗，写体重/身高/心率/摄入热量）。
- **相册**（仅当你用「快捷指令无图兜底」才需要）：允许「读取」。

## 5. 逐项验证清单

### A. App 内测试识别（免费账号也能验）
- **食物截图**：首页点「测试识别」→ 选图 → 确认页「食物」区应出现绿色提示
  `已按营养库『米饭』校正：每100g 116 kcal`（匹配到才显示，未匹配显示灰色「使用模型估算」）；
  点「存入」→ 饮食记录列表 + 首页卡片「摄入 / 宏量营养素」更新。
- **账单截图**：商户/金额/分类预填；存入 → 账单列表 + 首页「本月 ¥」更新。
- **待办截图（有日期无时间）**：确认页时间应**默认 8:00，不是 00:00**；有显式时间则保留。
- **健康截图**：指标/数值；存入 → 健康管理列表。

### B. 分享扩展（需付费 + App Groups）
- 在 iPhone 任意 App（如「照片」）打开一张截图 → 点系统「分享」→ 划到「AI 助理」→
  后台识别 → 回到本 App，自动弹出确认页。
- 没弹：检查两个 target 的 App Group 是否一致、都已勾选。

### C. 无感截图（需付费 + App Groups + 真机）
- 「快捷指令」App → 自动化 → + → 选「截屏」→ 添加动作「**识别截图**」（你 App 的意图）→ 关闭「运行前询问」。
- 截一张图 → 稍后打开 App → 弹出确认页。
- 兜底：直接运行该意图不传图 → 自动从相册读最近一张「截图」（需相册权限）。

### D. HealthKit（需付费）
- 打开 iPhone「健康」App → 浏览：
  - 身体测量：出现你保存的体重 / 身高；
  - 心率：有记录；
  - 营养 → 能量 → 「膳食能量」：有摄入值（来自食物保存）。
- 首页卡片「**消耗**」显示今日活动消耗（来自健康 App 的运动数据）；步数显示。
- **净热量 = 摄入 − 消耗**，随两项刷新。
  > 注：「消耗」只计「活动消耗」(active energy)，不含基础代谢，所以不运动时数值偏低属正常。

## 6. 常见坑

- **App Group 不一致** → 分享/无感结果主 App 读不到（弹不出确认页）。两个 target 必须同一个组。
- **没开 HealthKit 能力** → 首页卡片「消耗」恒为 0、写入静默失败。
- **免费账号硬上 HealthKit/App Groups** → 签名或运行报 entitlement 错误，功能不可用。
- **扩展 Info.plist 被重复拷贝** → 到 ShareExtension 的 Build Phases → Copy Bundle Resources 里移除 `Info.plist`。
- **模拟器不可用**：HealthKit、扩展、相册自动化在模拟器全部无效，必须真机。

## 7. 验证通过判断

A–D 全部跑通 = 整套闭环验证完成。若只到 A，则至少核心识别 + 营养校正 + 待办时间规则已验证，剩余依赖付费会员。
