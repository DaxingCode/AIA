# 2026-07-21 工作日志

- **23:3x 开发「删除 App 重装后数据还在」功能（跨重装云同步恢复）**：用户希望删除 App 重装、重新登录后历史数据仍在。根因有三：① 登录态 + 同步账号(sync_user_id)都存 `UserDefaults`(@AppStorage)，iOS 删除 App 会清空 → 重装后丢失；② 微信登录把一次性 OAuth `code` 当 `userId`(`wechat_<code>` 随机)，每次登录开新云端空间、旧数据孤立；③ 云端 `aia-sync` 的 `pull` 用 `.limit(100)`，记录>100 条时全量恢复会静默丢数据。修复：新增 `AIA/AIA/KeychainHelper.swift`（iOS Keychain，`kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` —— 同设备重装保留、不随备份迁移新机）；`AuthManager.login` 写 Keychain、`logout` 清 Keychain、新增 `restoreFromKeychain()`(启动时恢复 isLoggedIn/userId 等) 与 `stableWeChatId`(Keychain 内稳定匿名 id，真实环境应换 unionid)；`AppDelegate.scene(willConnectTo:)` 先 `restoreFromKeychain()` 再决定首屏，恢复成功则直接进主页并 `syncAfterLogin` 全量拉云端；`LoginView` 微信分支改用 `stableWeChatId`。云端两份 `云函数/aia-sync/index.js` + `原生代码脚手架/云函数/aia-sync/index.js` 的 `pull` 改为 skip/limit(100) 分页拉全。`xcodebuild` BUILD SUCCEEDED；node 语法校验通过；分页用 mock DB 模拟 50/100/101/250/305 条均 PASS。**待用户操作**：① 重装新 App；② 重部署 aia-sync（运行 `云函数/aia-sync/deploy.sh cloud1-d1ga55pizf294dbe9-1445590522`，需浏览器扫码登录 CloudBase CLI）；③ 重装后若 Keychain 有旧身份会自动免登录并进主页拉数据，否则手动登录(手机号/Apple 因 id 确定性兼容旧数据，微信走新稳定 id)。注意：换手机需重新登录（Keychain 不跨设备），微信真实环境应改 unionid 跨设备。

- **21:4x 修复支付宝账单识别（v3，跨行 OCR 兜底）**：用户重装后仍错(商户19:09/时间状态栏21:41/分类交通)。根因 = Vision OCR 对支付宝详情页输出**两栏分行布局**：「支付时间」与值相隔~11行、「收款方全称」与值相隔~11行、顶部「阿里云」与「-10.00」分行；而 v2 修复只覆盖同行场景。三处失效：①`extractMerchant` 标签正则 `\s*` 匹配换行把下一行「订单号」误当值；②`extractAmount` 噪声窗口误杀 `-10.00` 选中状态栏21；③`extractISODateTime` 两栏兜底只扫4行够不到11行。修复(软件工程师寇豆码)：`extractMerchant` 正则改 `[\t ]*` 防跨行 + 新增跨行标签兜底(标签后15行内找有效商户) + blocklist 加订单号/流水号；`extractAmount` force 模式新增「金额独占一行且带负号/两位小数」强信号(score=100/95,跳过噪声检查)；`extractISODateTime` 兜底窗口 4→16 行；`guessCategory` 加云厂商完整名(阿里云计算有限公司等)；`localParseBill` 策略改**宽松兜底**(只要本地能确定金额即返回,商户空→"账单"、时间空→当前时间,**不再因 merchant/time 缺失 fallback 云端视觉模型**)。QA严过关回归：**24项全PASS**(含真实OCR端到端样例E/F), 路由 NoOne。`xcodebuild` BUILD SUCCEEDED。纯 App 本地代码,未动云端(l 版保持)。用户需**重装新 App** 后复测阿里云账单(预期:商户=阿里云/金额10.0/时间19:09:35/分类云服务)。QA 遗留建议:跨行兜底极端边界(商户距标签>15行)、ISO8601 输出 UTC 而非+08:00(预存行为,绝对时刻正确)。
- **20:5x 修复支付宝「阿里云」账单识别（v2，正规团队流程跑通）**：部署 l 后金额对(10.0)但商户/时间/分类仍错。根因 = **App 本地解析层 bug（非云端）**：① `tryExtractDateTime` 读年份后未推进捕获组索引(`idx=2`缺失)，致 `YYYY-MM-DD` 因 month=2026 判非法返回 nil；② `extractMerchant` 两栏布局Vision OCR 把「收款方全称」与「阿里云计算有限公司」分行且「全称」误识为「金称」，标签正则误捕「金称」当商户，上方兜底又可能吞时间标签；③ `extractISODateTime` 跨行「支付时间」未命中。修复：`idx=2` 修正、`extractMerchant` 加 3 字守卫+金额行上方兜底+同行商户模式+`isLikelyTime`/blocklist、增强 `extractISODateTime` 带标签优先+两栏兜底。`xcodebuild` BUILD SUCCEEDED。**另修复 QA 回归发现的 2 个预存缺陷**：`extractAmount` force 模式把日期月份(-07)误当金额(新增 `isDateContext` 跳过日期串内数字)、`extractMerchant` 上方兜底未排除时间标签/日期串(blocklist+isLikelyTime 扩展)。**走正规团队流程**（`software-bugfix-alipay-v2` 团队，software-engineer + software-qa-engineer 均成功派发并协作，本轮确认子 agent 可用——之前"不可用"是派发漏 team_name + 偶发抖动），QA 两轮回归 PASS=12/FAIL=0，路由 NoOne 可放行。纯 App 本地代码，未动云端(l 版保持)，用户需**重装新 App** 后复测。
- **19:4x 修复支付宝「阿里云计算有限公司 -10.00」账单四字段全错（商户19:09/金额7.0/时间19:34/分类交通）**：根因 = 本地 `localParseBill` 对支付宝「金额不带¥符号(-10.00)」布局提取失败→返回nil→走云端视觉模型，而云端 `SYSTEM_PROMPT_IMAGE` 对支付宝详情页三陷阱(金额来源/时间=状态栏19:34/商户=时间串19:09)无专门约束，模型看错。修复：云端 `index.js` 新增**规则16**（支付宝/微信账单详情页字段提取铁律：金额只从明确金额区或顶部-10.00读取、时间只从"支付时间"行读取绝不用状态栏时间、商户只从"收款方全称"读取绝不用时间串、分类云服务）。FN_VERSION 升 `20260721l`，已打包 `recognize-20260721l.zip`，`node -c` 语法校验通过。**注意：软件工程师子 agent 本次持续不可用（两次带team_name派发0秒失败），由主理人直接实现并编译/打包验证。** 仅改云端，用户只需重新部署 l 版 zip、不用重装 App；旧错误缓存(7.0/19:09)随 FN_VERSION 升级自动失效。用户需在 App 内删掉之前误建的错误账单(商户19:09/金额7.0)，再重新识别该图。

- **19:3x 用户已装新 App + 部署 recognize-20260721k.zip → h/i/j/k 四套修复 + 支付宝「阿里云」商户/分类修复全部生效，进入真机复测阶段**：复测清单 ①「晚餐吃了越南炸春卷100克」应有热量(非0)；② 培训通知截图归待办(非账单)；③ 阿里云¥10.0 无时间截图→时间=当前时刻(非00:00)、商户=阿里云、分类=云服务☁️；④ 阿里云账单(带状态栏时间)→商户不再误识为"19:11"。

- **19:3x 诊断「software-engineer 子 agent 不可用」根因 = 调用方式错误（已解决）**：用户问为何软件工程师子 agent 不可用。用 expert-manager 查 `software-company` 专家包（`~/.workbuddy/plugins/marketplaces/experts/plugins/software-company`）：plugin.json 完整、`teamInfo.memberAgents` 含 software-engineer、agents/software-engineer.md frontmatter 正确；`.codebuddy-plugin` 是该市场统一命名（非异常）；marketplace.json 只登记 team 一条、不单列成员（正常）。**真正根因**：software-engineer/架构师/PM/QA 都是 `software-company` 团队的**成员**，必须**在 TeamCreate 建团后、用 `Agent` 带 `team_name` + `name` + `subagent_type` 当队员派发**；之前 j 修复那次我虽建了 team 但 `Agent` 调用**漏了 team_name**，运行时把它当独立顶层 agent 去找 → 报 "not available"。实测：TeamCreate(software-diag-test) → Agent(team_name:"software-diag-test", name:"software-engineer", subagent_type:"software-engineer") 派发**成功**，工程师回话正常。**结论：后续走软件团队 SOP/快速/BugFix 时，务必 TeamCreate 后带 team_name 派发成员，不要裸调 Agent(subagent_type)。** 已删测试团队 software-diag-test。

- **19:2x 修复支付宝账单「阿里云」被误识为商户"19:11"且分类"交通"（App 代码，待重装）**：用户截图支付宝「支付成功」页，商户明明是「阿里云」，识别结果确认页显示商户=19:11（状态栏时间）、分类=交通。根因：① `AIA/AIA/RecognizeService.swift` 的 `extractMerchant` 标签规则未命中支付宝「商户与金额同行」布局（阿里云   ¥10.00），回退到「首行文本」兜底，首行正好是状态栏时间 19:11；② `guessCategory` 只接收 OCR 全文，被截图下方"支付有礼"里的"支付宝打车券"带偏成交通。修复：① `extractMerchant` 新增 `isLikelyTime` 拒绝时间串作商户；新增「金额同行商户」正则 `^(.+?)\s*[¥￥]\s*\d` 捕获「阿里云   ¥10.00」这种布局；新增非商户词黑名单（支付成功、支付有礼、完成、返回、支付方式等），命中这些词也跳过；首行兜底同样加时间/黑名单过滤。② `guessCategory` 改为 `guessCategory(_ merchant: String, _ text: String)`，优先按 merchant 判断，新增 cloudMerchants + 全文云关键词，命中则直接返回「云服务」，排在交通判断之前，避免被「打车券」覆盖。`localParseBill` 同步改调用。App 编译验证中。**无新 cloudfunction 版本，改动在 App 端，需重编译安装 App；同时需部署 k 云函数（分类映射）+ 之前 j（时间兜底）才能完整生效。** 团队成员 software-engineer agent 本环境不可用，由主理人直接实现修复。

- **19:2x 云端 prompt 补「云服务」分类规则（待部署 k + 重装 App）**：用户要求把阿里云/腾讯云/华为云/云计算/云服务器等归为独立「云服务」分类。改动：① 云端 `原生代码脚手架/cloudfunctions/recognize/index.js` 的 `SYSTEM_PROMPT_IMAGE` 新增**规则 15**（云厂商/云资源购买→category:"云服务"，不要归数码/购物/其他）；`SYSTEM_PROMPT_TEXT` 同步新增规则 16（同样的云服务分类约束，覆盖聊天文字输入路径）。② **App 端 `AIA/AIA/BillCategoryHelpers.swift` 补「云服务」映射**：原「数码/电子/软件」分支含"云""服务器"等词会归 📷，现抽出独立 `case "云服务","云计算","云服务器","云存储": return "☁️"`（icon），并从数码分支移除"云""服务器"两词避免冲突；`color(for:)` 新增 `云服务` 分支返回天蓝 `Color(hex:0x0EA5E9)`。`FN_VERSION` 升 `20260721k`，已打包 `recognize-20260721k.zip`，node -c 通过。`BillCategoryHelpers.swift` 改动属 App 代码，需重编译安装才生效。**注：编辑账单页分类是自由 TextField，无写死选项列表，加映射即全链路生效（识别/列表/月报图标色均走 BillCategoryHelpers）。**

- **19:1x 修复账单时间未识别时默认当前时间（非 00:00，待部署 j + 重装 App）**：用户截图「阿里云 ¥10.0」识别结果时间显示为 00:00，要求"时间没识别出来就默认记当前时间"。根因：`AIA/AIA/RecognizeService.swift` 的 `localParseBill` 在 `extractISODateTime` 返回 nil（截图无任何时间线索）时，回退 `todayAtMidnight`（今天 00:00），注释原写着"绝不使用当前时刻"。改为回退 `.now`（当前时间），删除不再使用的 `cal`/`todayAtMidnight`。云端同步加固：`原生代码脚手架/cloudfunctions/recognize/index.js` 的 `SYSTEM_PROMPT_IMAGE` 规则 4 新增：账单截图完全无时间信息时 time 必须填当前时间 {CURRENT_TIME}、绝不许写 00:00:00。`FN_VERSION` 升 `20260721j`，已打包 `recognize-20260721j.zip`，node -c 通过。App 重编译验证中。**注：截图里把「阿里云」误识为「交通」分类——该分类来自云端模型（非本地 guessCategory），本次未改，属待优化项，可后续在云端 prompt 补云服务类关键词。** 团队成员 software-engineer agent 本环境不可用，由主理人直接实现修复。

- **19:0x 修复「培训通知仍被误识为账单」根因 + 加固（待部署 i + 重装 App）**：用户真机反馈「招募训练营报名」培训通知截图**依然**被识别成账单 ¥18.0，并反问"本地识别不了怎么不上传云端？"。根因澄清：架构本身在 `localParseBill` 返回 nil 时会回退云端（文本模型→视觉模型），但 g 版本地拦截条件是 `if hasEventSignal && !hasBillSignal { return nil }`——通知里一旦夹带金额（如报名费¥18）就 `hasBillSignal` 为真，本地仍会声称账单并短路云端；更关键的是该拦截是 **App 代码**，用户之前只部署了云端 g、没重装 App，所以装的是旧版本地规则。加固：① `AIA/AIA/RecognizeService.swift` 的 `localParseBill` 改为「只要命中 eventSignals 一律 `return nil` 交云端」，不再叠加 `!hasBillSignal`，并扩充 eventSignals（加参训/招募令/活动报名）；② 云端 `原生代码脚手架/cloudfunctions/recognize/index.js` 的 `SYSTEM_PROMPT_TEXT` 新增规则 14（与视觉规则 14 对齐）：训练/通知/招募类文字必须归 todo、绝不为 bill，数字当金额噪声过滤，使便宜的文本模型路径也能正确分类；原饮食+金额规则顺延为 15。`FN_VERSION` 升 `20260721i`，已打包 `recognize-20260721i.zip`，node -c 语法校验通过。App 重编译验证中。**关键提醒用户：必须重新编译安装 App（否则本地旧规则仍在）+ 部署 i，培训通知才会归待办。**

- **19:0x 修复「越南炸春卷」陌生食物仍未记录（Task #214，待部署 h）**：用户真机反馈「晚餐吃了越南炸春卷100克」经 f/g 后仍记成热量 0 或完全没记录。根因：通用 `parseText` 对本地库没有的陌生食物，仍可能返回 none 或营养缺失，fallback 落 0 kcal 记录。修复方案——新增**专用食物营养查询链路**：① 云函数新增 `SYSTEM_PROMPT_QUERY_FOOD`（只估每100g营养，不走通用意图识别）+ `handleQueryFood` + 入口 `body.mode === 'queryFood'` 路由；② `AIA/AIA/RecognizeService.swift` 新增 `queryFood(name:)`（POST `{mode:"queryFood", foodName, provider:"qwenText"}`，解析 CloudResponse→FoodRef）；③ `AIA/AIA/ChatView.swift` 新增 `createFoodFromCloud(text:recentMessages:)`：食物未命中本地 `NutritionLibrary`/`FoodMeta` 时，**优先** `queryFood` 查营养 → 写入 `FoodMeta` 缓存 → 建 `FoodEntry`；`queryFood` 失败才兜底通用 `parseText`/fallback。食物分支调用改为 `await createFoodFromCloud(...)`。`FN_VERSION` 升为 `20260721h`，已打包 `recognize-20260721h.zip`。App 编译验证：xcodebuild BUILD SUCCEEDED。**待用户部署 h 后真机复测「越南炸春卷」应出正确热量。**

- **18:52 部署 recognize-20260721g.zip 到 CloudBase（用户确认）**：包含 f（文本 prompt 食物字段返回每100g营养、明确饮食意图必须返回 food）与 g（微信群聊/培训/活动通知归 todo、金额噪声过滤扩展时间与群人数/楼层）全部改动，整文件部署无需分别部署 f。下一步：真机复测「招募训练营报名」通知截图应归待办+「越南炸春卷」食物查询应出热量。

- **18:41-18:43 修复微信群聊培训通知被误识别为账单**：用户截图微信群里的「招募训练营报名」通知（含培训时间、地点、课程安排），被识别成账单 ¥18.0。修复：① `AIA/AIA/RecognizeService.swift` 的 `localParseBill` 增加 `eventSignals` 反向信号词（培训/训练营/招募/报名/上课/课程/讲座/会议/活动时间/地点/群聊/通知/海报等），命中且没有强账单信号时直接返回 nil，避免本地规则把通知类截图误判为 bill；② 云函数 `原生代码脚手架/cloudfunctions/recognize/index.js` 的 `SYSTEM_PROMPT_IMAGE` 新增规则 14：微信群聊通知/活动海报/培训会议/招募报名截图必须识别为 todo，标题用活动主题、due 取活动开始时间，绝不要识别为 bill；③ 同步扩展金额噪声过滤说明：时间（如 18:34、9:00-18:00）、群人数（如 (36)）、楼层/教室号（如 47楼）都不是金额。`FN_VERSION` 由 `20260721f` 升为 `20260721g`，已重新打包 `recognize-20260721g.zip`。App 编译验证：xcodebuild BUILD SUCCEEDED。下一步：部署 recognize-20260721g.zip 到 CloudBase 后真机复测该培训通知截图。

- **18:39-18:42 优化编辑账单页 UI**：用户截图编辑账单页，字段只有 bare 值（17:14 / 23.00 / 餐饮），缺乏标签和视觉层级。改动 `AIA/AIA/EditSheets.swift` 的 `EditBillView`：① 将原生 `Form/Section` 改为卡片式布局（`ZStack` + `ScrollView` + `.card()`），背景用 `AIATheme.fillSoft`；② 信息卡片整合商户、金额（左侧 ¥ 符号）、分类、时间四行，每行都有图标 + 中文标签；③ 备注单独卡片，带「备注」小标签；④ 收入/支出切换单独卡片，带图标；⑤ 保存按钮样式与编辑食物/待办统一。编译验证：xcodebuild BUILD SUCCEEDED。

- **18:31-18:38 本地食物库缺失时联网查询并沉淀到本地缓存**：用户截图显示「晚餐吃了越南炸春卷100克」被记为热量 0 并提示补全，要求本地食物库没有时联网查询并把数据补到本地。实现方案：① `AIA/AIA/Models.swift` 新增 `FoodMeta` SwiftData 模型（归一化食物名、每100g营养、来源、命中计数、最后更新时间），纯本地不上云；② 新建 `AIA/AIA/FoodMetaStore.swift` 封装 `lookup`/`upsert`/`upsertFromTotal`；③ `AIA/AIA/ChatView.swift` 改造 `createFoodLocally`：先查硬编码 `NutritionLibrary`，未命中再查 `FoodMeta`，命中即用缓存创建记录；④ `saveFromResult` 的食物分支（新建/更新）在写入 `FoodEntry` 后，把云端返回的每100g营养沉淀到 `FoodMeta`，下次同类食物可直接本地命中；⑤ SwiftData 升级 v6：`AIAMigrationPlan.swift` 新增 `SchemaVersion6` 与 `migrateV5toV6`，`AppPersistence.swift` 的 `currentSchemaVersion` 改为 6；⑥ 同步修正云函数 `原生代码脚手架/cloudfunctions/recognize/index.js` 的 `SYSTEM_PROMPT_TEXT` 规则 6：明确食物字段返回**每100g**营养（与 App 端 `base* / ratio` 换算逻辑一致，修复之前文本分支可能把总量当每100g或反向的问题），并新增规则 7：明确饮食意图时必须返回 food 类型并估算营养，不能保守返回 none。`FN_VERSION` 由 `20260721e` 升为 `20260721f`，已重新打包 `recognize-20260721f.zip`。编译验证：xcodebuild BUILD SUCCEEDED。下一步：部署 recognize-20260721f.zip 到 CloudBase 后真机复测「越南炸春卷」等食物。

- **14:51 部署 recognize-20260721e.zip 到 CloudBase（用户确认）**：超市小票识别修复（规则 11 增加超市/便利店/收银/购物/餐饮小票、外卖订单；金额优先级增加应收/实收/合计等标签后数字；列举订单号/会员卡号/流水号/开票号/税号/客服电话非金额；商户 cleaning 加小票店名提取）。App 本地 `RecognizeService.swift` 已对应改 `billSignals`/`extractAmount`/`extractMerchant`/`normalizeMerchant`。下一步：真机复测永辉超市小票（金额 12.90 元）应正确识别为 bill。

- **14:44-14:50 优化编辑食物页 UI**：用户截图编辑食物页，反馈营养区只有 62/10/0/2 四个 bare 数字，看不出分别代表什么。改动 `AIA/AIA/EditSheets.swift` 的 `EditFoodView`：① 将原生 `Form/Section` 整体改为与待办编辑页一致的卡片式布局（`ZStack` + `ScrollView` + `.card()`），背景用 `AIATheme.fillSoft`；② 食物名称单独一个白色卡片，带「食物名称」小标签；③ 餐次、重量合成一个卡片，餐次用 `Menu` 选择器，重量右侧显示 `g` 单位；④ 营养信息单独一个卡片，标题「营养成分（按当前重量）」，热量/蛋白质/碳水/脂肪每项都有左侧彩色图标（琥珀色食物色底）+ 中文标签 + 右对齐数值输入框 + 单位（kcal/g），彻底消除歧义；⑤ 保持「改重量自动联动总量、改总量反推每100g基准」逻辑不变，`totalBinding` 与 `save()` 原样保留。编译验证：xcodebuild BUILD SUCCEEDED。

- **14:03-14:17 编辑食物改重量联动热量与营养**：用户截图编辑牛肉 50g，要求改重量时热量和营养自动变化。实现方案：① `AIA/AIA/Models.swift` 给 `FoodEntry` 新增 5 个可选字段：`weightGram` + `baseCalories/Protein/Carbs/Fat`（每100g基准）。② `AIAMigrationPlan.swift` 新增 `SchemaVersion5` 并在 `SchemaVersion4` 内嵌旧版 `FoodEntry`，避免 checksum 重复；`AppPersistence` 升级到 v5。③ 写入路径补全基准：`RecognitionSaver.autoSave`（图片识别）、`ChatView.createFoodLocally`（本地营养库）、`ChatView.saveFromResult`（云端食物，顺带修复原把每100g当总量的 bug）、`ResultConfirmView.applyAndSave`（确认页）。④ `CloudSyncManager` 双向同步新字段。⑤ `EditSheets.swift` 的 `EditFoodView` 改造：基本信息里的「份量」改为「重量(g)」数字输入；营养字段改为绑定到「当前重量下的总量」，内部保存每100g基准；改重量时总量自动按比例重算，改总量时也会反推基准。旧数据无基准时，用当前营养/重量反推兜底。编译验证：xcodebuild BUILD SUCCEEDED。

- **14:18-14:22 修复支付宝账单详情截图未识别**：用户截图支付宝「账单详情」显示「未识别到可记录的内容」。排查根因：① 本地 OCR 金额正则只匹配 `¥/￥数字` 或 `数字元`，但支付宝详情金额是 `-18.00`，无货币符号；② 商户标签只匹配「收款方:」，未覆盖「收款方全称:」；③ 云端 prompt 对「账单详情」「负数金额」「经营码交易」「**娟(个人)」脱敏商户覆盖不足。修复 `AIA/AIA/RecognizeService.swift`：① `localParseBill` 增加强账单场景词检测；② `extractAmount` 新增 `force` 模式，命中账单场景词后兜底匹配 `-18.00` / `18.00` 等两位小数数字，并过滤年份/超大金额；③ `extractMerchant` 新增「收款方全称」「对方账户」「对方户名」「商品说明」等标签，并加 `normalizeMerchant` 清理星号与「(个人)」后缀；④ `guessCategory` 增加「医疗健康」「经营码」等关键词。同时更新云函数 `原生代码脚手架/cloudfunctions/recognize/index.js`：SYSTEM_PROMPT_IMAGE 规则 11 补充「账单详情」、负数金额取绝对值、脱敏商户 cleaning；SYSTEM_PROMPT_TEXT 规则 3 补充负号金额语义。`FN_VERSION` 由 `20260721b` 升为 `20260721c`，已重新打包 `recognize-20260721c.zip`。App 编译验证：xcodebuild BUILD SUCCEEDED。下一步：部署 recognize-20260721c.zip 到 CloudBase 后真机复测。

- **14:25-14:30 优化图片查看页交互**：用户反馈全屏查看照片时右上角关闭按钮不明显，并要求增加轻点关闭、长按保存。改动 `AIA/AIA/LocalImageStore.swift` 的 `FullImageView`：① 关闭按钮改为白底黑叉圆形，带阴影，尺寸 32pt，更醒目；② 图片添加 `.onTapGesture { dismiss() }`，轻点任意处关闭；③ 添加 `.onLongPressGesture(minimumDuration:0.5)`，触发震动反馈后弹出确认 Alert；④ 保存使用 `UIImageWriteToSavedPhotosAlbum` + 内嵌 `ImageSaveDelegate`（`NSObject` 子类）处理 Objective-C 回调，成功/失败均底部 toast 提示。编译验证：xcodebuild BUILD SUCCEEDED。

- **14:32-14:38 疑似重复图片改为「照常自动入库 + 警告提示」**：用户要求"似乎已记录过的图片也自动记录，用户可编辑/修改/删除"。改动：① `AIA/AIA/DuplicateStore.swift` 新增 `DuplicateHint` 结构体（Codable，含 recognizedAt/summary/existingTypes），并更新注释（不再硬拦）。② `AIA/AIA/RecognitionSaver.swift`：`SavedSession` 新增可选 `duplicateHint`；`saveOrCheckDuplicate` 命中历史指纹时**不再返回 .duplicate 阻断入库**，改为照常 `autoSave` + 登记指纹，并把原记录摘要挂到 `session.duplicateHint`。③ `AIA/AIA/ResultConfirmView.swift` 新增 `effectiveDuplicateHint` 计算属性（优先 `existingSession.duplicateHint`，兼容旧 `duplicate` 参数）；顶部警告横幅、日期文本改读它；保存按钮文案统一为"保存"（不再"仍要记录"）；同步更新多处注释。④ `ContentView.checkScreenshotPending` 注释更新。编译验证：xcodebuild BUILD SUCCEEDED。注：`.duplicate` 枚举分支保留但已不可达（为兼容 `makeResultConfirmView` switch 穷尽性）。

- **14:34-14:37 修复金额误识别为信用卡号后四位**：用户截图支付宝账单详情，金额 -18.00 被识别成 6590（来自「中国银行信用卡(6590)」卡号后四位）。根因是 `extractAmount` 兜底匹配所有裸数字并取最大值，未排除卡号上下文。修复 `AIA/AIA/RecognizeService.swift`：重写 `extractAmount`，引入 `Candidate` 按置信度排序——明确货币（¥/元）最高；force 模式下带负号金额（-18.00）置信度 90；并新增 `isNoiseContext` 过滤，跳过含「信用卡/储蓄卡/银行卡/借记卡/尾号/卡号/手机/电话」等关键词上下文中的数字；同时排除 1900-2100 年份与超百万数字。同步更新云函数 `原生代码脚手架/cloudfunctions/recognize/index.js`：SYSTEM_PROMPT_IMAGE 规则 11 与 SYSTEM_PROMPT_TEXT 规则 3 都增加「不要把银行卡号/信用卡号后四位/手机号/订单号当金额」的强提示，并说明金额优先级（带¥/负号优先）。`FN_VERSION` 由 `20260721c` 升为 `20260721d`，已重新打包 `recognize-20260721d.zip`。App 编译验证：xcodebuild BUILD SUCCEEDED。下一步：部署 recognize-20260721d.zip 到 CloudBase 后真机复测。

- **14:38-14:42 修复超市收银小票识别为未识别**：用户截图永辉超市小票显示「未识别到可记录的内容」。排查根因：① 本地 `billSignals` 只覆盖移动支付场景词，未覆盖「小票/应收/实收/成交价/永辉/超市」等线下收银场景词，导致未进入 force 金额提取；② `extractAmount` 未过滤订单号/会员卡号/流水号/开票号/客服电话等长数字串，小票上 `8268900990031390`、`5177723124119321636`、`4200003112202607153880404554`、`0771-5622691` 都可能被误当金额候选；③ 云端 prompt 未把「超市小票/便利店小票/收银小票/购物小票/餐饮小票/外卖订单」明确列入 bill 类型。修复 `AIA/AIA/RecognizeService.swift`：① `billSignals` 新增超市/便利店/收银小票场景词；② `extractAmount` 扩展 `isNoiseContext`，过滤订单号/会员卡/流水号/开票号/税号/客服电话等上下文；新增 `nearAmountLabel` 辅助，给紧邻「应收/实收/合计/总计/成交价/微信支付/支付宝支付/现金」的金额候选加 25 分置信度；③ `extractMerchant` 新增小票顶部店名正则，如「永辉(95HC 南宁盛隆世界店)欢迎您」捕获「永辉」；④ `normalizeMerchant` 增加清理分店后缀（店/世界/广场/中心/HC）。同步更新云函数 `原生代码脚手架/cloudfunctions/recognize/index.js`：SYSTEM_PROMPT_IMAGE 规则 11 增加「超市/便利店/收银/购物/餐饮小票、外卖订单」必须识别为 bill；金额优先级增加「应收/实收/合计/总计/成交价/微信支付/支付宝支付/现金」后面的数字；明确列举订单号/会员卡号/流水号/开票号/税号/客服电话不是金额；商户 cleaning 增加小票顶部店名提取示例。`FN_VERSION` 由 `20260721d` 升为 `20260721e`，已重新打包 `recognize-20260721e.zip`。App 编译验证：xcodebuild BUILD SUCCEEDED。下一步：部署 recognize-20260721e.zip 到 CloudBase 后真机复测。

- **14:30-14:31 聊天页快捷意图栏新增「识别记录」入口**：用户截图要求在聊天页底部快捷意图 chips 中增加「识别记录」按钮。改动 `AIA/AIA/ChatView.swift`：在 `chip("加待办") { ReminderListView() }` 与 `feedbackChip` 之间新增 `chip("识别记录") { RecognitionRecordsView() }`。编译验证：xcodebuild BUILD SUCCEEDED。

- **13:56-13:57 相机默认 1 倍焦距 + 自动对焦**：用户要求相机启动默认 1 倍焦距并自动对焦。改动 `AIA/AIA/CameraView.swift`：新增 `CameraManager.configureFocusAndZoom(_:)`——锁定设备配置后设 `videoZoomFactor=1`，`focusPointOfInterest=(0.5,0.5)` 并用 `continuousAutoFocus`（回退 `autoFocus`），曝光同步跟随中央 `continuousAutoExposure`；在 `configureSession` 设备就绪后及 `toggleCamera` 切换镜头后各调用一次，保证启动/切镜头都为 1x 且持续自动对焦。编译验证：xcodebuild BUILD SUCCEEDED。

- **13:53-13:56 待办默认时间改为「无日期无时间→1小时后」**：用户要求设置提醒时若没说明日期和时间，默认 1 小时后提醒。改动 `AIA/AIA/ChatView.swift`：① `parseTodoCreate` 时间设置块——原「完全无日期时默认 09:00」改为「完全没说明日期且无时刻/时段词时，due = now+1h」；有日期无时间或与有具体时刻/时段词的逻辑不变。② 云端 todo 路径 `saveFromResult` 中 `RecognitionResult.date(...) ?? .now` 兜底改为 `?? now+1h`，避免云端未返回日期时立即过期。编译验证：xcodebuild BUILD SUCCEEDED。

- **13:18-13:22 修复「早上吃的」被记到午餐**：用户说「早上吃的200克燕麦粥」被记录为午餐。根因是 `mealFromText` 只识别「早餐/早饭」等含「餐/饭」的词，没覆盖「早上/中午/晚上/今早/今晚/正午」，导致当前时间 13:18 按默认规则落入午餐。修复 `AIA/AIA/ChatView.swift`：`mealFromText` 增加上述时段词映射；同时 `parseFoodNameAndWeight` 的餐次词清洗也同步加入这些词，避免混入食物名。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:44-12:47 气泡随机切换加入过渡动画**：用户要求随机切换别太生硬。在 `AIA/AIA/ContentView.swift` 的 `aiSummarySection` 新增 `rotatingBubble(messages:accent:)`：每 3 秒从候选随机取 idx，给气泡加 `.id(idx)` + 不对称 transition（新→从底部滑入淡入、旧→向顶部滑出淡出）+ `.animation(.easeInOut(duration:0.45), value: idx)`，实现平滑切换。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:41-12:42 修复气泡随机切换不生效（Timer 被反复重建）**：用户反馈"怎么没有随机切换"。根因是 `.onReceive(Timer.publish)` 挂在 computed property 返回的 view 上，每次 body 因 `@Query`/HealthKit 重算时 Timer 被取消重建，频率高于 3 秒则永不到点。改用 `TimelineView(.periodic(from:.now, by:3))` 作为独立周期 driver，内部每周期随机取 index；移除不再需要的 4 个 `@State idx`。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:49-12:54 气泡切换动画改为「转轴滚动」**：用户要求气泡内部像转轴一样滚动切换（而非整条淡入淡出）。改造 `AIA/AIA/ContentView.swift`：`aiSummarySection` 去掉外层 `TimelineView` 包裹；新增独立 `RollingBubbleView`（struct，自管 `@State` Timer，`Timer.publish(every:3).autoconnect()` + `.onReceive`）：气泡框固定不动，内部用 `ZStack` 叠两层文本，每 3 秒 `display→incoming` 切换，旧内容 `offset.y` 0→−rowH、新内容 rowH→0，`.animation(.easeInOut(duration:0.5))` 形成上滚转轴感；动画结束 0.5s 后把 `display=incoming` 复位（内容相同无缝）。顺序滚动（display+1）更贴合"转轴"意象。加 `import Combine`；删除不再使用的 `summaryBubble`。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:56-13:01 气泡改为「轮流滚动、一次一条」**：用户要求四条不要同时滚动。改动 `AIA/AIA/ContentView.swift`：把每条各自的 Timer 调度改为全局共享调度器——`ContentView` 加 `@State rollSlot=0` + `@State rollTimer=Timer.publish(every:3).autoconnect()`，`aiSummarySection` 用 `.onReceive(rollTimer)` 每 3 秒 `rollSlot=(rollSlot+1)%4`；每条 `rollingBubble` 接收 `active: rollSlot==0/1/2/3`，仅当 `active` 变 true（`.onChange` + 首条 `.onAppear`）时才 `roll()` 一次。`RollingBubbleView` 删除内部 Timer，只保留转轴滚动视觉（active 触发）。效果：每 3 秒只滚动当前轮到的那一条，四条依次轮流；`count<=1` 的气泡轮到时跳过（不动）。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:39-12:40 气泡内部改为 3 秒随机切换一条**：用户要求气泡滚动区域每 3 秒随机切换一条内容。修改 `AIA/AIA/ContentView.swift` 的 `aiSummarySection` Timer 触发逻辑：把饮食/健康/账单/待办四个 index 的 `(index + 1) % count` 顺序递增改为 `Int.random(in: 0..<count)` 随机取。候选只有 1 条时保持静态。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:38-12:39 饮食气泡只轮播今天吃的食物**：用户要求"如果今天已记录食物，饮食气泡滚动展示今天吃的食物"。原 `AISummary.dietMessages` 在多餐时会追加一条"今日已记 N 餐"汇总，调整为其只返回每餐食物本身（一餐静态、多餐轮播）。空态文案不变。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:35-12:37 待办气泡轮播最近 5 条待办**：用户截图中宫格显示多条未来待办，但气泡只展示一条今天到期。修改 `AIA/AIA/UIComponents.swift` 的 `AISummary.todoMessages`：过滤条件从"仅今天到期"放宽为"所有未完成且未过期"，按到期时间升序取前 5 条，时间展示区分今天/明天/具体日期。无待办时仍显示"今天待办已全搞定"。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:33-12:35 修复「晚上提醒我吃饭」未创建待办的问题**：用户输入含"提醒"但 AI 只回复饮食总结。根因是 `ChatView.processNext` 中食物分支排在待办分支之前，"吃饭"触发 `isFoodLike` 后直接被食物分支吞掉。修改 `AIA/AIA/ChatView.swift`：把 `createTodoLocally` 提到 `isFoodLike` 之前，分流顺序改为「疑问句 → 待办 → 账单 → 食物 → 兜底」。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:28 修复 SwiftData 迁移崩溃（RecurringRule 新增字段必填导致）**：用户真机/模拟器启动报 `Validation error missing attribute values on mandatory destination attribute`（entity=RecurringRule, attribute=customUnitRaw）。根因是 v4 新增的 `cycleRaw`/`customValue`/`customUnitRaw` 为非 Optional 必填，旧 store 记录无这些值时 lightweight migration 失败。修改 `AIA/AIA/RecurringBillManager.swift`：把三个字段改为 Optional，计算属性 nil 时兜底 monthly/month，使用处加 `?? 1`。编译验证：xcodebuild BUILD SUCCEEDED。若仍无法启动，建议卸载 App 或重置模拟器让 store 重建。

- **12:25 首页气泡改为「四条固定 + 各自内部轮播」**：用户纠正——不是把所有气泡放一个轮播，而是饮食/健康/账单/待办四条都展示，每条内部轮播不同内容。
  - `UIComponents.swift` 的 `AISummary` 拆成 `dietMessages`/`healthMessages`/`billMessages`/`todoMessages` 四个候选数组函数（兼容旧 `messages` 保留）。
  - `ContentView.swift` 的 `aiSummarySection` 改为 VStack 四条气泡，各用 `@State` index，`.onReceive(Timer.publish(every:3))` 各自 index+1 取模（候选>1 才轮播，单条静态）。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **12:21 首页今日事项气泡改为轮播滚动**：用户要求把四条气泡做成轮播展示。修改 `AIA/AIA/ContentView.swift` 的 `aiSummarySection`：
  - 用 `TabView(selection:)` + `.tabViewStyle(.page(indexDisplayMode: .never))` 实现横向自动轮播。
  - 每条气泡一页，保留左侧类型色条，3 秒自动切换下一条。
  - 数据变化时重置到第 0 条并重启轮播 Timer；页面消失时清理 Timer。
  - 新增 `summaryBubble` 辅助函数与 `startSummaryCarousel`。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **12:20 运动时长改为展示准确分钟数（单位 min）**：用户截图显示运动时长为「—」。根因是 `exerciseTimeText` 在 0 分钟时返回「—」，且 ≥60 分钟时转成了 `1h30m` 形式。修改 `AIA/AIA/ContentView.swift` 的 `exerciseTimeText`：统一返回 `\(Int(health.exerciseTimeToday)) min`，0 分钟显示「0 min」，90 分钟显示「90 min」。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:18 宫格「点击」提示移到每个宫格正中**：用户要求把「点击」提示从右下角（仅空态显示）移到每个宫格中间展示。修改 `AIA/AIA/ContentView.swift` 的 `tile`：overlay 由 `.bottomTrailing` + `if isEmpty` 改为 `.center` 且所有宫格都显示（半透明 accent 胶囊）。编译验证：xcodebuild BUILD SUCCEEDED。

- **12:15 健康宫格下方三行标签重命名**：用户要求改为「运动时长 / 能量消耗 / 静息心率」。修改 `AIA/AIA/ContentView.swift` 的 `healthTile`：
  - 「运动」→「运动时长」
  - 「静息能量」→「能量消耗」（仍读取 SwiftData 里 metric 为「静息能量」的数据值）
  - 「静息心率」保持不变
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **12:08-12:10 二次排查并修复 ChatView 解析链路的同类 bug（4 处）**：
  - ① 本地记账 `createBillLocally` 不识别收入：「记一笔工资8000」被误记成支出。新增收入关键词表（工资/薪资/报销/退款/红包等），命中即 isIncome=true，category 落「收入」。
  - ② `extractAmount` 只取第一个数字：「买3个苹果花了15」误记 ¥3。改为优先带金额单位（元/块/￥/¥）的数字，否则取句末最后一个数字。
  - ③ 食物份量 `parseFoodNameAndWeight` 未识别「两」单位：「三两米饭」记成默认 100g（应为 150g）。unitWeights 增补「两」→ 50g（中文数字「两」=2 已能解析）。
  - ④ 待办 `createTodoLocally` 负面词「关闭/关掉」过宽：「提醒我晚上10点关闭wifi」被误杀。改为仅屏蔽「关掉提醒/取消提醒/删除提醒」等明确针对提醒本身的短语，「关闭」作为提醒内容时正常创建。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **12:05-12:07 排查并修复待办时间解析的同类问题**：
  - 根因排查：`parseTodoCreate` 的时段解析有三处隐患——①「今晚」缺失（→09:00）；②时段词默认值与具体时刻冲突，「晚上9点」会被错设为时段默认 20:00 而非 21:00；③常见时段词（晌午/午饭/拂晓/黎明/天亮/大早等）未覆盖。
  - 修改 `AIA/AIA/ChatView.swift`：
    - `timeOfDayKeywords` 增补「今晚/晌午/午饭/拂晓/黎明/天亮/大早/一大早/大清早」。
    - 新增 `parseClockTime`：解析「9点」「21:30」「下午3点半」「晚上9点」等，含 12 小时制 pm 偏移（下午/晚上/夜里等 +12）。
    - `parseTodoCreate` 时间设置改为优先级「具体时刻 > 时段词默认值 > 默认 09:00」。
    - 标题清理同步移除具体时刻表达（「9点运动」→「运动」）。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **12:01 待办空态文案改为「今天待办已全搞定」**：用户要求把首页气泡里的「今天没有待办，安排得不错 👍」改成「今天待办已全搞定」。修改 `AIA/AIA/UIComponents.swift` 中 `AISummary.messages` 的待办兜底文案。编译验证：xcodebuild BUILD SUCCEEDED。

- **11:44 CloudBase 部署确认**：`recognize-20260721a.zip`（四层省钱链路：本地 OCR+规则 → 文本模型 qwenText → 视觉模型兜底）已由用户上传并部署到 CloudBase，`FN_VERSION=20260721a` 已在真机生效。

- **12:00 修复气泡里仍显示已过期待办的问题**：
  - 根因：`AISummary.messages` 待办过滤用 `isDateInToday && !done`，未排除已过期（due < 现在）的待办，导致「2分钟后打开 app」类提醒时间过了还挂在气泡。
  - 修改 `AIA/AIA/UIComponents.swift`：待办过滤加 `due >= now`，只显示今天到期、未完成且未过期的；过期/完成则走兜底「今天没有待办，安排得不错 👍」。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:56 健康步数进度条改为与卡路里进度条同尺寸**：用户要求步数进度条大小和饮食卡路里进度条一致（高度 5）。
  - 修改 `AIA/AIA/ContentView.swift`：`sparkBars` 改为复用 `MiniBar(value:color:height:)`，height 设为 5，与饮食宫格的 `MiniBar` 一致；移除自定义 `GeometryReader` 实现。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:54 去掉待办宫格底部「今日待办 X」**：用户反馈底部数字重复，已移除 `todoTile` 的 `.overlay(alignment: .bottomLeading)`。今日待办数量仍由宫格右上角 badge「今日 X 项」展示。
  - 修改 `AIA/AIA/ContentView.swift`：移除 `todoTile` 底部 overlay。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:53 健康管理宫格横条改为今日步数进度条**：
  - 修改 `AIA/AIA/ContentView.swift`：将 `sparkBars` 从近7日步数柱状图改为今日步数进度条，进度 = `health.stepsToday / stepGoal(10000)`，已走部分用 `AIATheme.health` 填充，未走部分用灰色底色。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:52 待办宫格 badge 改为「今日 X 项」**：用户要求将待办宫格副标题从「最近 X 项」改为「今日 X 项」，数据取 `todayTodos.count`。
  - 修改 `AIA/AIA/ContentView.swift`：`todoTile` 的 `TileBadge` 文本从 `"最近 \(recentTodos.count) 项"` 改为 `"今日 \(todayTodos.count) 项"`。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:45 修复「中午提醒我运动」提醒时间被设为 09:00 的问题**：
  - 根因：`parseRelativeTime` 只处理「2分钟后」「1小时后」等偏移，没有处理「中午」「晚上」等时段词，导致无具体日期时落到默认时间 09:00。
  - 修改 `AIA/AIA/ChatView.swift`：
    - 新增 `timeOfDayKeywords` / `timeOfDayPattern` 与 `parseTimeOfDay(from:)`，支持早上/上午 08:00、中午 12:00、下午 14:00、傍晚 18:00、晚上 20:00、深夜 23:00 等。
    - 重构 `parseTodoCreate`：先解析日期/相对偏移，再叠加时段词设置时分；若仍无信息才默认 09:00。
    - 标题清理增加移除时段词，避免标题残留「中午运动」。
  - 编译验证：xcodebuild BUILD SUCCEEDED。

- **11:38 在「待办提醒」页新增「提醒设置 / 自动记待办」入口**：
  - 新增 `AIA/AIA/TodoToolsView.swift`：聚合页，含两个入口：
    - 「设置默认提醒时间」→ `NavigationLink` 到 `DefaultReminderSettingsView`。
    - 「截屏自动生成待办」→ 走 `NavigationRouter.shared.path.append(HomeRoute.autoSetup)`。
  - 修改 `AIA/AIA/ContentView.swift`：
    - `HomeRoute` 新增 `todoTools`。
    - `navigationDestination` 增加 `.todoTools → TodoToolsView()`。
  - 修改 `AIA/AIA/RecordsViews.swift`：在 `ReminderListView` 的 `ScrollView` 顶部新增入口卡片「提醒设置 / 自动记待办」，点击 `router.path.append(.todoTools)`。
  - 修改 `AIA/AIA/AutoRecognitionSetupView.swift`：完成按钮改为 `finish()`，若 `router.path.last == .autoSetup` 则 `removeLast()`（path 驱动场景），否则 `dismiss()`（兼容 NavigationLink 场景）。
  - 编译验证：`xcodebuild -project AIA/AIA.xcodeproj -scheme AIA -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` → **BUILD SUCCEEDED**。
：
  - 修改 `AIA/AIA/BillToolsView.swift`：在「数据导入」section 下方新增「自动记账」section，入口按钮标题「自动记账教程」，副标题「设置快捷指令，截屏自动记账单 / 待办 / 饮食」，图标用 `sparkles.rectangle.portrait.fill`。
  - 点击走 `NavigationRouter.shared.path.append(HomeRoute.autoSetup)`，与首页/账单页的空态引导共用同一路由，确保「完成」按钮 `removeLast()` 能正确 pop 回「记账工具」页。
  - 编译验证：`xcodebuild -project AIA/AIA.xcodeproj -scheme AIA -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` → **BUILD SUCCEEDED**。
：
  - 根因：`BillImportView` 内部包了一层 `NavigationStack`，而它被外层 `BillToolsView` → `BillListView` → `ContentView` 的 `NavigationStack` push 出来，形成 **NavigationStack 嵌套**，导致点击后页面不跳转（符合已知铁律：被外层 push 的 view 不可再包 `NavigationStack`）。
  - 修改 `AIA/AIA/BillImportView.swift`：移除内部 `NavigationStack`，保留 `ScrollView` + `.navigationTitle` + `.toolbar` + `.fileImporter` + `.alert`，让其直接作为外层导航栈的内容页面。
  - 编译验证：`xcodebuild -project AIA/AIA.xcodeproj -scheme AIA -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` → **BUILD SUCCEEDED**。重新安装后点击「账单导入」应正常 push 到账单导入页。

  - 修改 `原生代码脚手架/cloudfunctions/recognize/index.js`：
    - 新增 `qwenText` provider（`qwen-plus`），用于纯文本/OCR 文字识别，比 `qwen-vl-plus` 便宜一档。
    - 入口自动选择 provider：无 `imageBase64` 且非 chat 模式时默认走 `qwenText`；App 可显式指定 `provider` 覆盖。
    - `FN_VERSION` 升级到 `20260721a`。
  - 重新打包：`bash cloudfunctions/package-recognize.sh` → 生成 `recognize/recognize-20260721a.zip`，**需部署到 CloudBase** 后链路才生效。
  - 修改 `AIA/AIA/RecognizeService.swift`：
    - `RecognitionSource` 新增 `cloudText`（文本模型命中）。
    - 新增 `recognizeOCRText(_:)` 方法，把 OCR 文字发云端 `qwenText` provider。
    - `parseText` 显式指定 `provider: "qwenText"`，聊天文本输入也走文本模型省钱。
    - `recognizeWithLocalPriority` 改为四层回退：本地 OCR → 本地规则（0 成本）→ 文本模型（OCR 文字，便宜）→ 视觉模型（兜底）。
  - 编译验证：`xcodebuild -project AIA/AIA.xcodeproj -scheme AIA -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` → **BUILD SUCCEEDED**。

  - 根因：`ChatView.createTodoLocally` 的 `hasVerb` 只包含「增加/添加/创建/新建/设置/记/帮我」，未把「提醒」本身当作创建动词；同时 `hasExplicitCreateIntent` 也未包含「提醒」。于是该文本既没命中本地待办创建，也没命中云端记录兜底，最终走了闲聊分支。
  - 修改 `AIA/AIA/ChatView.swift`：
    - `createTodoLocally`：新增负面意图过滤（「取消/删除/关掉/关闭/停止/不要」）避免误创建；`hasVerb` 增加「提醒」，使「X 分钟后提醒我…」能命中本地创建。
    - `parseTodoCreate`：在移除相对时间后、移除「提醒/待办/任务/事项」后缀前，先移除「提醒我」，避免标题残留「我」（如「我打开阿宝AI管家app」）。
    - `hasExplicitCreateIntent`：增加「提醒我」「提醒」，让类似表达在本地解析失败时仍可走云端兜底。
  - 编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**。用户重新安装后，「2分钟后提醒我打开阿宝AI管家app」应创建标题为「打开阿宝AI管家app」、提醒时间为 2 分钟后的待办。

- **11:05 修复首页四宫格顶部类型色细色条不显示**：用户截图显示宫格顶部看不到类型色细色条。根因：色条原先通过 `.overlay(alignment: .top)` 加在 `.background(bg)` 之后，可能被 SwiftUI 的层级/渲染顺序影响，实际未显示。修改 `AIA/AIA/ContentView.swift` 的 `tile` 构造器：将色条从 `.overlay` 移到外层 `VStack` 的第一个子视图（在 padding 之外、紧贴卡片顶部），内部内容再包一层 `VStack` 并应用 padding；外层总高度仍限制 165pt，内部内容 minHeight 从 170 调为 135 以给色条留空间。编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**。用户重新安装后应能看到四宫格顶部清晰的类型色条。

- **11:00 首页四宫格顶部色条改为圆角贴合**：`ContentView.swift` 的 `tile` 顶部类型色细色条原为直角 `Rectangle().frame(height:3)`（靠外层 clipShape 裁圆）。改为 `UnevenRoundedRectangle`（topLeading/topTrailing=rLG，底部 0）+ `frame(height:4)`，顶部两角与卡片圆角贴合、更清晰。色仍用 `accent`（food/health/bill/todo 语义色）。BUILD SUCCEEDED。

- **10:44 优化首页四宫格内部排版**：用户截图 iPhone 17 Pro，四宫格高度合适但内部布局显得拥挤、丑陋（空态「点击查看」大胶囊遮挡内容，饮食/健康 pill 网格重复且占空间，账单行过多）。
  - 修改 `AIA/AIA/ContentView.swift`：
    - `tile` 构造器：内部 VStack spacing 从 8 降到 6；padding 从 14 降到 12；空态遮罩从中央大胶囊改为右下角小「点击」提示，不再遮挡内容。
    - 饮食 tile：移除「早中晚加」两排 pill 网格，改为大数字 + MiniBar + 3 行精简摘要（目标 / 待摄入或已超 / 净热量）。
    - 健康 tile：移除「睡眠/体重/BMI」两排 pill 网格，保留 sparkBars + 3 行关键指标（运动 / 静息能量 / 静息心率）。
    - 账单 tile：4 行列表改为 2×2 指标卡片（本月收入 / 本月支出 / 本月预算 / 本月结余），更紧凑。
    - 删除不再使用的 `mealPill`、`healthPill` 辅助函数。
  - 编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**。
- **10:53 优化宫格标题与顶部色条间距**：用户截图反馈各宫格标题（图标+文字+角标）离顶部彩色细条太近。修改 `AIA/AIA/ContentView.swift` 的 `tile` 构造器：将卡片内容区 padding 从统一 `padding(12)` 改为 `.padding(.horizontal, 12).padding(.top, 16).padding(.bottom, 10)`，标题区上方多留 4pt 呼吸空间。
  - 编译验证：**BUILD SUCCEEDED**。

- **10:30 重新设计账单导入页样式**：用户发送两张截图，要求把记账工具页里的「账单导入」入口 redesign 成右侧参考图风格。
  - 修改 `AIA/AIA/BillImportView.swift`：
    - 顶部标题栏右侧新增「导入记录」占位入口（TODO）。
    - 新增「请选择账单来源」卡片，内含三行：支付宝（蓝图标 #1677FF）、微信（绿图标 #07C160）、其他来源（蓝图标 + chevron）。目前三行点击都调用文件选择器，后续可扩展按来源解析。
    - 替换原来的「模板」区块为说明文案：「支持导入银行、钱包和财务类 App 的账单。如果 AIA 无法识别你的文件，请下载并填写标准模板。」，其中「标准模板」蓝色高亮可点击，点击后复制模板到剪贴板。
    - 新增底部两个大按钮：「上传账单文件」（主按钮蓝底白字）和「查看交易记录」（蓝字浅色底）。
    - 保留原 `previewSection` 预览/全选/导入逻辑，选择文件或粘贴后仍显示预览列表。
  - 顺手修复 `importButton` 中未使用的 `duplicates` 变量警告。
  - 编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**。
  - 后续 TODO：①「导入记录」入口需接入实际导入记录页；②「查看交易记录」需接入交易记录列表；③支付宝/微信来源可后续按来源格式做特化解析。

- **10:29 首页四宫格改为固定高度并裁剪溢出内容**：用户截图 iPhone 17 Pro，四宫格过高，希望调低为固定高度，超出内容隐藏。
  - 修改 `AIA/AIA/ContentView.swift` 的 `tile(...)` 构造器：将卡片从自适应高度改为固定总高度 165pt，在 `.clipShape` / `.contentShape` 后追加 `.frame(maxWidth: .infinity, minHeight: 165, maxHeight: 165)` 与 `.clipped()`，使每个宫格卡片等高，内部详情超出部分被裁剪。
  - 编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**（第一次误用 `height:` 参数报错，已修正为 `minHeight:maxHeight:`）。
  - 真机验证注意：饮食/健康卡片内容较多，165pt 可能会裁剪掉最下方的热量/指标行；如不够，可再把高度调整到 180pt 或减少卡片内行数。

- **10:16 实现「本地 OCR + MerchantMeta 规则优先」识别层**：用户要求先在 RecognizeService 里把本地优先识别这一层实现出来，把 token 成本压到最低。
  - 修改 `AIA/AIA/RecognizeService.swift`：
    - 新增 `import Vision`、`import SwiftData`。
    - 新增 `enum RecognitionSource { local/cloud }` 标记识别来源。
    - 新增 `localOCRText(from:)`：用 `VNRecognizeTextRequest`（.accurate + 中英文）设备端提取截图文字（0 成本）。
    - 新增 `localParseBill(text:in:)`：从 OCR 文字用正则提取金额（¥数字 或 数字元，取最大）、商户（MerchantMeta 命中 > 标签提取 > 首行文本）、日期（ISO8601），并查 `MerchantMetaStore.lookup` 补全分类；无经验库时按关键词猜分类/收支。能解析出明确金额才返回本地 `RecognitionResult`，否则返回 nil（交云端）。
    - 新增 `recognizeWithLocalPriority(image:in:)` 与 `recognizeWithLocalPriority(imageData:in:)`：先跑 OCR+规则，命中返回 `.local`，否则回退 `recognizeResilient`(`.cloud`)。
  - 修改 `AIA/AIA/UIComponents.swift` 的 `runImageRecognition`：相机/相册入口改用 `recognizeWithLocalPriority(image:in:)`，rawText 用本地 OCR 文字。
  - 修改 `AIA/AIA/ScreenshotIntent.swift`：无感截屏后台识别改用 `recognizeWithLocalPriority`；有 `AppDelegate.sharedContainer` 时构造 ModelContext 查 MerchantMeta，无则回退原云端；加 `import SwiftData`。
  - 沙箱编译验证通过 **BUILD SUCCEEDED**。注意：本地层仅覆盖「含明确金额的账单截图」场景；食物照片/待办/复杂小票仍走云端视觉模型。后续可加「本地 OCR 后只传文字给文本模型」进一步省钱。


  - 根因分析：`AutoSyncSettingsView.swift` 中 `@State private var userId: String = CloudSyncManager.userId` 只在页面初始化时读取一次，存在缓存旧值风险；且页面没有直接退出登录入口，用户切换账号不便。
  - 修改 `AIA/AIA/AutoSyncSettingsView.swift`：
    - 移除 `@State userId`，改为计算属性 `syncUserId`：登录态优先取 `auth.userId`，未登录回退 `CloudSyncManager.userId`，保证每次刷新都读最新值。
    - 复制按钮也改为复制 `syncUserId`。
    - 在「账号绑定说明」卡片内增加「退出登录」按钮，点击后弹出 `confirmationDialog` 确认，确认后调用 `auth.logout()`。
  - 沙箱编译验证通过 **BUILD SUCCEEDED**。用户真机验证：重新用 18977149919 登录后，同步账号应显示为 `phone_18977149919`；如仍显示旧号码，说明当前登录的就是旧账号，可在此页直接退出后重新登录。

- **10:09 合并账单管理入口为「周期记账/账单导入/自动记账」聚合页**：用户截图要求把「周期/订阅账单」和「账单导入」两个按钮聚合成一个入口，并把两个按钮移到新的页面里。
  - 新增 `AIA/AIA/BillToolsView.swift`：创建「记账工具」聚合页，内部分「周期记账」和「数据导入」两个区域，分别 NavigationLink 到 `RecurringRuleListView` 和 `BillImportView`，并传递 `modelContext`。
  - 修改 `AIA/AIA/RecordsViews.swift` 的 `BillListView`：移除原来的两个独立 `NavigationLink`，替换为一个「周期记账 / 账单导入 / 自动记账」入口，图标使用 `wand.and.stars`，副文案「管理周期账单、批量导入历史账单」。
  - 沙箱编译验证：`xcodebuild build -scheme AIA -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**。



- **09:27 首页待办模块去掉"最近待办事项"文案**：用户截图要求去掉首页待办卡片里的"最近待办事项"标题。修改 `AIA/AIA/ContentView.swift` 的 `todoTile`：删除 `Text("最近待办事项")`，保留空态"暂无待办 👍"和 `recentTodos` 列表。未编译，需 Xcode Build + 真机验证。
- **09:31 修复周期账单列表左滑删除无反应 + 编辑页增加删除**：用户反馈周期账单列表页左滑删除没反应，怀疑点击编辑太敏感与左滑冲突，并要求编辑页也加删除。
  - 根因：`RecurringRuleListView` 用 `ScrollView + VStack + ForEach` + `.swipeActions`，而 `.swipeActions` 在 ScrollView 中不可靠（项目内已有 `SwipeToDeleteCard` 替代方案）。同时列表项上加了 `.onTapGesture` 打开编辑，确实容易和滑动冲突。
  - 修改 `AIA/AIA/RecurringRuleViews.swift` 的 `RecurringRuleListView`：
    - 去掉 `.onTapGesture` 编辑方式，改为 `.onLongPressGesture(minimumDuration: 0.5)` 打开编辑 sheet（编辑动作更"迟缓"，不再与滑动冲突）。
    - 用 `SwipeToDeleteCard` 包裹 `ruleCardContent`，替代 `.swipeActions`，调用内部 `deleteRule(rule)`。`ruleCard` 拆分为 `ruleCard`（包装 SwipeToDeleteCard）和 `ruleCardContent`（卡片内容）。
  - 修改 `RecurringRuleEditView`：
    - 新增 `@State private var showDeleteConfirm` 和 `deleteRuleAndDismiss(_:)`。
    - 编辑模式下在表单底部（备注下方）显示红色「删除这条周期账单」按钮，点击弹 `.confirmationDialog` 确认；确认后删除规则并 dismiss。
  - 仍未能在此沙箱完整编译（缺 SwiftUI/SwiftData 框架），需用户 Xcode Build + 真机验证：列表页长按可编辑、左滑可删除、编辑页底部可删除。

- **09:27 修复食物输入被重复记成账单的问题**：用户截图显示：第一句"晚上吃老友粉花了¥13"被记为支出后，第二句"早餐吃了两个鸡蛋，一碗燕麦粥"又被云模型额外带回一笔"支出 老友牛肉粉 ¥13"。根因是 `isFoodLike` 分支走 `parseText` + `saveFromResult` 时未做类型过滤，云端返回的 result 里同时包含 `bill`，导致把旧账单/新幻觉账单又存一次。
  - 修改 `AIA/AIA/ChatView.swift`：给 `saveFromResult` 增加 `allowedTypes: [String]?` 参数（默认 nil 表示不限制）；内部用 `shouldSaveType` 分别控制 food / bill / todo 三种类型的保存。
  - 在 `processNext` 的 `isFoodLike(t)` 分支里，调用 `saveFromResult(result, originalText: t, allowedTypes: ["food"])`，强制只保存食物记录，丢弃云端返回的账单。
  - 其它两个调用点（兜底创建意图分支）保持 `allowedTypes: nil`，继续允许多类型混合保存。
  - 仍未能在此沙箱完整编译（缺 SwiftUI/SwiftData 框架），需用户 Xcode Build + 真机验证：纯食物语句不再产生重复账单、原有账单/待办/创建意图兜底不受影响。

- **10:04 修复首页底部 AI 栏被 Home Indicator 遮挡**：用户截图 iPhone 17 Pro 显示底部「点相册可上传、识别哦」输入栏与 Home Indicator 区域重叠，小屏机器更明显。
  - 根因：`AIA/AIA/UIComponents.swift` 的 `AIBottomBar` 用了 `.offset(y: 20)`，把底部栏向下硬推 20pt，导致压到全面屏底部安全区（Home Indicator）。
  - 修改：去掉 `.offset(y: 20)`；把 `.padding(.vertical, 10)` 改为 `.padding(.horizontal, 12).padding(.top, 10).padding(.bottom, 14)`，让底部栏依靠 SwiftUI 安全区自然停留在 Home Indicator 上方，同时保留底部视觉间距。
  - 沙箱编译验证：已发起 `xcodebuild build -scheme AIA ...`，结果待回。用户真机验证：首页底部相机/输入/麦克风/相册一排按钮不再被底部横条遮挡。

- **00:1x 开发「一图多账单」同时识别、分别记录（RecognizeService 拆分 + 逐条解析）**：用户发 4 张截图（支付宝支付消息列表 / 支付宝交易记录列表 / 支付宝账单列表 / 微信支付详情多记录页），要求一次识别图中所有账单、分别记录。
  - 链路：`recognizeWithLocalPriority` OCR 后先 `localParseMultiBillsIfNeeded`（detect 需 ≥2 金额行且 ≥2 不同商户）→ `extractBillEntries` 以金额行为锚点向上找「最近商户 + 时间行」构造最小 block → 逐 block 调 `localParseBill(forceAmount:true, preferredMerchant:拆出的商户)` 合并 `bills` 数组。
  - 关键修复（寇豆码主理人实现）：① `isLikelyTime` 扩展到识别「今天/昨天+时刻」「星期X+时刻」「M月D日+时刻」，避免相对时间串被当商户；② `extractRelativeDateTime` 加 `referenceDate` 参数 + 新增「星期X」(取不晚于参考日最近的那一周X) 与「纯 HH:MM」(用参考日补日期) 两优先级；③ `isPotentialMerchant` 允许中文单字商户（如收款人"娟"）；④ block 构造跳过下一行若它像商户标题（避免邻居账单文本污染当前条目的分类，修复上海票优被"米线"、老汤和被"腾讯云"带偏）；⑤ `localParseBill` 新增 `preferredMerchant/referenceDate/forceAmount` 透传，`localParseMultiBillsIfNeeded` 直接采用拆分时已定位的商户，避免对多行 block 重跑 extractMerchant 把整段当商户名。
  - 确认页/入库：`ResultConfirmView` 已支持 `billList` 逐条卡片（可删单条）、`RecognitionSaver.autoSave` 已逐条关联同一 `imageName`——多账单原生就支持，本轮无改动。
  - QA：新增 `QA_Regression_Tests/multi_bill_regression.swift`（H/I/J/K 四张真实 OCR 文本，64 项断言全 PASS）；回归 `regression_harness`(21 PASS)/`qa_real_ocr_e2e`(32 PASS) 均无回归。`xcodebuild` BUILD SUCCEEDED。
  - **待用户操作**：重装新 App 复测；识别上述 4 张截图应分别列出多条账单、确认页可逐条改/删、保存后各独立入库。注意：K 图(微信)状态栏"23:36"已不再污染（block 不含状态栏行，时间取自"星期四 18:26"→最近周四 2026-07-16）。


