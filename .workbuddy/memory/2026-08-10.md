# 2026-08-10 工作日志

- 用户已购 Apple Developer Program 个人付费账号（$99/年），上架首道门槛打通。更新 MEMORY.md：账号约束由「无付费账号」改为「已购」，并记录 Bundle ID 前缀 `com.daxing.aia`、真机/Archive 需开签名。
- 工程现状 Bundle ID：主 App `com.daxing.aia.AIA`；AIAKit `com.daxing.aia.AIAKit`；Widget `com.daxing.aia.AIA.AIAWidget`；ShareExtension `com.daxing.aia.AIA.ShareExtension`；LiveActivity `com.daxing.aia.AIA.AIALiveActivityWidget`。App Store Connect 建 App 须用同一 Bundle ID。
- 产出「上架准备包.html」（交互式勾选清单：截图规范/隐私标签填写建议/审核自测雷区/发布操作顺序，勾选态存 localStorage）+「privacy-policy.html」（可直接托管的隐私政策静态页，含数据类型表与联系方式占位）。下一步待用户在 Xcode 完成签名跑通后进入 Archive→上传阶段。
- 上架备案策略结论（用户问"需不需要备案"）：识记AI 属工具类（非游戏/新闻/宗教/出版），且为个人开发者账号主体，苹果对这类**非强制 ICP 类目**通常不在提审时硬性拦 ICP 号（强制类目/中国公司主体才会红灯"缺少 ICP 备案编号"）。建议**先全球上架提审，遇苹果真要求 ICP 号再决定：关中国区 or 办备案**。隐患：App 含广告(AdBanner)+云同步(需账号)，若被认定经营性可能需 ICP 备案/许可证（须公司主体），但个人含广告 App 上架 App Store 普遍可行，不必先办。隐私政策页托管用 CloudBase 默认二级域名或 GitHub Pages（境外）可免网站备案。
- 改名决策：用户曾要求把 App 改名「全能记」，经排查当前实际已为「好记AI」(显示名`好记`)，且 App Store 已有同名在架「全能记」(id 1587301632) + 多款「全能记事本」占用，改名上架必被驳回。**用户最终决定保留好记AI，不做改名**（约30处代码/配置/图标/文件名改动均保持现状）。MEMORY.md 品牌命名段已同步修正为现名好记AI。
- 用户协议决策：因做订阅（¥88/年、¥8.8/月）→ 苹果强制要求自定义 EULA。已新建 `好记用户协议.html`（订阅/注销/数据删除/退款条款，文件名须严格匹配 AppURLs.defaultAgreement）；`AppURLs`/`SettingsView`/`LoginView` 的 userAgreement 入口早已预留，仅缺页文件。
- 应用内账户删除（苹果 Guideline 5.1.1(v) 强制，有账号体系必做）已全量实现：① 云函数 `云函数/aia-sync/index.js` 新增 `deleteAccount` action（删该 userId 分区 aia_records/aia_events/aia_devices/aia_user_links/aia_quota_usage）；② 客户端 `CloudSyncManager.deleteAccount(context:)`（云端删→本地后台 ModelContext 全量清空→ManualHealthStore.clearAll→AuthManager.logout 回登录页）；③ `MyAccountView` 新增删除账户 UI（二次确认+输入"删除"校验+订阅先取消提示）；④ `ManualHealthStore` 加 `clearAll()`。另产出 `上架合规_Connect待填清单.md`（EULA URL/描述补订阅/隐私营养标签/含广告开关）。
- 构建验证环境限制：命令行 `xcodebuild ... CODE_SIGNING_ALLOWED=NO` 失败，全部错误仅来自 `AIAKit/AIAMigrationPlan.swift` 的 SwiftData `@Model` 宏——`swift-plugin-server could not be loaded: Operation not permitted`（沙箱/CLI 环境禁止加载 Swift 编译器插件），**非本次代码错误，真实 Xcode GUI 不受影响**。改动文件（MyAccountView/CloudSyncManager/ManualHealthStore）未被报告任何错误。需在真实 Xcode ⌘B 做最终编译验证。
