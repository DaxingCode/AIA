# 2026-07-26 工作日志

## 00:07 · 新人引导页（OnboardingView）重排序
- 需求：用户给出目标顺序 12 页（去掉原「跟阿宝说一句话」voicePage），要求按该顺序调整。
- 改动：`AIA/AIA/OnboardingView.swift`
  - `private let total = 13` → `12`（用户已先行改好，本次确认）。
  - `TabView` 内 `.tag()` 重排为：welcome(0)/screenshot(1)/payScreenshot(2)/notifyScreenshot(3 待办提醒)/payFood(4 饮食识别)/voiceScenario(5)/siri(6)/quickActions(7)/health(8 健康数据)/ask(9 问 AI)/shortcuts(10 快捷指令配置核心)/done(11)。
  - 原 `voicePage`（「跟阿宝说一句话」）属性已从文件中移除（用户已先行删），无残留引用。
  - 底部圆点 / 跳过 / 主按钮均用 `total` 驱动，无需额外改。
- 验证：xcodebuild BUILD SUCCEEDED。改动尚未 commit。

## 08:23 · 饮食记录页「常吃食物」快速记录区（Task U）
- 需求：在饮食记录 tab 的餐次区域增加「常吃食物」名字展示；点击名称自动将该食物保存到当前餐次；左右滑动食物名称区域切换食物。
- 改动：`AIA/AIA/RecordsViews.swift`（`FoodListView`）
  - 新增 `frequentFoods` 计算属性：从 `foods` 历史按名称出现频次聚合 top10；无历史则回退 `defaultFrequentFoods`（米饭/鸡蛋/牛奶/苹果/鸡胸肉/面包/面条/牛肉/西兰花/香蕉，均已确认存在于 `NutritionLibrary`）。
  - 新增 `saveFrequentFood(_:)`：用 `NutritionLibrary.shared.match(name)` 取每100g 营养 ×100g 入库，`meal: meal.mealString`、`date: selectedDate`、`weightGram: 100`、base* 透传每100g 基准；`context.insert` + 轻触觉反馈（SwiftData autosave 自动持久化，不手动 save）。
  - 新增 `frequentFoodRegion`（横向分页 `TabView(.page)` 展示食物名 +「左右滑动切换」提示 + 点击卡片保存）与 `frequentFoodCard(_:)` helper（卡片含 + 图标 + 名称 + 「kcal/100g」）。
  - 插入位置：餐次分段 `SegmentedPicker` + 本餐汇总卡下方，仍在 `if !foods.isEmpty` 内。
- 验证：xcodebuild BUILD SUCCEEDED。commit `51c32a3`（仅 RecordsViews.swift + 分工表.md），§4 已释放。

## 08:35 · 「常吃食物」由分页卡片改为 chip 横滑
- 用户确认要 chip 横滑样式（对比图见上一轮对话）。
- 改动：`frequentFoodRegion` 由 `TabView(.page)` 改为 `ScrollView(.horizontal, showsIndicators:false)` + `HStack` 一排胶囊标签；`frequentFoodCard(_:)` 替换为 `frequentFoodChip(_:highlighted:)`（Capsule 形状，首个高频食物 `highlighted` 高亮，其余常规）；标题提示「左右滑动浏览」；保存逻辑 `saveFrequentFood` 不变。
- 验证：xcodebuild BUILD SUCCEEDED。commit `d75cad9`（+24/-36），§4 已释放。

## 08:39 · 饮食记录页新记录置顶
- 需求：新记录的食物显示在列表上方。
- 根因：`mealItems` 原按 `date` 倒序；chip 快速记录 `saveFrequentFood` 用 `date: selectedDate`（所看日期，打开页面即固定、会变旧），导致新加的食物 `date` 不晚于同餐次已有项，排不到顶。
- 改动：`mealItems` 排序改为按 `syncUpdatedAt` 倒序（记录时间最新者置顶），对今天/历史日均稳定生效，且与聊天/拍照入库（date≈记录时）行为一致。仅改 1 行。
- 验证：BUILD SUCCEEDED，commit `2e0e673`（+3/-2），§4 已释放。

## 08:47 · 「常吃食物」标题追加 100g 提示
- 需求：「点一下快速记录」后加「100g」默认份量提示。
- 改动：`frequentFoodRegion` 标题文案 `Text("常吃食物 · 点一下快速记录 100g")`（原来无 100g）。
- 验证：BUILD SUCCEEDED，commit `6b806ff`（+3/-2），§4 已释放。

## 08:50 · 深色模式「常吃食物」区域可见性优化
- 问题：chip 原 `food.opacity(0.08)` 底 + `ink` 字，深色下近乎隐形（`ink` dark=0x2c2c2e 近黑；`food` dark=0xf2b04a 亮琥珀可用）。
- 改动（`frequentFoodRegion`/`frequentFoodChip`，加 `@Environment(\.colorScheme)`）：按 colorScheme 分支——
  - 深色：chip 背景 `AIATheme.dietBG`（饮食琥珀淡底，dark=0x2a2416 暖棕，明显区别于页面 0x1c1c1e），文字 `AIATheme.food`（亮琥珀），描边 `food` 0.35/高亮 0.7 且高亮线宽 1；
  - 浅色：保持原 `food.opacity(0.08~0.16)` 底 + `ink` 字 + `food` 0.22/0.45 描边。
  - 标题 `常吃食物 · 点一下快速记录 100g` 字色由 `AIATheme.ink` 改 `Color.primary`（深色保证可读）。
- 验证：BUILD SUCCEEDED，commit `9fc85ff`（+15/-9），§4 已释放。

## 09:02 · 写入长期记忆：深色模式强制适配铁律
- 用户要求：新建/修改 UI 必须适配深色模式，以后创建都按此执行。
- 已写入 `MEMORY.md` 的「UI/视觉约定」首条（最高优先级）：同时验证浅/深双配色；颜色走 AIATheme 自适应令牌；禁止 `ink` 当前景色（深色近黑隐形）；有色淡底用 dietBG 等自适应淡底而非低透明度叠深色；改完切深色模式肉眼核对。

## 09:28 · 「常吃食物」改为展示所有记过的菜（去重+按次数排序）
- 需求：横滑能看到所有记过的菜，不要限 Top 10。
- 改动：`frequentFoods` 去掉 `prefix(10)` 截断，对全部 `FoodEntry` 按菜名去重后按出现次数倒序排列；无历史仍回退 `defaultFrequentFoods`（10 项）。
- 验证：BUILD SUCCEEDED，commit `6b15840`（+6/-5），§4 已释放。

## 09:31 · 「常吃食物」混合排序（Top10 按次数，其后按最近进食时间）
- 需求：常吃前十按次数排序，从第 11 个起按最近时间排序。
- 改动：`frequentFoods` 同时聚合每菜计数与最近 `date`；前 10 按计数倒序，第 11 个起按 `date` 倒序拼接；无历史仍回退 `defaultFrequentFoods`。
- 验证：BUILD SUCCEEDED，commit `1445e1b`（+17/-4），§4 已释放。

## 09:37 · chip 背景色改为标题图标同色（food 琥珀）
- 需求：chip 横滑胶囊背景改成标题开头 `fork.knife` 图标同色。
- 改动：`frequentFoodChip` 去掉 colorScheme 分支，背景统一 `AIATheme.food`（琥珀，与图标同色）；文字 `AIATheme.ink`（琥珀非深底，深字可读，不触发 ink 隐形规则）；描边 `ink` 分级（高亮 0.55/1.5，常规 0.22/0.5）；删除 `@Environment(\.colorScheme)`。
- 验证：BUILD SUCCEEDED，commit `341dc0b`（+9/-13），§4 已释放。

## 09:43 · 回退 chip 配色到上一版
- 需求：用户觉得实心 food 琥珀底太重，要求「还是改成原来的样子」。
- 改动：`frequentFoodChip` 回退到 commit `341dc0b` 前——深色 `dietBG` 暖底 + `food` 字/描边（高亮更粗），浅色 `food` 淡底 + `ink` 字；恢复 `@Environment(\.colorScheme)`。实心 food 配色方案被否决。
- 验证：BUILD SUCCEEDED，commit `3745175`（+15/-9），§4 已释放。

## 09:28 · 待办提醒页点击圆圈增加震动效果
- 需求：todoDoneButton 点完圆圈没有触觉反馈，用户感知不到「点中了」。
- 修法：`toggleDone(_:)` line 2471-2476 在 `r.done.toggle()` 前加双向语义震动：
  - 未完成 → 完成：`UINotificationFeedbackGenerator().notificationOccurred(.success)`（强烈三段式，标志任务完成）
  - 完成 → 未完成：`UIImpactFeedbackGenerator(style: .light).impactOccurred()`（轻触，反向操作不打扰）
- 对齐现有项目震动规范：ChatView:729/735、MerchantRuleViews:23/33、MonthlyBillListView:116/122 都是 success + light 配对。
- 验证：BUILD SUCCEEDED，commit `5cd4065`（+9，1 文件），§4 已释放。

## 09:39 · 待办「已完成」区域按日期倒序展示
- 需求：用户截图「已完成 16」tab 只展示了 7月21日 一个日期组（12 项），不符合「最近完成的在前」的预期。
- 根因：`finished` 计算属性只 filter 不排序，靠下游 `groupedByDate` 排，但 `groupedByDate` 写死 `d1 < d2` 升序（适合「未完成：最近 due 在前」）。已完成视图沿用了同一排序导致结果反了。
- 修法：`groupedByDate` line 2037-2044 排序方向按当前 filter 区分——
  - `filter == .finished`：日期组倒序、同组内 due 也倒序
  - `filter == .active`：维持当前升序
- 不动 Reminder schema、不加 `doneAt` 字段（最小改动）：现在用 due 作完成时间的合理近似——用户标记完成的瞬间附近一般就是 due 设置的时间。
- 验证：BUILD SUCCEEDED，commit `a93cca1`（+10/-2，1 文件），§4 已释放。

## 10:30 · 云端记水统一写入 FoodEntry.waterIntake（与本地优先一致）
- 需求：用户（小白）确认把云端 `create_water` 从独立的 `water` 类型（对应 App 端 WaterLog）改为与本地 `ChatView.createWaterIntake` 一致——写入饮食记录 `food` 类型的 `waterIntake` 字段，统一存到同一本 FoodEntry。
- 根因/发现：云端 create_water 原写 `type:'water'`（WaterLog），而本地清晰话语记水走 FoodEntry.waterIntake，两套存储割裂。进一步检查发现 App 端 `CloudSyncManager.applyFood` **未读取** `waterIntake`、`pushFood` payload 也**漏了** `waterIntake`——若不补，云端改过去后拉取时水仍会丢（且本地聊天记的水跨设备同步也会丢）。
- 改动：
  - `云函数/recognize/agentTools.js`：`create_water` 改写 `type:'food'`，payload 含 `name:'饮用水'`、`waterIntake:amount`、营养全0、`meal`/`portion`/`date`/`note`；`delete_water` 兼容删 `food`(waterIntake>0) 与历史 `water`（按 id 双删+无 id 合并两类取最新）；`list_waters` 合并统计 `water`(老)+ `food` 中 waterIntake>0。
  - `AIA/AIA/CloudSyncManager.swift`：`applyFood` 两处（existing 更新 + 新建）补读 `waterIntake`；`pushFood` payload 补 `waterIntake`，闭合跨设备往返。
  - `云函数/recognize/index.js`：`FN_VERSION` → `20260726g-water2food`（逻辑变了，需重部署）。
- 展示层无需改：饮水总量 `waterIntakeToday` = `foods.waterIntake + WaterLog` 已自动包含；饮食列表本就展示 FoodEntry（含本地聊天记的"饮用水"），`topFoods` 已过滤 `name!="饮用水"`。
- 验证：`node --check agentTools.js` OK；CloudSyncManager.swift lint 0 错误。**待办：需重部署 recognizer 云函数（中文路径下 package-recognize.sh 可能失败，建议手动 `zip -r recognize-20260726g-water2food.zip index.js package.json agent*.js` 后控制台上传）。** 改动尚未 commit。
