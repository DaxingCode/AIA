# 2026-08-02

## 品牌命名与 logo
- 用户最终弃用「阿宝AI管家」（撞《功夫熊猫》阿宝角色名在先权益），定名 **识记AI**。
- 生成 5 个 SVG 矢量 logo 方案 → `识记AI_Logo方案/`：①熊猫扫描 ②对话框光圈 ③印章识 ④镜头记录点 ⑤四格合一。选矢量因 AI 生图中文必乱码。
- 同步更新 MEMORY.md「品牌命名」节（长期事实）。
- 下一步：用户选 1 个 → 可微调配色/字形、导出 PNG、做商标检索+著作权登记。
- logo_1 微调：去掉熊猫中间扫描线，底部改为「识记」在上、「AI」单独在图片最底部（viewBox 加高到 600）。识记AI 全名保留。
- logo_1 再微调：熊猫黑眼圈由旋转椭圆(ellipse)改为正圆形(circle, r=38)，去掉旋转角度。
- logo_1 再微调：黑眼圈内白色眼珠填成与眼圈同色渐变(pd1)，眼圈变实心黑圆。
- 出对比版 `logo_1b_熊猫扫描_高光.svg`：实心黑圆眼圈 + 缩小白高光点(r=6,偏左上)，有神不抢整体感。与 logo_1(纯实心)并列供选。
- 出 `logo_1c_熊猫扫描_AI在框内.svg`：按用户截图红框，把「AI」移进取景框内熊猫脸下方，框外只保留「识记」。
- logo_1c 微调：AI 字号改为 60(与识记等大)，y 上移356→372(更贴取景框内底部)。
- 出 `logo_1d_熊猫扫描_框内纯图标.svg`：用户要框内纯图标——熊猫上移缩小+取景框加高(h=440)，「识记」(上)与「AI」(下)都进框内，框外留空。
- logo_1c 微调：AI 再下移到 y=398(更贴取景框内底部)。
- 出 `logo_1e_熊猫扫描_纯图标.svg`(去识记、画布贴合480x480) 并转 PNG：`@resvg/resvg-js`(托管node install于 binaries/node/workspace) 渲染 1024px → `logo_1e_熊猫扫描_纯图标.png`。
- **已把 App 图标换成该熊猫扫描图**：`logo_appicon.svg`(蓝底铺满不透明+熊猫+白AI) → resvg 渲染 1024px 写入 `AIA/AIA/Assets.xcassets/AppIcon.appiconset/AppIcon.png`，复用原 Contents.json(单图引用3次)。xcodebuild BUILD SUCCEEDED。
- 用户要求保持 logo_1e 浅蓝背景色：生成 `logo_appicon_light.svg`(整画布填同色浅蓝渐变、不透明)，重新渲染写入 AppIcon.png 替换深蓝渐变版。xcodebuild BUILD SUCCEEDED。

## 待办页面长按拖动改造（live reflow）
- 需求：① 长按太敏感（轻碰即触发）与轻点冲突；② 拖动时加类似首页模块的"行实时让位"效果。
- 决策（用户确认）：落点"只改到目标天"（不插值提醒时间）；视觉"浮起跟随 + 插入间隙"。
- 文件：`AIA/AIA/RecordsViews.swift`。
- 改动：新增 `TodoRowFrame`/`ContainerOriginKey`/`DropIndex`(Equatable) + `TodoRowFrameKey`；`todoRow` 手势调稳(`minimumDuration:0.5,maximumDistance:12` + `DragGesture(minimumDistance:8)`，且 `isDragging` 延迟到 `.second` 且手指已移动才置位)；行 frame 经 `TodoRowFrameKey` 上报；`shouldPushDown` 让位；`draggedRowOverlay` 浮起快照；`todoListView()` 抽离整段 ScrollView 规避 body type-check timeout；两处 `ForEach` 补传 group/index。
- 编译坑（已修）：元组不遵守 `Equatable` → `PreferenceKey` value 与 `.animation(value:)` 必须用显式 struct（报过 `type '(frame:..., group:...)' cannot conform to 'Equatable'` 与 body "unable to type-check"）。BUILD SUCCEEDED。
- 已同步写入 MEMORY.md SwiftUI 铁律（PreferenceKey 元组坑 / 拖拽手势调稳 / live reflow / 大 body 超时抽函数）。

## App 改名落地：识记AI + 对话页阿记
- 全工程批量替换：品牌名「阿宝AI管家/阿宝AI」→「识记AI」；助手昵称「阿宝」→「阿记」。范围：AIA/AIA 下 swift/plist/pbxproj、云函数 recognize/*.js、旧副本原生代码脚手架、现行配置指南 md、根目录 HTML 预览稿。历史分析文档(商标/候选名/分工表)保留「阿宝」作为旧名语境故意不改。
- pbxproj：`INFOPLIST_KEY_CFBundleDisplayName`→「识记AI」；权限描述里「阿宝」→「阿记」。
- 云函数 `abao_learning` 英文字段名保留（历史数据兼容），仅中文 prompt/文案改「阿记」。
- 中途 perl 批量改 pbxproj 触发一次 'damaged project' 瞬时报错（实为长 clean build 仍占用工程的竞态），pbxproj 结构实际完好（括号平衡、section 完整）；最终 incremental + clean 两次 build 均 BUILD SUCCEEDED。
- 教训：跨多文件批量改中文用 perl -i 有风险（UTF-8/工程文件），应优先用 Edit 工具或确认无误后再跑；pbxproj 改动务必复查。

## App 图标换版：熊猫 → 「记」字标（终版）
- 早晨换的熊猫扫描版（浅蓝底+取景框+阿宝脸+AI）识别度尚可，但熊猫与《功夫熊猫》形象关联强，长远 IP 风险仍模糊（角色名/视觉形象双重），决定彻底去熊猫。
- 设计方向（与用户确认）：① 概念=「记」字标（推荐，单字辨识度强、避 IP）；② 配色=科技蓝渐变（当前工程主色 378ADD→38BDF8）。
- 程序化渲染：`AppIcons/build_icon_final.py`（PIL+numpy+Hiragino Sans GB.ttc）。产出 4 个交付物：
  - `AppIcon_识记AI.png` 1024×1024 主图（含 AI 火花）
  - `AppIcon_识记AI_plain.png` 1024×1024 极简版（无火花）
  - `Logo_识记AI_horizontal.png` 1536×512 横向 lockup（图标+识记AI）
  - `Logo_识记AI.svg` 矢量源
- **已部署：含火花版**。备份旧熊猫到 `AppIcons/icon_backup_panda_2026-08-02.png`，写入 `AIA/AIA/Assets.xcassets/AppIcon.appiconset/AppIcon.png`。xcodebuild BUILD SUCCEEDED。
- 用户选择"稍后自己提"，本次**未提交 git**。下次提交建议：`git add AIA/AIA/Assets.xcassets/AppIcon.appiconset/AppIcon.png AppIcons/AppIcon_识记AI*.png AppIcons/Logo_识记AI* AppIcons/build_icon_final.py`。
- 同步写入 MEMORY.md「App 图标（2026-08-02 迭代二）」。

## App 图标迭代三：记 → 繁体「記」+ 方方正正字体
- 用户要求：图标和 logo 的「记」→ 繁体「記」，用方方正正字体，出几张效果图挑。
- 方字体选型：系统可用偏方正中文 = **STHeiti 华文黑体**（Light/Medium）。其 **index 0 = Heiti TC（繁体字面）**，正好匹配「記」繁体，黑体天生方方正正。Hiragino 圆润不够方。
- 出 3 张图标候选 + 1 张 logo 预览（`AppIcons/build_ji_variants.py`）：① stheiti_medium ② stheiti_light ③ hiragino_w6 基线 ④ Logo_記_stheiti_medium（識記AI）。
- 用户拍板：**图标用 A=华文黑体·中黑（STHeiti Medium，Heiti TC）**；**logo 保持简体「识记AI」不动**（故 logo 代码未改，仍为原 Hiragino 简体版）。
- 落地：`build_icon_final.py` 的「記」字标字体改为 `STHeiti Medium.ttc index=0`（Heiti TC），字符「记」→「記」；AI 仍 Hiragino W6；SVG 矢量源同步（`font-family` 改 Heiti TC、字改「記」）。logo 函数保持原 `load_font(232)` 简体。
- **已部署**：重渲染 `AppIcon_识记AI.png` → 拷贝到 `AIA/AIA/Assets.xcassets/AppIcon.appiconset/AppIcon.png`，并 open 预览。未提交 git。
- 待确认：① 图标字体是否就用 A（或改 B/C）；② ChatView.swift:513 编译错误（`self` is immutable，mutating 运算符左侧不可变）为预存独立问题，会阻断 App 构建/运行，用户尚未决定是否让我修。
