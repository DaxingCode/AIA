# 四大模块功能细化

基于「E宫首页 + D话对话 + 截图/拍照自动识别」的整体设计，细化饮食、健康、账单、待办四个模块的功能。每个模块都可由**首页宫格进入、底部 AI 栏随时喂入、对话页(D话)自然语言操控**。

---

## 1. 饮食记录（Diet）

**核心**：拍照/截图识别食物 → AI 结构化出菜品、分量、热量与三大营养素，接营养库校正精度。

**功能清单**
- 拍照识别：相机拍食物 → 识别菜名/分量/热量/蛋白/碳水/脂肪，营养库校正
- 截图识别：外卖/食谱截图自动提取并记入对应餐次
- 手动添加：搜索食物库 / 语音"午餐拉面25" / 输入热量
- 餐次归类：早 / 午 / 晚 / 加餐，自动按时间建议餐次
- 每日汇总：摄入热量、目标进度条、剩余额度
- 营养明细：蛋白质 / 碳水 / 脂肪 / 膳食纤维占比
- 趋势分析：日/周/月热量曲线，与目标的偏差
- 净热量联动：`净热量 = TDEE(来自健康模块) − 摄入`，首页直接展示
- 条目管理：改分量、改餐次、删除、查看识别原图
- 饮水记录（可选扩展）

**入口与交互**
- 首页宫格「饮食记录」→ 饮食页（见 `UI完整页面流.html` ③）
- 底部栏相机/相册 → 识别中 → 结果确认（存入饮食）
- D话："记一笔午餐拉面520" → 自动建条目

---

## 2. 健康管理（Health）

**核心**：打通 HealthKit，集中呈现运动/睡眠/身体指标，并驱动净热量计算的 TDEE。

**功能清单**
- 运动：步数、距离、活动能量、锻炼时长（HealthKit 拉取）
- 睡眠：睡眠时长；若数据源支持，展示深睡/浅睡/REM 分期
- 身体指标：体重、身高、BMI、体脂率、静息心率
- 指标趋势：体重曲线、静息心率变化、步数七日柱
- 目标设定：步数目标、体重目标（如 65kg）
- 手动录入：无手环时手动填体重/身高，保持连续
- 周报/简报：AI 生成的"本周健康小结"
- 异常提醒：久坐提醒、心率异常、目标未达成提示
- TDEE 计算：由身体指标+活动量估算每日消耗，回传给饮食模块

**入口与交互**
- 首页宫格「健康管理」→ 健康页（④），含双环(步数/睡眠)+指标卡+七日趋势
- 权限引导页开启 HealthKit（⑨）
- D话："我昨天睡了几小时" → 查睡眠

---

## 3. 账单管理（Bills）

**核心**：截图/小票自动识别入账，AI 提取金额/商户/分类，用户核对后保存。

**功能清单**
- 截图识别：支付宝/微信/银行交易截图自动解析入账
- 小票/发票拍照：识别金额、商户、时间
- AI 提取 → 确认流：展示结构化结果，用户改字段后「存入账单」
- 分类：餐饮 / 交通 / 购物 / 娱乐 / 居家 / 医疗
- 状态：待确认 / 已确认，待确认项首页红点提示
- 月度统计：当月总额、分类占比
- 预算与超支：设分类预算，超支本地通知
- 周期/订阅账单：房租、会员自动重复，续费提醒
- 搜索与筛选：按商户/分类/月份
- 数据导出：CSV / JSON

**入口与交互**
- 首页宫格「账单管理」→ 账单页（⑤），含本月总额+待确认/已确认列表
- 底部栏相机/相册 → 识别中 → 结果确认（存入账单）
- D话："这个月餐饮花了多少" → 出统计

---

## 4. 待办提醒（Todos）

**核心**：从截图/对话中自动抽取待办与截止时间，本地通知准时提醒。

**功能清单**
- 自动提取：截图（如聊天"周五交报表"）或对话 → AI 生成待办+到期时间
- 手动/语音添加："提醒我明天10点复诊"
- 时间提醒：基于截止时间的本地通知
- 优先级与分类：工作/生活/健康，高/中/低
- 重复待办：每天/每周/每月（如"每周健身"）
- 完成与延期：勾选完成、延后、稍后提醒
- 智能建议：AI 根据截图中的日期/地点主动建议建待办
- 日历联动（可选）：同步到系统日历

**入口与交互**
- 首页宫格「待办提醒」→ 待办页（⑥），含今天到期列表+最近三项
- 底部栏文本/语音 → D话 → "加个待办"
- 识别流中若判定为待办，结果确认页切换为待办模板

---

## 跨模块 AI 能力（统一底座）

- **统一入口**：底部 AI 栏（相机/文本/语音/相册）和 D话对话页，是所有模块的共同入口
- **自动归类**：截图/拍照进来后，AI 判断归属模块（账单/饮食/待办/健康）并路由到对应结果确认页
- **今日简报**：首页/对话页汇总四模块当日变化（如"已记午餐¥128、步数7420、待办3项"）
- **本地优先**：财务与健康敏感数据默认本地存储，上云仅传结构化字段

---

## 建议补充的页面（如需继续做高保真）

| 模块 | 可补页面 |
|---|---|
| 饮食 | 食物识别结果详情（营养明细卡）、营养趋势周报 |
| 健康 | 体重趋势折线图页、睡眠分期页、目标设定页 |
| 账单 | 分类统计页（环形占比）、预算设置页 |
| 待办 | 待办详情/编辑页、重复规则设置页 |
| 通用 | 食物拍照识别专项页（取景框+识别） |

选定某个模块，我可以把它扩成高保真子页面，或按技术栈出原生代码。
