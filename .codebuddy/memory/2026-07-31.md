# 2026-07-31

## 已完成的任务
- **三个柱状图区域改为可左右滑动翻周 + 滑动重播生长动画**（RecordsViews.swift）。
  - 饮食页 Card3 近7日热量、健康页 CardB 近7日步数、账单页 Card 近7日消费。
  - 三处数据函数 `weekData/weekSteps/weekSpend` 改为接受 `offset: Int`（0=本周，负数=过去第|n|周）。
  - 每图新增 `@State chartWeekOffset` + `TabView(selection:).tabViewStyle(.page(indexDisplayMode:.never))` + `ForEach((-8)...0)` 共 9 屏（过去 8 周）。
  - 卡片挂 `.onChange(of: chartWeekOffset)`：先 `withAnimation(.none){reveal=false}` 再 150ms 后 `true`，重播从 0 向上生长。
  - `SectionTitle.trailing` 显示 `chartWeekRangeLabel`（本周→"本周"，历史周→"M/d–M/d"）；底部 `ChartPageDots` 圆点指示当前屏。
  - 饮食页 `.onChange(of: selectedDate)` 内追加 `chartWeekOffset = 0`（选中日变化回到以新选中日为终点的本周）。
  - 新增 fileprivate 辅助 `ChartPageDots` / `chartWeekRangeLabel`（文件末尾）；4 个 .strings 加 `week.current` 文案。
  - 用户确认项：过去 8 周、周范围文字+小圆点、保持 150ms。xcodebuild BUILD SUCCEEDED，无 lint。
- 注意：健康页历史周步数依赖 HealthManager.stepsForDay 缓存（仅近 7 天），更早的历史周可能显示 0（数据限制，非崩溃）。
- **翻周 offset 符号 bug 修复**（RecordsViews.swift）：`weekData`/`weekSteps`/`weekSpend`/`chartWeekRangeLabel` 共 4 处原用 `value: -offset * 7`，负号导致负 offset（本意过去周）反而指向未来。改为 `value: offset * 7`，使右滑（退到 `offset=-1`）显示过去一周。xcodebuild BUILD SUCCEEDED，lint 0。
- **隐藏「本周」标签**：`chartWeekRangeLabel` 在 `offset==0` 时改返回 `""`（原返回 `week.current`="本周"）。本周时右上角不再显示文字；历史周仍显示「M/d–M/d」不变。xcodebuild BUILD SUCCEEDED。
- **Xcode 报 `invalid reuse after initialization failure` 诊断**：用户在 Xcode GUI 点运行模拟器时报此错，清理 DerivedData 后消失。根因=DerivedData 缓存损坏（Xcode 链接器 ld 读取损坏中间缓存无法复用），与 Swift 业务代码无关——证据是命令行 `xcodebuild` 始终 BUILD SUCCEEDED。该错误常见于 Xcode 15+ 新链接器/缓存问题，非 Flutter 专属。后续复发直接 `rm -rf ~/Library/Developer/Xcode/DerivedData/AIA-*` 重编即可，无需改代码。
- **账单周空态引导文案**（RecordsViews.swift 近7日消费卡片）：整周无消费时（原左对齐用 `bill.noRecords`="暂无记录"），改为居中展示 `bill.week.empty`="暂无记录，可使用语音、文字、截图、导入记账"。新增独立本地化键 `bill.week.empty`（zh-Hans/zh-Hant/zh-HK/en 四份），不复用 `bill.noRecords` 以免串到月度预算空态。居中新写法：`.multilineTextAlignment(.center)` + `.frame(maxWidth:.infinity,maxHeight:.infinity,alignment:.center)`。xcodebuild BUILD SUCCEEDED。
- **饮食周空态引导文案**（RecordsViews.swift 近7日热量卡片 Card3）：与账单对称。去掉原外层 `if !foods.isEmpty`（让完全没有记录的也显示引导），内部每屏 `weekData` 全 0 时居中展示 `food.week.empty`="暂无记录，可使用语音、文字、照片记录"。新增独立本地化键 `food.week.empty`（四语言）。xcodebuild BUILD SUCCEEDED。
- **跨用户「数据统计与导出」功能（全新）**。目标=分析所有用户的活跃度与功能热度，指导后续功能优先级。
  - **关键事实更正**：`recognition` / `chat` / `water` **早已在云同步范围内**（CloudSyncManager 的 type 列表含 bill/reminder/food/health/manualHealth/recognition/merchant_meta/chat/water/recurring_rule/setting/profile），此前记忆里「识别/聊天/饮水纯本地」的判断是错的。所以业务侧统计不需要埋点，云端 `aia_records` 直接聚合即可；埋点只用于补「无数据落地的行为」。
  - 云端（`云函数/aia-sync/index.js`）新增两个 action，**不新建云函数**（沿用广告/配置的 `/sync` + action 多路复用范式）：
    - `logEvent`（无口令，支持批量 events 数组，单次上限 100 条）→ 写入新集合 `aia_events`，文档 `{userId,event,ts(ms),day,platform,appVersion,meta}`。
    - `stats`（需 `passcode === DEV_PASSCODE`）→ 全量扫 `aia_records` + `aia_events`（`scanAll` 分页 limit 1000，上限 5 万），返回 `{ok,summary,csvs}`，9 张 CSV：用户总览/日活趋势/功能渗透率/行为事件统计/识别使用情况/账单分类分布/待办完成率/饮食与饮水/用户明细。CSV 云端生成、带 BOM。
    - 云端工具函数：`toMillis`（payload 时间字段是**秒**，>1e11 才当毫秒）、`dayKey`（统一 UTC+8 分日）、`csvEscape`/`toCSV`、`FEATURE_NAMES` 映射。`stats`/`logEvent` 的路由必须放在 userId 守卫**之前**。
  - App 端新增 `UsageAnalytics.swift`：批量缓冲埋点（内存+UserDefaults 持久化，满 10 条 / 距上次 60s / 进后台 才批量发，上限 300 条丢最老，失败保留重试），压低云调用成本；另含 `fetchStats(days:)`（走全局 `postAdsJSON` + `DeveloperGate.passcode`）和 `writeTempFiles`。事件名把维度拼进名字（`feature_open:diet`、`record_add:bill`）方便云端直接分组。
  - App 端新增 `DataStatsExportView.swift`：概览指标网格 + 趋势范围 30/90/365 切换 + 每张表可展开预览、单独分享、一键导出全部（`ShareSheet`+`SharePayload`）。入口=开发者中心新卡片 `dataStatsEntry` → `NavigationRouter.shared.navigate(.dataStatsExport)`，`HomeRoute` 加 `case dataStatsExport`。
  - 埋点接入点：AppDelegate（app_launch / sceneDidBecomeActive→logSessionStart 30min 节流 / sceneDidEnterBackground→flush）、AuthManager（login_success）、RecognizeService（把 `recognizeWithLocalPriority` 改成 wrapper，内部实现改名 `...Core`，埋 recognize_start/success(types,source)/fail）、RecognitionSaver（record_add:*,source=recognition）、ResultConfirmView（recognize_confirm）、ChatView（chat_send + record_add:*,source=chat）、AddFoodManualView/BodyDataView（source=manual）、RecordsViews（水 record_add:water、todo_done、4 个页面 `feature_open`）。
  - 状态：`node --check` 通过，已 `bash 云函数/aia-sync/package-aia-sync.sh` 打包并同步根目录 `aia-sync-deploy.zip`；xcodebuild BUILD SUCCEEDED，lint 0。**云函数尚未部署**（本机无 CLI），线上 curl `action=stats` 仍返回 `missing userId`（旧版本），部署后应返回统计 JSON。
- **解锁跳转目标改为开发者中心**（SettingsView.swift:110）：长按版本号→输入口令解锁后，原 `.navigationDestination(isPresented:$devNavigate){ AdManagerView() }` 改为 `DeveloperCenterView()`。广告管理仍是开发者中心内的入口（DeveloperCenterView.adManagerEntry），口令正确逻辑/错误提示均不动。xcodebuild BUILD SUCCEEDED，lint 0。
- **埋点加固（云函数 aia-sync）**：`handleLogEvent` 写入前新增幂等 `ensureCollection(EVENTS_COLLECTION)` 自动建 `aia_events` 集合，并把单条失败计数返回为 `failed` 字段（返回结构 `{ok,inserted,failed}`）——解决「集合不存在导致埋点静默丢失、显示 inserted:0」。已打包 `aia-sync-20260731.zip` 并同步根 `aia-sync-deploy.zip`，用户已部署，curl 验证 `inserted:2,failed:0` 通过。
- **用户提交 Apple 开发者付费账号申请（sanhaoai@163.com），状态=注册申请审核中（未激活）**：审核中无法选付费 Team/HealthKit/AppGroups（选 capability 会生成描述文件失败），属预期。已暂停签名配置流程，等 Membership 变 Active 后从「开发者后台开通 HealthKit+AppGroups」继续。待办：签名配好后把 `HealthManager.autoAuthEnabled` 置 true、给 ShareExtension 补 App Group entitlements。Xcode 版本 26.6（打开项目配置面板技巧：先关掉 pbxproj 源码标签页再单击蓝色 AIA 项目图标，或 File→Project Settings…）。
- **首页右上角图标换为设置齿轮（ContentView.swift:233）**：工具栏展开按钮收起态图标 `ellipsis.circle`→`gearshape`，展开态仍 `xmark.circle`；点击行为（展开工具栏）不变。xcodebuild BUILD SUCCEEDED。
- **营养构成目标值为硬编码常量（RecordsViews.swift:842-851）**：蛋白75/碳水220/脂肪55/纤维25/糖50/钠2000mg，通用成人参考值，不关联用户身高体重/热量目标/TDEE，无设置项。
- **健身目标（减脂/增肌/维持）+ 饮食分析「目标达成」卡（全新）**。
  - 数据源：BodyDataView 新增「健身目标」SegmentedPicker 选择卡（存 `@AppStorage aia.fitnessGoal`，改时调 `markProfileChanged()` 打 userProfileUpdatedAt/userSettingUpdatedAt 锚点+syncAfterLocalChange）。`FitnessGoal` 枚举（cut/bulk/maintain）定义在 BodyDataView.swift 顶部，internal 供 RecordsViews 复用。
  - 算法常量：目标热量=TDEE×系数（减脂0.82/-18%、增肌1.10/+10%、维持1.0，依据 Helms 2014 缺口10-25%取中上/盈余10-20%取下沿）；目标蛋白=体重×g/kg（减脂2.0/增肌1.8/维持1.2）。TDEE=health.restingEnergyToday+activeEnergyToday，0 时回落 mifflinBMR×activityMultiplier。
  - UI：DietAnalysisView（饮食页第三段 tab）营养网格下新增「目标达成」GoalCheckCard——顶行目标标识+目标热量，两行进度条（热量/蛋白）+ 状态徽章（热量 ratio<0.95 偏低 warn / 0.95-1.05 达标 ok / >1.05 超出 over；蛋白≥推荐达标否则偏低）；体重缺失时降级为 DietAnalysisNoWeightCard 提示卡。
  - 云同步：CloudSyncManager push profile payload 加 `fitnessGoal`（默认 "maintain"），pull applyProfile 回写；老版本忽略新字段、新版本拉到旧 payload 保持本地值，无兼容风险。
  - 本地化：12 个键 `fitness.goal.*`×5 + `diet.analysis.*`×7（zh-Hans/zh-Hant/zh-HK/en 四份）。
  - 已 xcodebuild BUILD SUCCEEDED（期间修过一处：periodAvg 的 reduce 元组标签丢失报 `no member 'cal'`，改显式 for 循环累加）。
- **更正：健身目标设置模块从「身体数据页」迁到「健康目标页」**（BodyDataView → HealthGoalsView）。
  - BodyDataView.swift：删除 `@AppStorage aia.fitnessGoal` 声明、`fitnessGoalBinding`/`goalCard` 计算属性和 body 里的 `goalCard`；`markProfileChanged` 因唯一调用方消失一并删除（save() 自调 syncAfterLocalChange 不受影响）。**`FitnessGoal` 枚举保留在 BodyDataView.swift 顶部**（internal，DietAnalysisView 仍消费）。
  - HealthGoalsView.swift：加 `@AppStorage aia.fitnessGoal` 声明；「目标身材」区块顶部（目标身高之前）插入 `FitnessGoalCard` 组件（新 private struct，SegmentedPicker 三选一，与页面其他行同款 surface 底/圆角14/hairline stroke），挂 `.onChange(of: fitnessGoalRaw) { markProfileChanged() }`（复用页内已有 markProfileChanged，同身高/体重同步锚点）。
  - 存储键 `aia.fitnessGoal` 不变，云同步（profile 通道 push/pull）、DietAnalysisView 消费、本地化键全不变，纯 UI 位置迁移，无兼容影响。xcodebuild BUILD SUCCEEDED，lint 0。
- **营养构成「目标」改「建议」+ 三值联动健身目标（RecordsViews.swift Card2）**。
  - 文案：4 语言文件 `food.nutritionTarget` 的「目标/目標/Goal」→「建议/建議/Suggested」（键名不变）。
  - 算法（nutritionTargets，替代硬编码 75/220/55）：建议热量 = 自定义 `aia.calorieGoalOverride`（尊重用户设定不再乘系数）或 TDEE × 健身目标系数；蛋白 = 体重 × g/kg（减脂2.0/增肌1.8/维持1.2）；碳水 = 热量×50%÷4、脂肪 = 热量×25%÷9（碳水/脂肪比例取 USDA DGA 45-65%/20-35% 中值）。任一前提缺失（无体重/无热量基准）回落 75/220/55。进度条分母同步改（`macros.x / Double(target)`，target 为 0 时进度 0）。
  - 依赖：饮食记录页已复用 `@AppStorage aia.fitnessGoal`（变量名 fitnessGoalDiet，同文件多页同名键不冲突）+ 已有 goal/tdee/weightKgDiet。xcodebuild BUILD SUCCEEDED，lint 0。
  - 追加：顶部副行数值顺序改为「碳水 / 蛋白 / 脂肪」（原蛋白/碳水/脂肪），4 语言 `food.nutritionTarget` 字符串重排 + RecordsViews.swift 传参改 `t.carb, t.protein, t.fat`，与下方 MacroCard 网格顺序（碳水/蛋白/脂肪）一致。xcodebuild BUILD SUCCEEDED。
- **饮食分析「目标达成」卡新增纤维/糖/钠三行（按目标热量线性缩放）**（RecordsViews.swift）。
  - 用户公式：纤维 = 建议热量 × 14 ÷ 1000；糖 = 建议热量 × 10% ÷ 4；钠 = 建议热量 × 1000 ÷ �2000 × 2000（用户原文如此）。
  - 实现：`periodAvg` 扩展聚合周期日均 fiber/sugar/sodium；`GoalCheckCard` 增 fiberTarget/sugarTarget/sodiumTarget + 实际值参数，body 加三行 goalRow（纤维"越高越好"、糖/钠"越低越好"上限目标，状态判定 0.95/1.05 阈值）。
  - **基准取舍**：本卡热量基准是 `calorieTarget`（= TDEE × 健身目标系数，即卡片顶部"目标热量"）；而"建议热量" suggestedCalories 定义在另一视图（依赖自定义目标键 `aia.calorieGoalOverride`，本卡作用域不可用）。故三项目标统一用 `calorieTarget` 作"建议热量"等价基准，与热量行同源一致（无 TDEE 时卡片降级不显示，沿用原行为）。
  - **钠公式修正（待用户确认）**：用户原公式字面化简 = 建议热量 × 1000（2000kcal→2,000,000mg，数值荒谬）。按营养学合理口径实现 `sodiumTarget = calorieTarget`（2000kcal 标准人对应 2000mg 钠，线性缩放 = 目标热量 mg）。代码注释保留原公式并注明如需严格字面改 `cal * 1000`。**用户尚未确认此修正**，若其本意为 ×1000 需改回。
  - xcodebuild BUILD SUCCEEDED。
- **饮食记录页「营养构成」卡每格显示「实际 / 建议」+ 建议量随热量缩放**（UIComponents.swift + RecordsViews.swift）。
  - `MacroCard` 加 `targetText: String?` 可选参数；主数值 subhead 大字后跟「/ 建议量」muted 微缩 + minimumScaleFactor(0.7) 防溢出。
  - `nutritionTargets` 3 元组扩为 6 元组（protein/carb/fat/fiber/sugar/sodium）；纤维/糖/钠按「建议热量」线性缩放（cal×14/1000、cal×10%/4、cal mg），与目标达成卡同公式；无建议热量回落 25/50/2000。**钠 = 建议热量(mg) 同上一轮修正一致**。
  - 纤维/糖/钠 progress 基准也改为跟随 `t.fiber/t.sugar/t.sodium`（原硬编码 25/50/2000 不再适用）。
  - **Swift 编译器「unable to type-check」级联卡死应对（重要经验）**：把 Card2 整体抽成独立子视图 `NutritionCompositionCard`（含参数 + 私有辅助 `valueText/progress`），父级 body 就一行调用，是根治手段；只在父级拆 let 抽变量（progress/字符串/标题分别抽）只能拖延级联，不能根治。MacroCard 参数顺序需把新增 `targetText` 放在 `progress` 之前，否则编译器会报「argument 'progress' must precede argument 'targetText'」——Swift 结构体自动合成 init 对 labeled arg 顺序敏感。
- **「建议量」从 value 后挪到 title 后 + 顶部汇总行删除**（UIComponents.swift + RecordsViews.swift）。用户要求只调展示位置，不动 `nutritionTargets` 计算。
  - `MacroCard.body`：value 单独大字一行（去掉原 `/ targetText` 同行小字）；title 与「建议\(target)」（sub 微缩）改为同一 HStack 同行，title muted、建议 sub 更弱。
  - `NutritionCompositionCard`：SectionTitle trailing 改 `nil`（删除"建议 碳水238 / 蛋白71 / 脂肪 53 g"汇总行，因各卡已带建议量）。
  - `targetText` 参数 / 调用 6 处都未动，只挪了它在 MacroCard 内的渲染位置；`nutritionTargets` 计算口径（含上一轮钠修正）保持不变。
  - xcodebuild BUILD SUCCEEDED。
- **食物营养「同一食物数值不同」根因修复（手动 vs 聊天路径归一一致性）**：
  - 根因：手动搜索 `FoodSearcher.localSearch` 走本地 `NutritionLibrary.shared.match` 优先；聊天 `ChatView.createFoodFromCloud` 直接走云端 `queryFood` 跳过本地 → 常见食物两端数值可能不一致。
  - A1/A3：`createFoodFromCloud` 改为**本地优先**——`canonicalFoodName` 归一后 `match` 本地单库，命中即用确定值（与手动同源）；仅本地不认识才走云端。
  - C1：扩充 `NutritionLibrary.rows` 到 354 项 + 别名 73 条 + `seasoningPrefixes` 去前缀，`SEED_VERSION=2`。
  - C2：`FoodMetaStore.seedBuiltin`（覆盖式）+ `AppDelegate` 改**版本化 seed**（读 `foodMetaSeedVersion`，老用户升级自动重 seed，刷新被 LLM 污染的旧 cloud 值）。
  - C3：云端 `handleQueryFood` 查表优先——新增 `云函数/recognize/foodTable.js`（AUTO-GENERATED，由 `gen-food-table.mjs` 从 `NutritionLibrary.swift` 解析生成，零转录误差），命中即返确定值（对齐 App 端 `result.types/food` + `kcal→calories`），仅长尾回落 LLM。
  - 打包脚本 `package-recognize.sh` 已加 `foodTable.js`；`FN_VERSION='20260731a-foodtable'`；已重打包。**已部署**（用户 2026-07-31 上传 `recognize-20260731.zip`）。curl 验证通过：`mode:queryFood&foodName=红烧牛肉` 返回 `source:table, matched_name:牛肉, calories:125, ver:20260731a-foodtable`，与本地库一致、且未触发 LLM。
  - App 端 BUILD SUCCEEDED。注意 `rows` 元组已改为 8 元素（name+kcal,protein,carbs,fat,fiber,sugar,sodium），老行（火腿、咖喱）缺字段已补齐。
- **Siri/快捷指令记食物与聊天路径对齐（LocalQuickParse + TellAIAIntent）**：原 Siri 食物解析只走本地 `match` 且不归一，本地不识则整句回落 `RecognizeService.parseText`(LLM)，与聊天「本地优先+queryFood查表」不同源。现对齐两点：
  - ① `LocalQuickParse.parseFood` 食物名先经 `NutritionLibrary.canonicalFoodName` 归一再 `match`（消除红烧牛肉→牛肉这类漂移）。
  - ② 本地不识的长尾改 `try? await RecognizeService.queryFood(name:)`（云端查表优先，非整句 LLM），命中后 `FoodMetaStore.upsert(...source:"cloud")` 写缓存；失败才回落上层 `parseText`。
  - 抽出 `resolveFoodRef(name,in:)`(async, 返回 (ref,fromCloud)) 与 `upsertFoodRef`；`parse`/`parseFood` 均改 async，`TellAIAIntent.perform` 调用处加 `await`。与 `ChatView.createFoodFromCloud` 逻辑完全同源，三端（手动/聊天/Siri）食物数值一致。xcodebuild BUILD SUCCEEDED，lint 0。
