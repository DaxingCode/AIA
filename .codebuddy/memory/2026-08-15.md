# 2026-08-15

## 启动页卡死 → 回退 18:19 之前版本 · 后续排查

### 用户反馈路径
- 用户要求"回退到 18:19 修改之前的版本"
- git 状态：217 个文件未提交，最新提交 9d4fd35（更早），无 18:19 提交点
- 关键 4 个启动相关文件改动：ContentView +1098/-471, AIAApp +199/-24, QuickActionRouter +112/-13, AppDelegate +188/-238（含功能改动，非纯启动修复）

### 我犯的错
- 之前建议 "Xcode Editor → Local History" —— **错的！Xcode 没有 Local History**（混淆了 IntelliJ/VSCode）。
- 用户截图（Xcode View 菜单）证明 Xcode 本身无此功能。
- 纠正：未提交文件恢复用 **macOS 原生版本系统**（Finder 选中 → 顶部菜单栏 **文件** → **复原到** → **以前存储的版本**）。

### Finder 修改时间定位（用户截图 18:19 之后改过）
- ContentView.swift 21:52
- QuickActionRouter.swift 21:28
- AIAApp.swift 21:18
- Info.plist 18:48
- AppDelegate.swift 18:25
- main.swift 18:17

### 重要提醒（已告知用户）
- 修改时间 ≠ 18:19 的内容快照，只能定位修改范围，不能用来恢复 18:19 内容
- macOS"复原到→以前存储的版本"在顶部"文件"菜单，不在右键菜单
- 回退 4 个文件会丢今天的功能（试用弹窗、每日小结、周期账单等）
- ~~探针已证根因不是这 4 个文件的某次修改（是 @Query 首帧数据加载 + NavigationStack 撞车），回退不一定解决问题~~ **【已推翻·以 2026-08-15 晚覆盖的 4 文件为准】**：覆盖后 ContentView 仍是 `@ObservedObject`（注释称改 let 未落实）、`Self._printChanges()` 探针仍在跑，启动死循环根因尚未定性，仍在排查中。

### 用户的右键菜单截图确认
- 右键菜单无"复原到"，符合预期；正确路径是顶部"文件"菜单

### 2026-08-15 晚 覆盖 4 文件（以用户发出的 ContentView/AIAApp/AppDelegate/QuickActionRouter 为准）
- 入口维持纯 SwiftUI @main App + WindowGroup（未退回经典 UIKit 入口）；AppDelegate 纯 UIApplicationDelegate，performActionFor 已删。
- ContentView 70-72 行 quickAction/router/layout 仍是 @ObservedObject（注释声称改 let 未落实），Self._printChanges() 探针仍在 → 启动死循环仍在排查未根治。
- NavigationRouter 已落地 enqueue/flushScheduled 帧合并，根治同帧多次改 path 崩溃（已生效）。

### 2026-08-15 晚 第二次崩溃：Desktop/bug版 覆盖导致启动即崩
- 用户把 `/Users/daxing/Desktop/bug版/` 的 6 个文件（ContentView/AIAApp/AppDelegate/QuickActionRouter/Info.plist/main.swift）覆盖进 `AIA/AIA/`，app 启动即崩；换回稳定版后正常。
- **根因**：bug版 把入口从「纯 SwiftUI @main」改成了「经典 UIKit（`main.swift` 的 UIApplicationMain + AppDelegate 兼 UISceneDelegate 自建 window）」，但 `Info.plist` 的 `UISceneConfigurations` → `Default Configuration` **只写了名字、漏填 `UISceneDelegateClassName`** → 系统进 Scene 生命周期却找不到 SceneDelegate → 启动崩/卡死。这是半成品入口改造。
- **逐文件 diff 定论**：bug版 没有带来任何稳定版缺失的业务逻辑（AppBootstrap/SplashView/RootView、截图识别/APNs/通知路由/试用探测、帧合并/checkScreenshotPending 都已与稳定版一致；稳定版反而多了 `Self._printChanges()` 探针）。**bug版全部 6 文件无保留价值，直接丢弃即可**。
- 处置：工作区已恢复纯 SwiftUI 入口（AIAApp @main+WindowGroup、AppDelegate 仅 UIApplicationDelegate、Info.plist 无 UISceneManifest、无 main.swift）。`/Users/daxing/Desktop/bug版` 用户选择**保留**作对照、未删。
- 教训再固化：经典 UIKit 入口这条路线在本项目已证伪（缺 SceneDelegate 类名即崩），不要再走；纯 SwiftUI @main 入口是已验证稳的基线。
