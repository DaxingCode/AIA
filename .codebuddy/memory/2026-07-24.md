# AI 助理 · 工作日志

## 2026-07-24 · recognize 云函数升级为「全能数据助理」（FN_VERSION 20260724f-allops）
- 需求：用户希望阿宝能像真人助理一样查看/修改/删除 App 里**所有**数据、操作所有功能。
- 架构前提（已确认）：云端 agent 只能操作 `aia_records` 集合里**已云同步**的数据；App 在 agent 回复后会 `syncAfterLocalChange` → push+pull 把云端改动拉回本地。
- 已云同步可被 agent 管理的类型：`bill` `reminder` `food` `health` `merchant_meta` `recognition` `chat`。
- **本地-only、云端 agent 管不到**（历史约束，部分已解决）：`WaterLog`（饮水）、`RecurringRule`（周期账单排程）已于 20260724h 纳入云同步；仍仅本地：`FoodNote`/`ReminderNote`。
- 本次新增工具（agentTools.js）：
  - 商户分类规则：`set_merchant`（按商户名 upsert，复用已有 id 防重复）/ `list_merchants` / `delete_merchant`。
  - 识别历史：`list_recognitions` / `delete_recognition`。
  - 跨类搜索：`search_all`（bill/food/reminder/health 按关键词，返回 id+type）。
  - 区间报告：`get_report`（today/yesterday/last7Days/last30Days/thisMonth/lastMonth + 自定义 from/to；含收支/分类占比/Top 商户/饮食营养/待办完成/最新健康）。
  - 批量：`complete_all_todos`（可 keyword 过滤）/ `delete_by_merchant`（默认仅账单，可扩展 type/types）。
  - **关键修复**：`create_bill/food/todo/health` 改为**合并更新**——传 id 时只覆盖有值的字段、保留其余原值（App 端 apply* 也会再 merge 一次）。此前部分修改（如"把滴滴那笔改成餐饮"只传 category）会清掉金额/时间等字段。
- agentSchema.js：新增上述 9 个工具的 function schema（不含 userId，handler 注入）。
- agentHandler.js：TOOL_FUNCS 注册 9 个新工具；**幻觉检测正则扩展**覆盖商户规则/识别/报告/搜索/批量意图 + "已更新/已新增/已生成/已搜索/已找到/已标记完成"成功词。
- agentPrompt.js：能力清单扩到 11 项；新增【商户分类规则】【搜索/报告/批量】规则段；跨轮记忆说明覆盖全部 recent 列表（含 id）。
- ChatView.swift：新增 `@Query merchantMetas` / `@Query recognitions`；`buildContext()` 加 `merchantRules`(id/merchant/category/isIncome) 与 `recentRecognitions`(id/rawText/types/recognizedAt)，新增 `buildContext_merchantDict` / `buildContext_recognitionDict`。
- 验证：`node --check` 5 个 JS 全 OK；`xcodebuild -scheme AIA` 模拟器+关签名 **BUILD SUCCEEDED**；打包 `云函数/recognize/recognize-20260724f-allops.zip`（6 文件）。
- 仍需手动：① 上传 zip 重部署 recognize 云函数；② 重编译安装 App（否则 merchantRules/recentRecognitions 不进上下文）。

## 2026-07-24 · 阿宝「学习成长」能力（FN_VERSION 20260724g-learn）
- 需求：阿宝遇到不会的问题要主动问用户怎么做；用户教会后，阿宝下次按教的方式做（能力可成长）。
- 方案（刻意**不碰 SwiftData**，避免 @Model/schema 迁移风险）：把"用户教的习惯/规则"存进云端 `aia_records`（type=`abao_learning`，随用户云同步），由 handler 每轮自动拉取注入系统提示词，阿宝永远"记得"并作为最高优先级指令执行。
- agentTools.js 新增：`teach_abao`（按 topic 去重 upsert，没 topic 用 rule 前 12 字；payload 含 topic/rule/example/learnedAt）、`list_teachings`（可 keyword 过滤）、`recall_teachings`（按 query 匹配 topic/rule/example）、`getLearnedRulesForPrompt`（供 handler 注入，取最近 30 条倒序）。
- agentHandler.js：TOOL_FUNCS 注册 3 个工具；进入对话前 `await getLearnedRulesForPrompt` 拼 `{LEARNED_RULES}` 占位符（默认"（暂无，用户还没教过我什么）"）；写意图/幻觉正则扩展纳入"教/记住/习惯/下次照做/怎么处理"及"已教/已学/记住了/教过你了/学到了"成功词。
- agentSchema.js：追加 teach_abao/list_teachings/recall_teachings 的 function schema。
- agentPrompt.js：身份描述加"会成长"；能力清单加第 12 条【学习成长】；新增【学习成长/你学到的用户习惯】段——说明 {LEARNED_RULES} 为已学规则，且：(1) 不会的/超能力范围要诚实说"还没学会"并请用户教；(2) 用户教时先调 teach_abao 再确认；(3) 用户问"你都学会了什么"用 list_teachings。
- 设备端零改动：`CloudSyncManager.swift` switch type 的 `default: break` 已能安全忽略 `abao_learning`，不会崩溃也无需建模型。
- 验证：`node --check` 5 个 JS 全 OK。仍需手动重部署 recognize 云函数（FN_VERSION=20260724g-learn）。
- 边界：规则只作用于"阿宝被问到相关场景"时；纯 LLM 判断"会不会"，靠提示词引导其诚实求助，非 100% 确定性。
- 仍遗留：SenseChat-Turbo function-calling 服从度一般（"说删了实际没删"可能需换 DeepSeek-V3/Qwen）；`FoodNote`/`ReminderNote` 仍仅本地（暂无管理需求）。

## 2026-07-24 · 阿宝可管理饮水/周期排程 + 「新功能须可经云端由阿宝管理」硬规则（FN_VERSION 20260724h-newfeat）
- 需求：阿宝 也要能管饮水、周期排程；并确立设计规则——**App 新增任何功能，阿宝 都应能管理**（即新功能数据须走云同步 + 配套 agent 工具），将此规则写入工作记忆。
- 云函数侧（agentTools.js 新增 6 工具）：`create_water`/`delete_water`/`list_waters`（amount 单位毫升；range=today/yesterday/last7Days/last30Days/thisMonth/不填=全部，用文件内 `startOfDayShanghai` 算区间）、`create_recurring`/`list_recurring`/`delete_recurring`（同商户名去重；cycle=daily/weekly/monthly/quarterly/yearly/custom）。存储 type=`water`/`recurring_rule`，复用 `queryRecords` 与 `crypto.randomUUID`。
- agentSchema.js 追加 6 个 function schema；agentHandler.js 注册 6 工具 + 写意图正则纳入"喝水/饮水/水/周期/排程/订阅/自动扣费/自动记账"；agentPrompt.js 能力清单加 13 饮水 / 14 周期排程，并新增【可管理范围的扩展规则】段（"App 加了什么，阿宝 就能管什么"，禁用本地-only 死角，附新增功能 8 步检查清单）。
- 设备侧（CloudSyncManager.swift，无 schema 迁移——WaterLog/RecurringRule 已是 sync-ready 模型）：`buildPushItems` 加 WaterLog(`water`)/RecurringRule(`recurring_rule`) 推送；`pull` switch 加 `case "water"/"recurring_rule"`；新增 `applyWater`/`applyRecurring`；`cleanupSyncedTombstones` 加两者墓碑清理。阿宝 pull 到的 recurring_rule 由 `RecurringBillManager.generateDue` 在下次前台自动生成账单。
- 验证计划：`node --check` 5 个 JS；`xcodebuild -scheme AIA` 模拟器+关签名。FN_VERSION=20260724h-newfeat。
- 边界：① 饮水新增 type=`water` 独立于 FoodEntry.waterIntake，二者在饮食页聚合展示；② 周期规则跨设备 lastGeneratedAt 同步可能致重复生成账单（单用户无碍）；③ 仍遗留 FoodNote/ReminderNote 仅本地。

## 2026-07-24 · 阿宝账号隔离加固（A+B，FN_VERSION 20260724i-isolation）
- 需求：用户明确"阿宝只能管当前登录账号的数据，不能跨账号操作他人数据"（张三指挥阿宝动李四的数据必须被拒绝）。
- 结论前置：云端 recognize 本就两层隔离——① App 端 `ChatView`/`CloudSyncManager` 只发当前登录 `aia.userId`；② `agentHandler.js` 工具调用前无条件覆盖 `args.userId = userId`（userId 取自请求体，非模型可控）。切换账号 `AuthManager.login/logout` 会清/覆盖，不会串号。
- 改动 A（agentPrompt.js）：新增「数据边界·只能管当前登录账号」段——要求对跨账号增删改查请求明确拒绝（"我只能管理你自己的数据哦～"），且不要把第三方名字当商户名误改自己数据。
- 改动 B（agentHandler.js）：覆盖前检测若模型自带 `args.userId` 且与会话不符 → `console.warn` 留痕 + `delete` 剥离，再统一覆盖（防御纵深，防未来新增工具误读）。
- index.js：`FN_VERSION` 升 `20260724i-isolation`；`node --check` 3 文件全 OK；重打包 `云函数/recognize/recognize-20260724i-isolation.zip`（6 文件）。纯云端改动，App 端零改动。
- 平台级残留：/recognize 与 /sync 均无真鉴权（靠 userId 不可猜性），"知道 userId 冒充他人"为共有假设；真鉴权为独立后续项，未做。
- 记忆：已把"账号隔离硬约束"写入 MEMORY.md「阿宝可管理范围」段下。

## 2026-07-24 · 招呼语升级（ABCD，FN_VERSION 20260724j-greeting）
- 需求（用户反馈）：招呼语总是说早餐、所有入口都一样（太重复）。
- 现状澄清：招呼语非随机，是确定性数据驱动。本地 `buildGreeting()`（兜底）+ 云端 LLM `handleGreeting`（异步替换，默认开启 `aia.greetingLLM=true`）。之前觉得"每次一样"是因为数据（14天早餐众数 + 未记早餐）每次都满足同一条件。
- **A · 入口感知**：ChatView 新增 `entrySource` 参数（home/voice/todoReminder）。`buildGreeting()` 根据入口给不同开场：voice→"我刚听到你按了语音键"、todoReminder→"你刚才叫我提醒你"；`buildGreetingContext()` 传 `entrySource` 给 LLM。
- **B · 多款变体 + 每日限次**：早餐提醒用 4 种变体轮换、已记反馈 3 种、待办 3 种、步数 3 种，通过 `@AppStorage("aia.greetingVariant")` 轮转。早餐提醒每天只弹一次（`lastBreakfastReminderDate` 对比 todayStr），再次进聊天页不复读。
- **D · LLM 更多素材**：`buildGreetingContext()` 新增 `todayBillSummary`（今日笔数/支出/收入/Top 支出）、`latestHealthMetric`（最近一次健康指标）。`GREETING_SYSTEM_PROMPT` 更新字段说明，新增"注意多样性"要求。
- 涉及文件：ChatView.swift（属性/init/buildGreeting/buildGreetingContext）、QuickActionRouter.swift（chatEntrySource）、ContentView.swift（传 entrySource）、UIComponents.swift（设 entrySource）、云函数 index.js（GREETING_SYSTEM_PROMPT）。
- 零 lint 错误。打包 `recognize-20260724j-greeting.zip`（云端需重部署才能生效 LLM 版）。App 端本地改动即时生效（不需 LLM 也体现 ABCD）。

## 2026-07-25 · 补齐 query_reminders + 提问 vs 指令规则（FN_VERSION 20260724k-query）
- 问题复现（阿宝截图）：用户问"我现在有什么待办提醒"→ 阿宝却创建了待办"我现在有什么"。用户由此产生疑问"是不是规则太多限制发挥"。
- 诊断：非规则问题。`agentPrompt.js` 第 4 条写了"还有哪些待办→调查询工具"，但 `query_bills/foods/health` 都存在，唯独 **`query_reminders` 缺失**。模型手里没待办查询工具，只能退化去 `create_todo`。
- 修复：
  - `agentTools.js` 新增 `query_reminders(args)`——同 query_* 模式，支持 from/to/done/limit，通过已有 `queryRecords` 查 reminder 类型。
  - `agentSchema.js` 追加 `query_reminders` function schema（done/from/to/limit 参数）。
  - `agentHandler.js` TOOL_FUNCS 注册 `query_reminders: TOOLS.query_reminders`。
  - `agentPrompt.js` 第 4 条工具列表加 `query_reminders`；**新增「区分提问 vs 记录指令」规则段**：含"?""多少""吗""什么"等疑问词优先当查询，举"我现在有什么待办提醒→查不是建"为示例，拿不准宁可先查也不乱建。
- 5 个 JS 语法全通过。打包 `recognize-20260724k-query.zip`（累积包含 j 招呼升级 + k 查询补齐）。
- 结论回复用户：不是规则太多，是规则→工具的闭环没建好，已补齐。上传 k 部署即可。

## 2026-07-25 · 工具补全 + 上下文补全（FN_VERSION 20260724l-tools）
- 用户进一步要求"如叫一个人帮我操作 app 一样"，做系统审计。审计后发现的 3 类缺口：
  - **writeIntent 正则不全**：缺 `吃/喝/餐次名/健康指标名/配置` 等关键词。"早餐吃了鸡蛋" 不会命中写意图 → 工具模式未激活 → 直接当闲聊回复。
  - **buildContext() 漏了 WaterLog 和 RecurringRule**：ChatView 里根本没 @Query 这两个模型，agent 第一句话就不知道用户有多少饮水/周期规则记录。
  - **buildContext_foodDict 没导出 waterIntake**：即使 FoodEntry 里记了水量，agent 也看不见。
- 修复：
  - `agentHandler.js`：writeIntent 正则加 `吃(?:了|掉|过)?|喝(?:了|掉|过|杯|了点)?|早餐|午餐|晚餐|加餐|宵夜|夜宵|体重|身高|心率|血压|睡眠|体脂|血糖|配置`。
  - `ChatView.swift`：加 `@Query private var waters: [WaterLog]`（带 `!syncDeleted` 谓词 + 按 date 倒序）和 `@Query private var recurringRules: [RecurringRule]`（带 `!syncDeleted` 谓词）；buildContext 导出 `recentWaters`（前 20 条 id/amount/date）+ `recurringRules`（merchant/amount/category/isIncome/cycleRaw/dayOfMonth/note）。
  - `ChatView.swift`：`buildContext_foodDict` 加 `waterIntake` 字段。
- 5 个 JS 语法全通过；ChatView 零 lint 错误。打包 `recognize-20260724l-tools.zip`（累积包含 j 招呼升级 + k 查询补齐 + l 工具/上下文补全）。
- 自我纠正：先前说"query_bills/foods/health 都不存在"是错的，三者一直都在，只有 query_reminders 是真的缺（用户截图核验）。`search_content` 用普通模式没命中、用 `^async function` 锚定行首才看清真相。后续审计必须用锚定搜索。

## 2026-07-25 · ③ 提示词结构优化 + ④ 多步推理（FN_VERSION 20260724m-reason）
- 用户目标："让阿宝变得跟你一样聪明"。拆解为 4 个层次：①换模型 ②加 web_search ③优化提示词 ④多步推理。用户选了 ③+④。
- **③ agentPrompt.js 重写**：从 14 条平铺能力列表 + 多段规则 → P0-P4 优先级分层结构：
  - P0 硬边界（数据隔离 + 禁止幻觉）
  - P1 用户教过的习惯（最高优先级）
  - P2 决策流程（区分提问vs指令 → 新建vs修改 → 选工具）
  - 工具清单按场景分组（查询/新增/修改完成/删除/商户/饮水/周期/学习/报表批量），附正反对比例
  - P3 字段规则
  - 防错清单（6 对常见错误→正确做法，含"我现在有什么待办提醒→查不是建"截图bug示例）
  - P4 回答风格 & 附加规则
  文件从 ~103 行扩展到 ~150 行（含大量结构化格式与示例帮助模型对齐）。
- **④ agentHandler.js 多步推理**：
  - 注入「思维步骤」系统指令：每轮让模型先想"查还是记"→再选工具→空结果换方案→提问不建记录。
  - 空结果兜底机制：query_/list_/search_all 工具返回 count=0 / data=空 / "未找到" → 自动追加提示"换个关键词或工具再查"，让模型有机会在下一轮尝试替代方案再告诉用户"没有"。仅在 round<3 时触发，避免无限循环。
- 所有 JS 语法全通过。打包 `recognize-20260724m-reason.zip`（累积包含此前 j-k-l-m 全部功能；是截止当前的最完整版本）。
