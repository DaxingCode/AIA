# 2026-08-01 工作日志

## 对话页 ↔ 模块页 双向同步（改造开工）

### 已落地（编译通过 BUILD SUCCEEDED）
- 新增 `AIA/AIA/ResultRowCard.swift`：
  - 协议前缀 `RECOGNITION_RESULT_PREFIX = "__RECOGNITION_RESULT__"`；`ChatMessage.text = 前缀 + JSON(RecognitionResultPayload)`。
  - `RecognitionItem`(含 `syncId:String?`)/`RecognitionResultPayload`/`ItemPayload`(bill/food/todo/health 关联值枚举 Codable)。
  - `ChatRecognitionBubble`：用 4 个 `@Query(!syncDeleted)` 拉四类「未软删」全集，按 `syncId.uuidString` 取活对象渲染。
  - 四类卡片 `BillRowCard/TodoRowCard/FoodRowCard/HealthRowCard` 三态：
    - 待确认（syncId=nil）：就地编辑字段 + 保存(入库写 syncId)/删除(移除卡片)。
    - 已保存（syncId 命中活对象）：in-place 编辑绑定真实模型(@Bindable) + 复制 + ••• + 删除(SafeDelete.*)。
    - 已删除（活对象不在）：`EmptyView` → 卡片整张消失。
  - **双向同步机制成立**：卡片引用真实模型实例（同 modelContext），任一端改/删经 `@Query(!syncDeleted)` 自动刷新到另一端。
- `ChatView.swift`：
  - 相册/相机识别完成 `.present(.pending)` → 不再弹 `ResultConfirmView` 全屏 sheet，改为 `insertRecognitionBubble` 把结果插成一条 AI 消息（协议气泡）。
  - `.present` 非 pending（saved/duplicate）仍走旧确认页（安全保留）。
  - `messageRow`/`messageList` 抽离，解决「unable to type-check」超时（ChatView body 过大）。
  - `.onChange(of: fileImportTag)` 触发插入（fileImportCoverItem 是 `CameraCoverItem?`，非 Equatable，故用派生 String 标记）。

### 未做（仍属 2026-08-01 定稿计划，待续）
1. `ContentView.checkScreenshotPending` / `UIComponents` 相册识别的 `.present` 也改路由进对话气泡（目前仍弹 `ResultConfirmView` sheet）。
2. `ImageAutoRecogSettings` 扩 source 维度（image/text 二维）+ 分组 UI（三档：自动保存/待确认/丢弃）。
3. `RecognitionSaver.processRecognition` 返回 per-type 状态列表（.saved/.pending/.discarded）。
4. `ResultConfirmView` 抽 `Editable*` 到 internal + `applyAndSave` 复用（本改造在 ResultRowCard 内独立实现了保存逻辑，未复用旧 Editable）。
5. 通知落地页 CRAFT 时改跳对话页。

### 验证建议
- 构建已通过；运行时需在模拟器/真机验证：对话页识别→保存→去账单页看记录；账单页删→回对话页卡片消失。
