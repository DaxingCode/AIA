# 识记AI · 截图无感识别 快捷指令配置指南

> 目标：手机上**截屏后自动识别**图片内容（账单/饮食/待办/健康），后台静默完成，打开 App 时弹出确认页让你核对入库。

---

## 一、原理（为什么是这样）

- App 内置了一个系统动作 **「识别截图」**（App Intent，`ProcessScreenshotIntent`）。
  它能在**后台静默运行**（`openAppWhenRun = false`），不打开 App、不打断你。
- 配合 iPhone「快捷指令」App 的 **「个人自动化：当截屏时」**，截图动作一发生就自动把最新截图传给这个动作。
- 链路：截屏 → 快捷指令拿最新截图 → 调「识别截图」→ 云端大模型识别 → 结果存进 App 沙盒 → 发本地通知 → 你打开 App 时自动弹「确认识别结果」页 → 你改字段后点「存入」才入库（防止误识别）。

### 关于「自动截图」的说明
iOS **没有让 App/快捷指令主动「自动截图」的接口**（截屏是系统手势）。
这里说的「自动」是指：**你正常截屏的那一刻，自动触发识别**。这是目前唯一合规、稳定的方案。

### 免费账号也能用
- 免费 Personal Team 无法启用 **App Group**，旧方案把结果存 App Group 会直接失效。
- 本次已改为**主 App 沙盒文件存储**（`Documents/pendingRecognition.json` + `attachments/` 原图）。
  App Intent 后台运行时就在主 App 沙盒内，读写一定能共享，免费账号真机即可工作。

---

## 二、在 iPhone 上配置（约 1 分钟）

> 前提：iPhone 已装好「识记AI」并**至少打开过一次**（让系统注册 App Intent）。模拟器无法验证。

1. 打开 **「快捷指令」App** → 底部切到 **「自动化」** → 右上角 **「+」** → **「创建个人自动化」**。
2. 滚动到 **「设备」** 区，选择 **「截屏」**。
3. 点 **「添加操作」** → 搜索「获取」→ 选 **「获取最新的截屏」**（拿到那张截图图片）。
4. 再点 **「+ 添加操作」** → 搜索「识别」或「阿记」→ 选 **「识记AI」下的「识别截图」**。
   - 上一步「获取最新的截屏」的结果会**自动作为「截图」参数**传入，无需手动连。
5. **关键**：关掉 **「运行前询问」**（否则每次截图都弹确认框，就不是无感了）。
6. 点 **「完成」**。

> 可选：若你更想「手动触发」而不是监听截屏，可在「快捷指令」里建一个**普通快捷指令**
> （获取最新截屏 → 识别截图），需要时手动运行，或加到桌面小组件。

---

## 三、使用流程

1. 任意界面截屏（电源键 + 音量上键）。
2. 后台自动识别（约 1~2 秒，**不打开 App**），成功后收到通知「识别完成」。
3. 点通知、或自己打开 App → 自动弹出 **「确认识别结果」** 页。
4. 检查/修改字段（金额、分类、餐次、重量等）→ 点 **「存入」** 才写入对应模块。

---

## 四、注意事项 & 排错

| 现象 | 可能原因 / 处理 |
|---|---|
| 快捷指令里搜不到「识别截图」 | App 没装好或没打开过；重启「快捷指令」App 再试 |
| 截图后没有任何通知 | 检查：① 通知权限是否开启（设置页「发送测试通知」已验证可用）；② CloudBase 云函数是否部署了最新版；③ 手机是否联网 |
| 打开 App 没弹确认页 | 说明后台识别没成功（无通知=没跑）。检查权限/网络；或进 App 用相机/相册手动识别兜底 |
| 识别类型不对/字段错 | 正常——确认页就是给你改的；改完再存 |
| 模拟器里没反应 | 模拟器无法截屏、也没有「快捷指令」自动化，必须在真机（iPhone 15 Pro Max）验证 |

---

## 五、代码改动记录（本次）

- `AIA/AIA/ScreenshotStore.swift`：去掉 App Group，改为沙盒文件存储（`pendingRecognition.json` + `attachments/` 原图），`PendingRecognition` 增加 `Identifiable`。
- `AIA/AIA/ScreenshotIntent.swift`：`save` 改为同时写入 `rawText` 与原图二进制。
- `AIA/AIA/ContentView.swift`：打开 App（onAppear / 回到前台）时读取 pending，自动以 `fullScreenCover` 弹出 `ResultConfirmView`；确认页关闭即 `clearPending`，避免重复弹。

> 编译：`xcodebuild -scheme AIA -destination 'generic/platform=iOS Simulator' build` → **BUILD SUCCEEDED**（2026-07-19）。
