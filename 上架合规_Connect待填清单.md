# 好记AI · App Store Connect 上架待填清单

> 配套文件：`上架准备包.html`（勾选式）、`privacy-policy.html`（隐私政策）、`好记用户协议.html`（本 EULA）。
> 以下为你在 **App Store Connect 网页端** 提审前必须补齐的字段。代码层（账户删除 / 订阅）已实现。

## 1. App 信息 → 条款（Terms of Use / EULA）URL【因做订阅→必填】
- **填写值**：`好记用户协议.html` 的云端网址
  `https://cloud1-d1ga55pizf294dbe9-1253522789.ap-shanghai.tcloudbaseapp.com/好记用户协议.html`
- 前提：需先把 `好记用户协议.html` 与 `privacy-policy.html`（云端文件名 `好记privacy.html`）部署到 CloudBase 静态站根目录，确保链接 200 可访问（苹果审核时会爬）。
- 若云端 `aia_config` 已配置 `userAgreementUrl`，App 端会优先用它；否则回退上面默认域名链接。

## 2. App 描述 / 推广文稿【Guideline 3.1.2 强制补订阅说明】
在描述中明确写出：
- 会员套餐：包月 ¥8.8 / 月、包年 ¥88 / 年（自动续期）。
- 自动续期说明：当前周期结束前 24 小时由 Apple 扣下一周期费用；可在「设置 → Apple ID → 订阅」取消。
- 退款由 Apple 统一处理（链接 support.apple.com 报告问题页）。
- 隐私政策与用户协议链接。
- 免费功能边界：本地记账/待办/饮食/健康/基础识别永久免费。

## 3. App 隐私 → 营养标签【须与 privacy-policy.html 声明一致】
| 数据类型 | 用途 | 是否关联 |
|---------|------|---------|
| 照片或视频 | 截图/拍照识别（部分图片经授权上传云端视觉模型） | 与您关联 |
| 健康与健身 | 身高/体重/健康指标/手动健康数据（开启云同步后上云） | 与您关联 |
| 标识符（设备 ID） | 云同步分区 / 推送设备标识 | 与您关联 |
| 诊断与用量 | 崩溃/行为埋点（aia_events） | 不与您关联 |
| 广告数据 | 含广告位（AdBanner），按 CloudBase 下发展示 | 见下 |

## 4. 含广告【工程有 AdBanner → 必须开启】
- App Store Connect → 各语言「广告」开关：**开启**。
- 年龄分级据此相应评估。

## 5. 应用内账户删除【Guideline 5.1.1(v) 已实现，审核时演示】
- 入口：我的账号 → 删除账户（二次确认 + 输入「删除」校验）。
- 行为：云端删除该账号分区全部数据 + 本地全量清空 + 回登录页。
- 订阅提示：弹窗已提示订阅用户先去系统取消订阅。

## 6. 其它提醒
- Bundle ID：`com.daxing.aia.AIA`（Xcode 自动签名会自动注册对应 App ID）。
- 截图：6.9″ 必备 `1290×2796`，参照 `上架准备包.html` 尺寸规范。
- 隐私政策网址、用户协议网址上线前需为可公开访问的真实 URL（建议 CloudBase 默认二级域名或 GitHub Pages，免备案）。
- 开发者联系邮箱：用户协议与隐私政策里的 `[请填写您的联系邮箱]` 占位需替换为真实邮箱。
