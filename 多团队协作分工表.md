# 多团队并行协作分工表

> 维护人：齐活林（software-company 主理人）
> 建立日期：2026-07-22
> 作用：所有并行工作的团队（本团队 software-company、其他专家团队）**开工前必读**；认领/释放模块时在此更新，避免互相静默覆盖。

## 0. 协作铁律（开工前必读）

1. 统一 git 已就绪（整个项目单一仓库，根目录 `.git`），冲突可被检测，但**不会自动避免冲突**。本表指向的"多团队"实际是**你在 WorkBuddy App 里的不同对话（会话）**——各对话独立上下文、操作同一份项目目录、互不知情，同一文件被两个对话各自改写时会静默覆盖。靠本表隔离改动范围。
2. 开工前先读本表 §2/§3/§4，确认目标文件未被其他团队认领或锁定。
3. 认领模块后立即更新「认领团队」与「状态」；释放时改回「待认领」。
4. 共享高危文件（见 §3）改动前，必须在 §4 标注「锁定中」并说明意图，改完解锁。
5. 小步提交；SwiftData Schema 变更（`+1 schemaVersion`）由 §6 指定责任人集中处理，禁止多队各自 +1。
6. 冲突发生时：后提交方负责 rebase / 解决，不得静默覆盖。

## 1. 团队清单

| 团队标识 | 名称 | 负责人 | 当前状态 |
|---------|------|--------|---------|
| 对话A | 本对话（software-company 小A小队）—— 负责 UI 优化、入口/导航、并行协作治理 | 小A | 活跃（当前对话，用户在用） |
| 对话B | software-company 另一对话 —— 负责 UI 优化、底部栏、识别链路 bug 修复 | 小B | 活跃（并行） |
| 对话C | 另一 WorkBuddy 对话 —— 负责导航/设置崩溃排查、并行协作协议执行 | 小C | 活跃（并行） |
| 对话D | 另一 WorkBuddy 对话（UI 设计 / 视觉动效优化方向） | 小D | 活跃（待命） |
| 对话E | 另一 WorkBuddy 对话（开发助手，待命） | 小E | 已登记·待命 |

> 注：此处的"对话"即你在 WorkBuddy 里开的**不同对话窗口**。请给每个对话一个稳定标识（如对话标题），填在"名称"列；认领模块时按对话填，避免两个对话抢同一文件。
> 当前与用户交互的对话 = **对话A**（即本行）。对话B/C/D 为并行活跃的其他对话，其行由各自对话自行维护；如与下方描述冲突，以各对话自身最新提交为准。

## 2. 模块分工矩阵

| 模块 | 主要文件 / 目录 | 认领团队 | 状态 | 备注 |
|------|------|---------|------|------|
| 账单 Bill | `Bill*`、`EditSheets`、`Merchant*` | 待认领 | 空闲 | |
| 饮食 / 食物 Food | `Food*`、`NutritionLibrary`、`RecognizeService`（食物部分） | 待认领 | 空闲 | |
| 健康 Health | `Health*`、`WeightTrend*` | 待认领 | 空闲 | |
| 待办 Todo | `Reminder*`、`Todo*` | 待认领 | 空闲 | |
| 聊天 Chat | `ChatView`、`ChatModels` | 待认领 | 空闲 | |
| 识别引擎 Recognize | `RecognizeService`、`ScreenshotIntent`、LocalOCR | 待认领 | 空闲 | 核心，建议单队负责 |
| 云同步 CloudSync | `CloudSyncManager` + `云函数/` | 待认领 | 空闲 | 含后端 |
| UI 基础 | `AIATheme`、`UIComponents`、`AIAIllustrations`、`EmptyStateView` | 待认领 | 空闲 | 共享高危 |
| 数据模型 Model | `Models`、`AppPersistence`、`AIAMigrationPlan` | 待认领 | 空闲 | 最高危 |
| 入口 / 导航 | `AIAApp`、`ContentView`、`AppDelegate` | 待认领 | 空闲 | |
| 设置 | `SettingsView`、`AutoSyncSettingsView` | 待认领 | 空闲 | |

## 3. 共享高危文件（动前必声明）

| 文件 | 风险 | 当前占用 |
|------|------|---------|
| `AIA/AIA/Models.swift` | 所有模块依赖的数据模型 | 空闲 |
| `AIA/AIA/AppPersistence.swift` | SwiftData 容器 / 迁移 | 空闲 |
| `AIA/AIA/AIAMigrationPlan.swift` | Schema 版本闸门 | 空闲 |
| `AIA/AIA/AIATheme.swift` | 全局主题令牌 | 空闲 |
| `AIA/AIA/UIComponents.swift` | 共用组件 | 空闲 |
| `AIA/AIA/RecognizeService.swift` | 识别主链路，多模块调用 | 空闲 |

## 4. 文件级占用看板（实时）

> 每个对话动手前查此表：要改的文件若已被其他对话占用，停下并告知用户换任务；空闲则写入自己的占用再动手；commit 后清除。
> 格式：`文件 | 占用对话 | 状态 | 预计释放`

| 文件 | 占用对话 | 状态 | 预计释放 |
|------|---------|------|---------|
| `AIA/AIA/ContentView.swift` / `UIComponents.swift` / `WeightTrendView.swift` | （已释放） | 对话A 已于 2026-07-22 commit 挂起改动，不再占用 | — |
| `AIA/AIA/UIComponents.swift` | 对话D（小D） | ✅ 已完成·悬浮胶囊重构（BUILD SUCCEEDED，已 commit） | 已释放 |
| 对话D 登记（待命） | 对话D（小D） | 已登记·待命 | — |
| 对话E 登记（待命） | 对话E（小E） | 已登记·待命 | — |
| `AIA/AIA/UIComponents.swift` | 对话D（小D） | ✅ 已完成·胶囊材质 `ultraThinMaterial`→`regularMaterial`→`thickMaterial`（BUILD SUCCEEDED） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话B（小B）** | ✅ **已完成**·列表页/服务消息/汇总行场景识别优化（commit `87b34a4` + `17e5468`） | 已释放 |
| `AIA/AIA/NutritionLibrary.swift` | 对话C（小C） | ✅ 排查完成，非此文件问题 | 已释放 |
| `AIA/AIA/RecognitionSaver.swift` / `UIComponents.swift` / `ResultConfirmView.swift` / `ContentView.swift` | **对话B（小B）** | ✅ **已完成**·账单改为点击「保存」再入库，不自动记录（commit `add12ac`）| 已释放 |
| `AIA/AIA/ChatView.swift` | 对话C（小C） | ✅ 已完成·已 commit `20affb5`（`saveFromResult` 加零值守卫） | 已释放 |
| `AIA/AIA/ContentView.swift` | 对话C（小C） | ✅ 已完成·撤回 header 同步状态指示器（静默同步，commit `6389d0f`） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话B（小B）** | ✅ **已完成**·加营养成分表回退护栏：检测到营养表但解析失败时跳过账单路径（BUILD SUCCEEDED） | 已释放 |
| ContentView.swift / RecordsViews.swift / WeightTrendView.swift / HealthDetailView.swift / BillCategoryView.swift / AllRecordsView.swift / TodoDetailView.swift / FoodDetailView.swift / BillDetailView.swift | 对话D（小D） | ✅ 已完成·页面底色改 `secondarySystemBackground`（BUILD SUCCEEDED） | 已释放 |
| `AIA/AIA/AppDelegate.swift` | 对话C（小C） | ✅ 已完成·首次同步延迟 0.3s（commit `1df51e5`） | 已释放 |
| `AIA/AIA/ContentView.swift` | 对话C（小C） | ✅ 已完成·header 加同步状态指示器（commit `1df51e5`） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话C（小C）** | ✅ 已完成·多账单识别改走视觉模型（移除本地提前胜出，fall through 到视觉） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话A（小A）** | ✅ 已完成·修复支付消息列表幽灵账单 + 日期时间仍为当前时间（commit `a24fd57`） | 已释放 |
| `AIA/AIA/CloudSyncManager.swift` | **对话A（小A）** | ✅ 已完成·修复 `userId` MainActor 隔离导致 AgentChatRequest 默认参数编译失败（commit `a24fd57`） | 已释放 |
| `AIA/AIA/ContentView.swift` | **对话D（小D）** | ✅ 已完成·4 宫格类型色条彻底去掉（顶条、左侧边两轮对比后用户最终选「完全无装饰条」；BUILD SUCCEEDED，已 commit `ee9ed0a`） | 已释放 |
| `AIA/AIA/UIComponents.swift` | **对话D（小D）** | ✅ 已完成·底部悬浮胶囊左右边距 12→20（与首页「今日事项预览」气泡同宽；BUILD SUCCEEDED，已 commit） | 已释放 |
| `云函数/recognize/index.js` + `原生代码脚手架/cloudfunctions/recognize/index.js` | **对话C（小C）** | ✅ 已完成·视觉 prompt 增加多账单 `bills` 数组输出；FN_VERSION 升 `20260722e-sensenova`；同步两份副本并重新打包 | 已释放 |
| `AIA/AIA/ChatView.swift` | **对话C（小C）** | ✅ 已完成·优化阿宝回复自然度（确认消息随机开场白池去模板化 + 问候只引用未来待办 + 账单商户名清洗修正） | 已释放 |

## 5. 本团队近期已改动文件（供其他团队参考，避免重复劳动）

| 日期 | 文件 | 改动摘要 |
|------|------|---------|
| 2026-07-22 | `DashboardView.swift` | 删除死代码（从未被实例化、硬编码脱离主题） |
| 2026-07-22 | `AIATheme.swift` / `UIComponents.swift` 等 10+ 文件 | 灰色令牌统一收口为 `AIATheme.sub` / `AIATheme.muted`，消除三套灰阶并存 |
| 2026-07-22 | `BillCategoryHelpers` | 账单分类色纳入主题 + 补深色变体 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 底部栏文案轮播改 `.task` 异步循环，修复不切换 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 提示箭头改为由 `iconOrder` 单一真源推导（§3 高危，对话B 已改完并 commit） |
| 2026-07-22 | `RecordsViews.swift` | 饮食/账单/待办三模块底部栏提示改用 `AIPrompt` 结构化，箭头随图标位置自动翻转 |
| 2026-07-22 | 仓库根 `.git` / `AIA/.git` | 合并 `AIA` 入父仓库，统一版本控制（父仓库现跟踪 162 文件） |
| 2026-07-22 | `ContentView`/`UIComponents`/`WeightTrendView` | 对话A 提交挂起改动：新增 PressableCardStyle 按压反馈、RingView/DonutView/MacroCard/MiniBar 描边生长动画、底部栏毛玻璃背景；与对话B 已提交的 AIBottomBar 改动共存，无覆盖 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 对话D 悬浮胶囊重构：4 个独立按钮 → 单体玻璃胶囊（.ultraThinMaterial + 柔阴影 + 左右 12pt 边距），去掉独立圆背景和贴边分割线，仍为 VStack 兄弟不改成 overlay；BUILD SUCCEEDED |
| 2026-07-22 | `RecognitionSaver.swift` / `ChatView.swift`（对话C） | 修复「识别失败未拦截 0 值入库」：在 `autoSave()` 和 `saveFromResult()` 两条路径加全宏量零守卫，拦截空壳食物记录；commit `20affb5` |
| 2026-07-22 | `AppDelegate.swift` / `ContentView.swift`（对话C） | 首页加载优化：首次同步延迟 0.3s（让首页先再渲染）+ 撤回 header 同步状态指示器（静默同步）；commit `1df51e5` + `6389d0f` |
| 2026-07-22 | `RecognizeService.swift` / `云函数/recognize/index.js` / `原生代码脚手架/.../index.js` / `package-recognize.sh`（对话C） | 多账单识别改走视觉模型：客户端移除多账单本地提前胜出（fall through 到 `recognizeResilient`）；云端视觉 prompt 新增规则13 输出 `bills` 数组、FN_VERSION 升 `20260722e-sensenova`、两份 index.js 同步一致并重新打包（zip gitignored 需手动部署） |
| 2026-07-22 | `云函数/recognize/index.js` / `agentTools.js` / `agentSchema.js` / `agentPrompt.js` / `package.json` / `AIA/AIA/AgentChatRequest.swift` / `docs/` / `test/`（对话A） | 云端聊天 Agent 接入（商汤只读 v1）：新增 4 个 function-calling 只读工具（查账单/饮食/健康/汇总），`handleAgent` 循环≤5轮+降级回 context-only；`mode:"agent"` 与 `mode:"chat"` 共存；FN_VERSION 升 `20260722k-agent-v1`；commit `9bfd446`（本轮未单独 commit 分工表，避免混入对话D 在途改动） |
| 2026-07-22 | `AIA/AIA/ChatView.swift`（对话C） | 优化阿宝回复自然度：①新增 `chatConfirmOpeners` 随机开场白池（记好啦/收到～/好嘞/搞定…），6 处确认消息（待办/账单/食物本地+云端/多记录×2）去模板化 + 类型 emoji 轮换；②上下文过滤同步改用 opener 池（`hasPrefix("记好啦")` 旧逻辑会漏过滤随机化后消息）；③底部问候只引用「今天到期+未完成+未来时间」最早待办，无未来待办则跳过，不再展示过期示例；④账单商户名清洗增加时间/饮食动词分组正则，纯餐次词（晚餐等）回退"其他消费" |

## 6. Schema 变更闸门

- SwiftData `@Model` 字段新增 / 变更 → `AIAApp.schemaVersion` +1（见 `AIAMigrationPlan`）。
- 责任人：**待指定**（建议单一团队统一管，避免版本号打架、旧 store 白屏）。
- 流程：改前在 §4 声明 → 责任人审批 / 执行 → 全量 `xcodebuild` 验证。

## 7. 使用流程

1. **开工前**：读本表 §2 / §3 / §4，确认目标文件空闲。
2. **认领**：在 §2 填团队 + 状态 = 进行中；若动 §3 文件，在 §4 加锁定。
3. **完工**：commit 后，§2 状态改「已完成 / 空闲」，§4 解锁。
4. **冲突**：git 标冲突 → 后提交方解决 → 更新本表。

---
*本文件本身纳入 git 版本控制；各团队通过读取最新提交获取最新分工状态。*

## 8. 新对话开局提示（复制给每个新对话）

> 在每个新 WorkBuddy 对话的第一句粘贴下面这段，之后正常下需求即可，**无需懂模块划分**——模块 / 文件判断由对话自己完成。

```
你是这个 AI 助理 iOS 项目的开发助手。当前是多对话并行改同一份代码，请遵守：
1. 每次动手前，先读本项目根的 `多团队协作分工表.md`，重点看 §3（共享高危文件）和 §4（文件级占用看板）。
2. 分析你要改的文件清单；若其中任一文件已在 §4 被其他对话占用，停下并告诉用户“XX 文件正被[对话]占用，请换任务或等其释放”，不要硬改。
3. 若空闲，先把你的占用写进 §4（文件 | 你的对话标识 | 进行中 | 预计释放），再动手。
4. 完工并 commit 后，把 §4 里你的占用清除。
5. 涉及 §3 的共享高危文件，无论是否被占，动手前都在 §4 标注并提醒用户。
```
