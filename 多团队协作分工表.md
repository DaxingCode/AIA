# 多团队并行协作分工表

> 维护人：齐活林（software-company 主理人）
> 建立日期：2026-07-22
> 作用：所有并行工作的团队（本团队 software-company、其他专家团队）**开工前必读**；认领/释放模块时在此更新，避免互相静默覆盖。

## 0. 协作铁律（开工前必读）

1. 统一 git 已就绪（整个项目单一仓库，根目录 `.git`），冲突可被检测，但**不会自动避免冲突**。本表指向的"多团队"实际是**你在 WorkBuddy App 里的不同对话（会话）**——各对话独立上下文、操作同一份项目目录、互不知情，同一文件被两个对话各自改写时会静默覆盖。靠本表隔离改动范围。
2. 开工前先读本表 §2/§3/§4，确认目标文件未被其他团队认领或锁定。
3. 认领模块后立即更新「认领团队」与「状态」；释放时改回「待认领」。
4. 共享高危文件（见 §3）改动前，必须在 §4 标注「锁定中」并说明意图，改完解锁。
5. 小步提交；SwiftData Schema 变更（`+1 schemaVersion`）由 §6 指定责任人集中处理，禁止多队各自 +1。
6. 冲突发生时：后提交方负责 rebase / 解决，不得静默覆盖。

## 1. 团队清单

| 团队标识 | 名称 | 负责人 | 当前状态 |
|---------|------|--------|---------|
| 对话A | 本对话（software-company 小A小队）—— 负责 UI 优化、入口/导航、并行协作治理 | 小A | 活跃（当前对话，用户在用） |
| 对话B | software-company 另一对话 —— 负责 UI 优化、底部栏、识别链路 bug 修复 | 小B | 活跃（并行） |
| 对话C | 另一 WorkBuddy 对话 —— 负责导航/设置崩溃排查、并行协作协议执行 | 小C | 活跃（并行） |
| 对话D | 另一 WorkBuddy 对话（UI 设计 / 视觉动效优化方向） | 小D | 活跃（待命） |
| 对话E | 另一 WorkBuddy 对话（开发助手，待命） | 小E | 已登记·待命 |
| 对话F | 另一 WorkBuddy 对话（开发助手，待命） | 小F | 已登记·待命 |
| 问答 | 另一 WorkBuddy 对话（开发助手 / 问答答疑方向） | 问答 | 已登记·待命 |

> 注：此处的"对话"即你在 WorkBuddy 里开的**不同对话窗口**。请给每个对话一个稳定标识（如对话标题），填在"名称"列；认领模块时按对话填，避免两个对话抢同一文件。
> 当前与用户交互的对话 = **对话A**（即本行）。对话B/C/D 为并行活跃的其他对话，其行由各自对话自行维护；如与下方描述冲突，以各对话自身最新提交为准。

## 2. 模块分工矩阵

| 模块 | 主要文件 / 目录 | 认领团队 | 状态 | 备注 |
|------|------|---------|------|------|
| 账单 Bill | `Bill*`、`EditSheets`、`Merchant*` | 待认领 | 空闲 | |
| 饮食 / 食物 Food | `Food*`、`NutritionLibrary`、`RecognizeService`（食物部分） | 待认领 | 空闲 | |
| 健康 Health | `Health*`、`WeightTrend*` | 待认领 | 空闲 | |
| 待办 Todo | `Reminder*`、`Todo*` | 待认领 | 空闲 | |
| 聊天 Chat | `ChatView`、`ChatModels` | 对话E（小E） | 进行中 | 优化「你是谁/在干嘛」等通用意图回复 |
| 识别引擎 Recognize | `RecognizeService`、`ScreenshotIntent`、LocalOCR | 待认领 | 空闲 | 核心，建议单队负责 |
| 云同步 CloudSync | `CloudSyncManager` + `云函数/` | 待认领 | 空闲 | 含后端 |
| UI 基础 | `AIATheme`、`UIComponents`、`AIAIllustrations`、`EmptyStateView` | 待认领 | 空闲 | 共享高危 |
| 数据模型 Model | `Models`、`AppPersistence`、`AIAMigrationPlan` | 待认领 | 空闲 | 最高危 |
| 入口 / 导航 | `AIAApp`、`ContentView`、`AppDelegate` | 待认领 | 空闲 | |
| 设置 | `SettingsView`、`AutoSyncSettingsView` | 待认领 | 空闲 | |

## 3. 共享高危文件（动前必声明）

| 文件 | 风险 | 当前占用 |
|------|------|---------|
| `AIA/AIA/Models.swift` | 所有模块依赖的数据模型 | 空闲 |
| `AIA/AIA/AppPersistence.swift` | SwiftData 容器 / 迁移 | 空闲 |
| `AIA/AIA/AIAMigrationPlan.swift` | Schema 版本闸门 | 空闲 |
| `AIA/AIA/AIATheme.swift` | 全局主题令牌 | 空闲 |
| `AIA/AIA/UIComponents.swift` | 共用组件 | 空闲 |

| 2026-07-24 | `AIA/AIA/UIComponents.swift`（对话D） | ✅ 已完成·识别中浮层中央内容**水平+垂直**强制居中（iPhone 15 Pro Max 真机 fix-2）：前版 `VStack.frame(maxWidth:.infinity, alignment:.center) + .padding(.horizontal, 32)` 仍偏差 → 改用**`.position(x: W/2, y: H/2)` 绝对锚定**中央 VStack（与返回按钮同款技术，commit `05a1636`）；删 frame/padding 干扰项；ZStack 的 alignment 行为不确定（GeometryReader + fullScreenCover + Dynamic Island 嵌套下）→ 改走几何锚定，100% 居中；BUILD SUCCEEDED，commit `c58ece7`（+6/-7，1 文件） |
| `AIA/AIA/RecognizeService.swift` | 识别主链路，多模块调用 | 空闲 |
| `AIA/AIA/Models.swift` / `AppPersistence.swift` / `AIAMigrationPlan.swift` | **🔒 对话D（小D）** · **FoodNote 新模型 + schema v10→v11 轻量迁移**（编辑食物页加备注栏+图片附件+删除按钮所需）· ✅ 已完成并 commit `0dcfa09`（BUILD SUCCEEDED） | 已释放 |

## 4. 文件级占用看板（实时）

> 每个对话动手前查此表：要改的文件若已被其他对话占用，停下并告知用户换任务；空闲则写入自己的占用再动手；commit 后清除。
> 格式：`文件 | 占用对话 | 状态 | 预计释放`

| 文件 | 占用对话 | 状态 | 预计释放 |
|------|---------|------|---------|
| `AIA/AIA/ContentView.swift` / `UIComponents.swift` / `WeightTrendView.swift` | （已释放） | 对话A 已于 2026-07-22 commit 挂起改动，不再占用 | — |
| `AIA/AIA/UIComponents.swift` | 对话D（小D） | ✅ 已完成·悬浮胶囊重构（BUILD SUCCEEDED，已 commit） | 已释放 |
| 对话D 登记（待命） | 对话D（小D） | 🔒 进行中·**修 crash 二次失败**（commit a427a2f 治标不治本）。**真根因**：`EditTodoView` 内部**嵌套了 NavigationStack**（行 1025），与父级 ContentView 的 `NavigationStack(path:)` 嵌套 → path 类型 `[HomeRoute]` vs `NavigationPath` 不匹配 → AnyNavigationPath 内部 try! comparisonTypeMismatch。**调用点**有 2 个：① ContentView.swift:121 navigationDestination 推 `.editTodo(r)` 已经在父级 NavigationStack 内；② AllRecordsView.swift:163 `.sheet(item: $editTodo)` 弹 sheet，sheet 自身是独立 NavigationContext 可包 NavigationStack。**修法**：拆出 `EditTodoSheet` wrapper（包 NavigationStack），`EditTodoView` 自身去掉 NavigationStack；`ContentView` 调 `EditTodoView`（父级内 push），`AllRecordsView` 改调 `EditTodoSheet`（sheet 包装） | 8 分钟内 |
| `AIA/AIA/Models.swift` + `AIA/AIA/AIAMigrationPlan.swift` + `AIA/AIA/EditSheets.swift` + `AIA/AIA/RecordsViews.swift` + `AIA/AIA/TodoDetailView.swift` | **对话D（小D）** | ✅ 已完成·待办「点行直达编辑页 + 编辑页加删除按钮 + 加备注」+v12 schema 加 ReminderNote 表+BUILD SUCCEEDED+commit `3d58f88`（+157/-260，6 文件：5 改 + 1 删 TodoDetailView.swift） | 已释放 |
| 对话E 登记（待命） | 对话E（小E） | 已登记·待命 | — |
| 对话F 登记（待命） | 对话F（小F） | 已登记·待命 | — |
| 问答 登记（待命） | 问答 | 已登记·待命 | — |
| `AIA/AIA/ChatView.swift` / `AIA/AIA/ChatModels.swift` | 对话E（小E） | ✅ 已完成·通用闲聊/元意图本地回复优化（你是谁/在干嘛/你好/谢谢/再见等）；改动已合入 `377fc62` | 已释放 |
| `AIA/AIA/UIComponents.swift` | 对话D（小D） | ✅ 已完成·胶囊材质 `ultraThinMaterial`→`regularMaterial`→`thickMaterial`（BUILD SUCCEEDED） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话B（小B）** | ✅ **已完成**·列表页/服务消息/汇总行场景识别优化（commit `87b34a4` + `17e5468`） | 已释放 |
| `AIA/AIA/NutritionLibrary.swift` | 对话C（小C） | ✅ 排查完成，非此文件问题 | 已释放 |
| `AIA/AIA/RecognitionSaver.swift` / `UIComponents.swift` / `ResultConfirmView.swift` / `ContentView.swift` | **对话B（小B）** | ✅ **已完成**·账单改为点击「保存」再入库，不自动记录（commit `add12ac`）| 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话D（小D） | ✅ 已完成·确认识别结果页「重量/热量」两模块单行化：新增 `inlineFieldRow` 私有方法（icon+标题 在左 / 输入控件+单位 在右），重量+热量两 cell 切换到 inlineFieldRow；其他 8 处 fieldRow（餐次/名称/商户/金额/分类/事项/指标/数值）保持原样；BUILD SUCCEEDED，commit `9389251`（+35/-20） | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话D（小D） | ✅ 已完成·结果确认页 6 大营养素 cell 居中化：macroCell 633-662 行 6 处对齐调整（VStack + 外层 frame + 标题 HStack + 2 个 Text），与 EditFoodView.nutritionCell 居中版视觉一致；BUILD SUCCEEDED，commit `68d9369`（+7/-2） | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | **对话D（小D）** | ✅ 已完成·结果确认页「识别原图」默认展示图片**上半部分**（banner crop）：`imageSection` 行 716-721 的 `Image.resizable().scaledToFill().frame(height: 180)` 居中裁剪 → 改为 `frame(height: 360).frame(height: 180).clipped()`，图片实际渲染 360pt，容器只露顶部 180pt（账单/待办统一改，单点改动，不动 `FullImageView` 全屏查看）；BUILD SUCCEEDED，commit `b457471`（+17/-6，1 文件） | 已释放 |
| `AIA/AIA/zh-Hans.lproj/Localizable.strings` + `en.lproj/Localizable.strings` + `zh-HK.lproj/Localizable.strings` + `zh-Hant.lproj/Localizable.strings` | **对话D（小D）** | 🔒 进行中·补 2 个漏注册 localization key：① `food.waterUnit`（RecordsViews.swift:258 单位）→ 4 语都「ml」；② `food.waterTapHint`（RecordsViews.swift:279 点击 +100ml 提示）→ zh-Hans「点击 +100ml」/zh-HK「點擊 +100ml」/zh-Hant「點擊 +100ml」/en「Tap +100ml」。紧跟 91 行 `food.water` 后插入。RecordsViews.swift 不动（key 引用对，只 strings 没注册）。全项目 food.* 共 22 个引用，本次确认仅这 2 个漏配 | 3 分钟内 |
| `AIA/AIA/AddFoodManualView.swift` | 对话D（小D） | ✅ 已完成·手动添加食物页「餐次」按当前系统时间自动选择：原 line 31 `@State meal: String = "午餐"` 写死午餐 → 改 `@State meal: String = RecognitionSaver.defaultMeal(for: .now)`，复用项目统一工具方法（5-11 早餐 / 11-16 午餐 / 16-22 晚餐 / 其他 加餐；同 ChatView/RecordsViews/ResultConfirmView 已用 5+ 处）。逻辑跟「饮食记录」页面 4 餐次 tab 完全一致。BUILD SUCCEEDED，commit `baefda6`（+1/-1，1 文件） | 已释放 |

| `AIA/AIA/RecordsViews.swift` | **对话D（小D）** | ✅ 已完成·**「在日历查看」tab 选中日期账单行间无分隔**修复：根因——`billCalendarView` line 1784 用 `ForEach(dayBills) { b in ... }`，**完全没加行间 hairline**，前几轮只改了「全部」tab 的 `groupedByDate` + 日期组之间，漏了这个独立 ForEach。修法：line 1784 改 `ForEach(Array(dayBills.enumerated()), id: \.element.persistentModelID)` + 行间插 `Rectangle hairline 0.7pt + padding(.leading, 62)`（让位 icon+padding 62pt，与「全部」tab 行间分隔 100% 对齐）。颜色 + 宽度自动继承 fix-3 标准（dark `0x636366` + 0.7pt），不动 UIComponents.swift。BUILD SUCCEEDED，commit `e8eeea8`（+9/-1，1 文件） | 已释放 |
| `AIA/AIA/UIComponents.swift` + `AIA/AIA/RecordsViews.swift` | **对话D（小D）** | ✅ 已完成·**深色模式行间分隔再增强（fix-3）**：根因——前两版（0.5pt 细线 + 0x545458 中等亮色）只解决了颜色对比，0.5pt 物理宽度本身视觉权重太弱，在 `surface 0x1c1c1e` 卡片底色上仍「细到几乎看不见」——用户截图反馈深色模式仍不明显。修法：① `UIComponents.swift:99` `hairline` dark `0x545458` → `0x636366`（系统深色 fill 3，对比度再强一档；light 保持 `0xe6e9ec` 不动）；② `RecordsViews.swift:1360/1411` 两处 `frame(height: 0.5)` → `frame(height: 0.7)`（视觉权重 +40%）。MonthlyBillListView 不动（其他对话多选删除功能暂存，绕开）。BUILD SUCCEEDED，commit `3136c4f`（+7/-5，2 文件） | 已释放 |
| `AIA/AIA/EditSheets.swift` + `AIA/AIA/RecordsViews.swift` | 对话D（小D） | ✅ 已完成·账单管理页右上角加「+」号 → 手动添加账单：① EditBillView 加 `let isAdding: Bool` 字段 + init `isAdding: Bool = false` 兼容参数（不破坏现有 3 个调用方：BillDetailView:35 / AllRecordsView:162 / RecordsViews:1326）；② EditBillView 改动：VStack `deleteCard` 行包 `if !isAdding` 条件；navigationTitle 改 `isAdding ? "添加账单" : "编辑账单"`；save() 添加模式 `bill.syncDeleted = false` 让草稿复活；③ BillListView 加 `@State addBillDraft` + `toolbar.topBarTrailing` 加号按钮 + `.sheet(item: $addBillDraft)` + addNewBill 函数（创建临时 Bill `syncDeleted=true` 软删除 → @Query 谓词过滤 → sheet 期间背景干净）；④ 交互闭环：保存 = syncDeleted→false 复活显示；取消/swipe = 临时 Bill 软删除保持过滤，零脏数据。BUILD SUCCEEDED，commit `ff43d92`（+41/-4，2 文件） | 已释放 |
| `AIA/AIA/ChatView.swift` | 对话C（小C） | ✅ 已完成·已 commit `20affb5`（`saveFromResult` 加零值守卫） | 已释放 |
| `AIA/AIA/ContentView.swift` | 对话C（小C） | ✅ 已完成·撤回 header 同步状态指示器（静默同步，commit `6389d0f`） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话B（小B）** | ✅ **已完成**·加营养成分表回退护栏：检测到营养表但解析失败时跳过账单路径（BUILD SUCCEEDED） | 已释放 |
| ContentView.swift / RecordsViews.swift / WeightTrendView.swift / HealthDetailView.swift / BillCategoryView.swift / AllRecordsView.swift / TodoDetailView.swift / FoodDetailView.swift / BillDetailView.swift | 对话D（小D） | ✅ 已完成·页面底色改 `secondarySystemBackground`（BUILD SUCCEEDED） | 已释放 |
| `AIA/AIA/AppDelegate.swift` | 对话C（小C） | ✅ 已完成·首次同步延迟 0.3s（commit `1df51e5`） | 已释放 |
| `AIA/AIA/ContentView.swift` | 对话C（小C） | ✅ 已完成·header 加同步状态指示器（commit `1df51e5`） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话C（小C）** | ✅ 已完成·多账单识别改走视觉模型（移除本地提前胜出，fall through 到视觉） | 已释放 |
| `AIA/AIA/RecognizeService.swift` | **对话A（小A）** | ✅ 已完成·修复支付消息列表幽灵账单 + 日期时间仍为当前时间（commit `a24fd57`） | 已释放 |
| `AIA/AIA/CloudSyncManager.swift` | **对话A（小A）** | ✅ 已完成·修复 `userId` MainActor 隔离导致 AgentChatRequest 默认参数编译失败（commit `a24fd57`） | 已释放 |
| `AIA/AIA/ContentView.swift` | **对话D（小D）** | ✅ 已完成·饮食模块：饮食记录页顶部加 饮食记录/饮食喜好/饮食分析 分段切换，toolbar 改条件渲染（仅记录 tab 显示日历）；ContentView 路由不变（实际直接在 FoodListView 内部加分段，零路由改动；BUILD SUCCEEDED，已 commit `0d25096`） | 已释放 |
| `AIA/AIA/RecordsViews.swift` | **对话D（小D）** | ✅ 已完成·所有染色卡（dietBG/healthBG/billBG）改为 surface 白底 + hairline 0.5pt 描边；2 个 hero 同步改白底+描边；文字语义色不变（食物琥珀/健康紫/账单绿）；BUILD SUCCEEDED，已 commit `15f91a2`（+42/-24） | 已释放 |
| `AIA/AIA/UIComponents.swift` | **对话D（小D）** | ✅ 已完成·识别中浮层 `RecognizingOverlay`「返回」按钮上移到屏幕**底部 1/3** 位置：原 VStack { Spacer(); Button; .padding(.bottom,24) } 贴底 → 改用 `GeometryReader` + `Button.position(x: W/2, y: H*2/3)` 绝对锚定到距顶 2/3（距底 1/3 屏高），跨设备自适应；BUILD SUCCEEDED，commit `05a1636`（+31/-32，1 文件） | 已释放 |
| `AIA/AIA/RecordsViews.swift` + 4 个 Localizable.strings | **对话D（小D）** | ✅ 已完成·饮食记录页热量卡从 2 列改为 3 等宽列（净热量/TDEE/今日消耗），用 hairline 0.5pt 细竖线分隔；净热量保留食物色、TDEE/今日消耗 ink 色中性；新增 `food.tdeeLabel` 本地化 key；移除原「摄入 X · TDEE Y」+「低于目标」行（3 数字自明）；COMMIT `db7bdd1`（+56/-10）；RecordsViews 自身编译干净，RecognizeService 的 `fiber/sugar/sodium` 缺失是独立问题 | 已释放 |
| `云函数/recognize/index.js` + `原生代码脚手架/cloudfunctions/recognize/index.js` | **对话C（小C）** | ✅ 已完成·视觉 prompt 增加多账单 `bills` 数组输出；FN_VERSION 升 `20260722e-sensenova`；同步两份副本并重新打包 | 已释放 |
| `AIA/AIA/ChatView.swift` | **对话C（小C）** | ✅ 已完成·优化阿宝回复自然度（确认消息随机开场白池去模板化 + 问候只引用未来待办 + 账单商户名清洗修正） | 已释放 |
| `AIA/AIA/ChatView.swift` | 对话C（小C） | ✅ 已完成·文本路由修复：食物+金额同时出现时拆分饮食+账单两条记录（commit `377fc62`+`5e8407e`） | 已释放 |
| `AIA/AIA/ContentView.swift` | **对话D（小D）** | ✅ 已完成·问候语 emoji 移到句尾 + 阿宝头像换为 app icon（`Image("AIAvatar")` 圆形裁剪 + 36pt 透明背景，用 catalog 已有 asset；保留呼吸脉冲动画 2.5s）；BUILD SUCCEEDED，已 commit `3311237`（+8/-14） | 已释放 |
| `AIA/AIA/ChatView.swift` / `AIA/AIA/NutritionLibrary.swift` | 对话C（小C） | ✅ 已完成·修复 1500大卡误建食物 + 奶茶热量异常（commit `d1c9259`）+ 食物名「汉堡花」残留修复（花费动词「花」后接金额整体删除，占位符保护爆米花/花菜等含「花」食物，commit `885c515`） | 已释放 |
| `AIA/AIA/NutritionLibrary.swift` / `云函数/recognize/index.js` | **对话C（小C）** | ✅ 已完成·膳食纤维/糖/钠三项一直 0 修复：① NutritionLibrary 101 行从 5 元组扩到 8 元组（kcal/protein/carbs/fat/fiber/sugar/sodium），按训练知识补全三类数据；② 云端 IMAGE prompt Rule 6 加固要求返回 fiber/sugar/sodium，未标注时按常见食物典型值估算，**绝不默认 0**（commit `0eb7591`+`3cd6a1a`，FN_VERSION 升至 `20260723c-multifood`） | 已释放 |
| `AIA/AIA/RecognizeService.swift` / `AIA/AIA/SettingsView.swift`（云端零改） | **对话A（小A）** | ✅ 已完成·新增模型切换开关：商汤/通义千问/GLM/千帆 四选一，前端 Picker + provider 动态透传（commit `dc6c215`）；云端 PROVIDERS 已含四家视觉+文本、零改动无需重部署 | 已释放 |
| `AIA/AIA/NotificationsDelegate.swift` | 对话C（小C） | ✅ 已完成·用户选择「允许」后拉取通知权限 => 过滤掉「设置界面顶部小白条UIBug」；BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/ChatView.swift`（云端零改） | **对话A（小A）** | ✅ 已完成·聊天入口食物+金额场景自动联网查营养并发送确认卡片，用户确认后写入饮食；缺营养则仅保留账单（commit `d5d0f97`） | 已释放 |
| `AIA/AIA/RecognitionTypes.swift` / `云函数/recognize/index.js` | **对话C（小C）** | ✅ 已完成·支付宝账单相对日期（昨天/星期几）倒推为绝对日期（commit `cf48117`）；客户端 parseRelativeDateTime 支持昨天/前天/大前天/星期X/纯时间→绝对 Date；云函数 prompt 加固要求支付宝相对日期必须转 ISO8601 | 已释放 |
| `云函数/recognize/index.js` / `原生代码脚手架/cloudfunctions/recognize/index.js` | **对话A（小A）** | ✅ 已完成·BugFix：云端 prompt 新增 Rule 14——通知/培训/会议/活动截图（有时间地点、无金额）→ todo 而非 bill。客户端零改动。FN_VERSION: `20260722m-notice-todo` | 已释放 |
| `AIA/AIA/AddFoodManualView.swift` | **对话A（小A）** | ✅ 已修改·保存按钮从底部移到右上角 toolbar（`ToolbarItem.topBarTrailing` 放「保存」，联动 `isValid` 启用/置灰；`fontWeight(.semibold)`）；底部 `saveButton` 视图已删除；BUILD SUCCEEDED | 未提交（等用户决定收口时机） |
| `AIA/AIA/Models.swift` / `AppPersistence.swift` / `AIAMigrationPlan.swift` / `CloudSyncManager.swift` / `RecordsViews.swift` / 4 个 `Localizable.strings` | **对话A（小A）** | ✅ 已完成·**今日饮水 tap +100ml + 震动**：新增 `WaterLog` SwiftData 模型 + schema v9→v10 lightweight 迁移 + CloudSync 同步 push/pull/cleanup + waterCard 改为 Button（+100ml 提示 + UIImpactFeedbackGenerator 触觉反馈 + `WaterCardButtonStyle` 按压 scale 0.94→1.0 spring 回弹 + `.contentTransition(.numericText())` 数字动画）；DietAnalysisView 同步把手动饮水计入平均每日统计；4 个 Localizable 加 `food.waterTapHint`/`food.waterUnit` 翻译；**BUILD SUCCEEDED，零 error 零 warning**（changed files lint 0） | 已释放 |
| `AIA/AIA/EditSheets.swift` + `AIA/AIA/RecordsViews.swift` + `AIA/AIA/ContentView.swift` | **对话D（小D）** | ✅ 已完成·**「编辑待办」从 navigationDestination 推页 → .sheet 弹起**（与「编辑账单」一致）：① ContentView 删 `HomeRoute.editTodo` case + navigationDestination 分支；② RecordsViews.ReminderListView 加 `@State editTodo` + 改 2 处 ValueSelectableCard → ZStack 分层（底层整张卡 onTapGesture 触发 sheet / 顶层 todoDoneButton 36×36 独立 hit area 拦截） + body 加 `.sheet(item: $editTodo) { EditTodoSheet(reminder:) }`；③ EditSheets toolbar 重新加左「取消」（sheet 模式需要 dismiss 关闭 sheet）+ EditTodoView/EditTodoSheet 注释更新为「单 sheet 调用点」。**UX 统一**：编辑待办 = 编辑账单 = 编辑食物 = sheet 弹起。BUILD SUCCEEDED，commit `647119d`（+45/-26，3 文件） | 已释放 |
| `AIA/AIA/EditSheets.swift` | **对话D（小D）** | ✅ 已完成·**「编辑待办」时间与提醒改版**：① `timeAlertCard` 段一 toggle 文字「设置截止时间」→「设置提醒时间」；② 段二 section header「提醒通知」→「提醒时间（最多可设置4次提醒）」；③ 「添加提醒时间」按钮默认 option `.before1Hour`→`.atTime`（准时，符合直觉）。**语义升级**：截止 + 通知 → 「在某天某时提醒我」+「最多 4 次准时/提前提醒」。toggle 仍控制「是否启用提醒」+ 基准时间；段二 4 条 option 全部相对这个基准时间算偏移（准时/15分钟/30分钟/1小时/1天/1周/自定义）。timeAlertCard/dueSection/alertSection 注释同步更新。BUILD SUCCEEDED，commit `293735b`（+7/-7，单文件） | 已释放 |
| `AIA/AIA/EditSheets.swift` | **对话D（小D）** | ✅ 已完成·**「添加提醒时间」按钮文字尾部加「（最多4次）」**（按用户截图标注；与 section header「提醒时间（最多可设置4次提醒）」信息同源，按钮处冗余声明约束）。BUILD SUCCEEDED，commit `0925ab8`（+1/-1，单文件） | 已释放 |
| `AIA/AIA/RecordsViews.swift` | 对话F（小F） | ✅ 已完成·账单管理页点击账单记录直接弹出「编辑账单」页（EditBillView，sheet）；替换原 BillDetailView 跳转（两处：账单详情列表 + 日历选中日列表）；改动已合入 HEAD | 已释放 |
| `AIA/AIA/RecordsViews.swift` | 对话F（小F） | ✅ 已完成·账单管理页点击账单记录统一弹出「编辑账单」页（不再区分有图无图；替换原 NavigationLink→BillDetailView 分流逻辑，改为 Button→editBill sheet；改动两处：账单详情列表 + 日历选中日列表）；commit `6ea5e8f` | 已释放 |
| `AIA/AIA/RecordsViews.swift` | 对话F（小F） | ✅ 已完成·账单点击分流：有图（`imageName` 非空且非空白）→ `BillDetailView` 详情页（详情页内再编辑）；无图 → 直达 `EditBillView` 编辑页（两处：账单详情列表 + 日历选中日列表）；改动已合入本对话 commit | 已释放 |
| `AIA/AIA/EditSheets.swift` + `AIA/AIA/RecordsViews.swift` | **对话D（小D）** | ✅ 已完成·**「待办提醒」页右上角加「+」→ 手动新增待办**：① EditTodoView 加 `let isAdding: Bool = false`（仿 EditBillView：`navigationTitle` `isAdding ? "添加待办" : "编辑待办"` + body VStack `if !isAdding { deleteCard }` 隐藏删除按钮 + save() 末尾 `if isAdding { reminder.syncDeleted = false }` 草稿复活）；② EditTodoSheet 加 `let isAdding` + 显式 init（Swift struct 自动 init 不支持可选默认参数，必须显式 init）；③ RecordsViews.ReminderListView 加 `@State addTodoDraft: Reminder?` + `addNewTodo()` 创建草稿 Reminder（title 空/syncDeleted=true）→ `addTodoDraft = draft` + `.toolbar { ToolbarItem(.topBarTrailing) { Image(systemName:"plus") } }`（与 BillListView 蓝「+」同款）+ body `.sheet(item: $addTodoDraft) { EditTodoSheet(reminder: r, isAdding: true) }`；④ 拆 `todoRow(r)` helper（ZStack 分层抽出去）—— 加 toolbar + 双 sheet 后 body modifier 链总深度让 Swift 编译器类型推断超时（`the compiler is unable to type-check this expression in reasonable time`），拆 helper 后 BUILD SUCCEEDED。**UX 统一**：手动新增 = 手动新增账单 = sheet 弹起 + 蓝「+」右上角。BUILD SUCCEEDED，commit `356dd10`（+68/-17，2 文件） | 已释放 |
| `AIA/AIA/ChatView.swift` | 对话D（小D） | ✅ 已完成·聊天「待确认饮食记录」卡片升级为 B 方案食物信息卡（hero 热量 + 6 列营养：碳水/蛋白/脂肪/纤维/糖/钠），底色 dietBG→surface 白底；BUILD SUCCEEDED，已 commit `e527583` | 已释放 |
| `AIA/AIA/RecordsViews.swift` | 对话D（小D） | ✅ 已完成·饮食记录列表行升级为 B 方案食物信息卡（hero 热量 + 6 列营养：碳水/蛋白/脂肪/纤维/糖/钠），复用 macroCell/formatValue 与聊天卡片视觉对齐；BUILD SUCCEEDED，已 commit `370f65c` | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话D（小D） | ✅ 已完成·识别结果确认页食物卡片补全 6 项营养（蛋白/碳水/脂肪/纤维/糖/钠）：EditableFood 增 3 字段；4 条预填路径（fillFromResult/NutritionLibrary/FoodMetaStore/resolveFoodNutrition）全部透传 fiber/sugar/sodium；卡片营养明细从 3 行扩展为 6 行（颜色：蛋白蓝/碳水琥珀/脂肪绿/纤维紫/糖警告红/钠深琥珀 mg）；applyAndSave 单食物覆盖 fiber/sugar/sodium + baseFiber/baseSugar/baseSodium，多食物每条 FoodEntry 都带入；correctionPayloads 纠错回流数据补 fiber/sugar/sodium；BUILD SUCCEEDED，已 commit `2f5300d`（+269/-134） | 已释放 |
| `AIA/AIA/FoodDetailView.swift` / `RecognitionRecordDetailView.swift` / `TodoDetailView.swift` | 对话D（小D） | ✅ 已完成·食物详情页优化 + 「识别原图」重复标题修复：① 热量卡去掉「识别置信度 —」占位文字，右列改「份量/餐次·时间」展示真实数据；② 营养明细 3×2 MacroCard 网格 → 6 行 macroRow 列表（与聊天/列表/结果确认页风格完全一致；数值 1 位小数不再被截 0；配色统一 6 色），新增 macroRow/formatValue 私有方法；③ 修复「识别原图」标题重复 bug（AttachmentSection 默认 title 与外层 SectionTitle 撞）：FoodDetailView / RecognitionRecordDetailView / TodoDetailView 三处统一传 title:nil（BillDetailView 原本就传 nil 不动）；BUILD SUCCEEDED，已 commit `812be24`（+59/-14） | 已释放 |
| `AIA/AIA/EditSheets.swift` | **对话D（小D）** | ✅ 已完成·**「添加待办」due 默认时间 = 当前时间 1 小时后**：按用户截图标注。改 init `_due = State(initialValue: reminder.due ?? Date())` → `_due = State(initialValue: reminder.due ?? Date().addingTimeInterval(3600))`。**精准最小改动**：仅新增场景（reminder.due == nil）走 Date() 改 1 小时后；编辑现有待办（reminder.due 有值）保留原值不动。hasDue 开关状态保持 reminder.due != nil 决定（不强行默认开启，避免打断编辑流程）。BUILD SUCCEEDED，commit `6d1c9f4`（+4/-1） | 已释放 |
| `AIA/AIA/EditSheets.swift` / `AddFoodManualView.swift` | 对话D（小D） | ✅ 已完成·营养成分模块 2×3 网格化：热量上移至标题栏「🔥 X kcal」pill；6 大营养素（蛋白质/碳水/脂肪/膳食纤维/糖/钠）改为 2×3 LazyVGrid（每格 icon+名称+输入框+单位，6 色调色板）；EditFoodView 加 displayedKcalText；新增 nutritionCell 私有方法；BUILD SUCCEEDED，commit `848a730`（+598/-25，AddFoodManualView 首次入仓） | 已释放 |
| `AIA/AIA/EditSheets.swift` / `AddFoodManualView.swift` | 对话D（小D） | ✅ 已完成·营养成分 cell 内容居中化：VStack(alignment: .leading) → .center；两 HStack 加 .frame(maxWidth: .infinity, alignment: .center)；TextField 改 .multilineTextAlignment(.center) 替换 .trailing；AddFoodManualView 移除 TextField 多余的 .frame(maxWidth: .infinity)；cell 最外层 frame 改 .center。BUILD SUCCEEDED，commit `cc948db`（+10/-8，2 文件） | 已释放 |
| `AIA/AIA/UIComponents.swift` / `ChatView.swift` | 对话D（小D） | ✅ 已完成·识别中页面升级：① `CameraCoverItem.recognizing` 加 `UIImage` 关联值（id 用图片 hash 防止 fullScreenCover 冲突）；② 新增 `RecognizingOverlay` 私有 view：背景 UIImage 撑满 + blur(25) 模糊 + 白遮罩 + 中央 spinner + 双行文字（阿宝AI正在识别中 / 账单、食物、通知都能识别哦）+ 底部「返回」按钮（ultraThinMaterial 胶囊）；③ runImageRecognition 加 nil 守卫（用户取消后丢弃结果，避免覆盖已关闭状态）。BUILD SUCCEEDED，commit `1e52b6a`（+73/-20，2 文件） | 已释放 |
| `AIA/AIA/RecordsViews.swift` | 对话D（小D） | ✅ 已完成·饮食记录 tab 点行直接弹出 EditFoodView（去掉 FoodDetailView 中间层）：行点击 `SelectableCard→Button`（`.buttonStyle(.plain)`）触发 `editFood = f`；末尾加 `.sheet(item: $editFood) { EditFoodView(entry:) }`；与 AllRecordsView 路径保持一致；FoodDetailView 保留以备其他入口（搜索/7日记录/食物喜好）；BUILD SUCCEEDED，commit `3da2319`（+13/-5） | 已释放 |
| `AIA/AIA/EditSheets.swift` | 对话F（小F） | ✅ 已完成·编辑账单页优化：新增删除按钮（确认弹窗）；备注区改用多行 TextEditor；有图时展示小图缩略（点击查看大图、X 移除）；支持拍照/相册添加图片；保存时回写 imageName；改动已合入本对话 commit | 已释放 |
| `AIA/AIA/EditSheets.swift` | 对话F（小F） | ✅ 已完成·备注栏压缩：外层 VStack spacing 12→8、TextEditor minHeight 60→44 降低整体高度、「备注」标签与图文区间距缩小（commit `26a1888`） | 已释放 |
| `AIA/AIA/EditSheets.swift` | 对话F（小F） | ✅ 已完成·编辑账单页「收入（非支出）」开关卡与「备注」卡位置互换：收入卡移到备注卡之上（commit `494a1ed`） | 已释放 |
| `AIA/AIA/Models.swift` | 对话F（小F） | ✅ 已完成·修复编译错误：`FoodNote.updatedAt` 默认值写在属性声明（`var updatedAt: Date = Date.now`）触发 `@Model` 宏 `Type 'Any?' has no member 'now'`（此 bug 由对话D commit `0dcfa09` 引入——其「`.now`→`Date.now`」改法无效，声明上的任意静态成员默认值都会触发）；修复=与全文件其他 8 个模型一致，默认值移到自定义 init 参数 `updatedAt: Date = .now`；**属性名/类型未变，不改 schema、不 +1 schemaVersion**；中间误 revert（a5ca2e5）已 `reset --hard` 回 44c6d64 恢复（commit `44c6d64`） | 已释放 |
| `AIA/AIA/BillCategoryHelpers.swift` / `AIA/AIA/ChatView.swift` | 对话F（小F） | ✅ 已完成·餐饮分类 emoji 统一从 🍔 改为 😋：① `BillCategoryHelpers.icon(for:)` 餐饮分支 return "😋"；② `ChatView.swift` 三处食物装饰随机 icon 数组里的 "🍔" 同步替换为 "😋"；commit `b648f49` | 已释放 |
| `AIA/AIA/AddFoodManualView.swift` | 对话F（小F） | ✅ 已完成·选中搜索结果后搜索框展示已选食物名（`applySearchResult` 内 `searchText = result.name`，不再清空）；结果列表仍收起；改动仅 1 行，commit `617216b`（§4 当时被对话A 标注「未提交」占用，实际工作树已无其改动，未连带） | 已释放 |
| `AIA/AIA/MonthlyBillListView.swift` | **对话D（小D）** | ✅ 已完成·月度账单页账单行点击统一为「直接弹 EditBillView sheet」：原 `NavigationLink { BillDetailView } label: { billRow }` push 详情页（两段式：详情→编辑）→ 改 `Button { editBill = b } label: { billRow }` + `.sheet(item: $editBill) { EditBillView(bill: $0) }`，与 BillListView / FoodListView / ReminderListView 一致（直接编辑 sheet 一段式）；BillDetailView 文件保留（BillCategoryView 仍用），不动；BUILD SUCCEEDED，commit `3e0a5cb`（+11/-3，1 文件） | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话F（小F） | ✅ 已完成·结果确认页「重量」与「热量」两个 inlineFieldRow 统一高度：给 `inlineFieldRow` 容器加 `.frame(maxHeight: .infinity)`，使同行两 cell 在 HStack 内自动等高开齐；commit `51007a4` | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` / `AIA/AIA/NutritionLibrary.swift` | 对话F（小F） | ✅ 已完成·营养估算可信度优化：① 模型估算来源文案改为「未查到权威营养数据，以下数值由 AI 看图估算，仅供参考，建议手动核对」并加合理性护栏（每100g 三大营养素合计>100g 或 热量与宏量推算相差>30% 时 amber 告警 banner）；② NutritionLibrary 补面食 aliases（拌面/凉面/汤面/炸酱面/热干面/油泼面/担担面/重庆小面/阳春面/炒面→煮面条，意面/意大利面→面条）；commit `fd9ac4e`，BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话F（小F） | ✅ 已完成·模型估算文案「以下数值」→「以上数值」（banner 位于营养数值下方，应指上方数据）；两处（未查到权威营养数据 / 联网查询失败）同步修改；commit `1b091cd` | 已释放 |
| `AIA/AIA/ResultConfirmView.swift` | 对话F（小F） | ✅ 已完成·账单确认卡片加引导文案：① 账单标题后加「（点输入框可修改）」；② 分类标签后加小字提示「下方图标，左右滑动可切换」（fieldRow 新增可选 hint 参数）；commit `ab84e34`，BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/ContentView.swift` + `AIA/AIA/RecordsViews.swift` | **对话D（小D）** | ✅ 已完成·**修「自动截屏识别」返回绕过「记账工具」**：根因——`BillToolsView` 是从 `BillListView` 用**闭包式 NavigationLink** 推入的（属于 BillListView 子栈），但 `BillToolsView` 内部 `path.append(.autoSetup)` 修改的是**根 NavigationStack**——`BillToolsView` 自己从根栈消失。返回时 `path.removeLast()` 移除 `.autoSetup` → 直接回 `.bill`（`BillListView`）→ **绕过了「记账工具」**。修法：① `HomeRoute` 加 `case billTools`；② ContentView switch 加 `case .billTools: BillToolsView()` 分支；③ `BillListView` line 1261 闭包 `NavigationLink { BillToolsView() }` 改 `Button { path.append(.billTools) }`，跟 `TodoToolsView` 入口完全对齐。BUILD SUCCEEDED，commit `f9e007c`（+15/-5，2 文件） | 已释放 | 已释放 |
| `AIA/AIA/LoginView.swift` | 对话F（小F） | ✅ 已完成·登录页副标题文案替换：原「给你与众不同的 / AI 管理体验」→「自动记账、记待办、记饮食 / 运动、睡眠、健康管理 / 一个App全搞定」；commit `11e866b`，BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/LoginView.swift` / `AIA/AIA/Assets.xcassets/AppLogo.imageset` | 对话F（小F） | ✅ 已完成·登录页顶部图标替换为 App 图标：新增 `AppLogo.imageset`（从 `AppIcon.appiconset/AppIcon.png` 复制），`logoSection` 的 sparkles 占位图改为 `Image("AppLogo")` 并裁剪为 44×44 圆角；commit `0cd6244`，BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/OnboardingView.swift` | 对话F（小F） | ✅ 已完成·Siri 引导页副文案替换：原「跟Siri说"叫阿宝AI管家"即可唤醒阿宝…」→「跟Siri说「用阿宝AI管家记」，就可以自动记账、记饮食、记待办，到点自动提醒」；commit `571a951`，BUILD SUCCEEDED | 已释放 |
| `AIA/AIA/OnboardingView.swift` + `AIA/AIA/AIAIllustrations.swift` | 对话F（小F） | ✅ 已完成·新增「都可以问阿宝 AI」引导页（第 7 页，插入在 shortcuts 之后、done 之前；tag 6，done 顺移 tag 7）；`total` 7→8；`AIAIllustrations` 新增 `Kind.ask` 插画（bubble.left.fill，蓝紫渐变，呼应「问阿宝AI」快捷操作）；BUILD SUCCEEDED，commit `8e02a63` | 已释放 |
| `AIA/AIA/OnboardingView.swift` | 对话F（小F） | 🔒 进行中·新增「已打通苹果健康数据」引导页（第 8 页，插入在 ask 之后、done 之前；tag 7，done 顺移 tag 8）；`total` 8→9；复用 `Kind.health` 插画（heart.fill，健康紫）| 3 分钟内 |

## 5. 本团队近期已改动文件（供其他团队参考，避免重复劳动）

| 日期 | 文件 | 改动摘要 |
|------|------|---------|
| 2026-07-22 | `DashboardView.swift` | 删除死代码（从未被实例化、硬编码脱离主题） |
| 2026-07-22 | `AIATheme.swift` / `UIComponents.swift` 等 10+ 文件 | 灰色令牌统一收口为 `AIATheme.sub` / `AIATheme.muted`，消除三套灰阶并存 |
| 2026-07-22 | `BillCategoryHelpers` | 账单分类色纳入主题 + 补深色变体 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 底部栏文案轮播改 `.task` 异步循环，修复不切换 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 提示箭头改为由 `iconOrder` 单一真源推导（§3 高危，对话B 已改完并 commit） |
| 2026-07-22 | `RecordsViews.swift` | 饮食/账单/待办三模块底部栏提示改用 `AIPrompt` 结构化，箭头随图标位置自动翻转 |
| 2026-07-22 | 仓库根 `.git` / `AIA/.git` | 合并 `AIA` 入父仓库，统一版本控制（父仓库现跟踪 162 文件） |
| 2026-07-22 | `ContentView`/`UIComponents`/`WeightTrendView` | 对话A 提交挂起改动：新增 PressableCardStyle 按压反馈、RingView/DonutView/MacroCard/MiniBar 描边生长动画、底部栏毛玻璃背景；与对话B 已提交的 AIBottomBar 改动共存，无覆盖 |
| 2026-07-22 | `UIComponents.swift`（`AIBottomBar`） | 对话D 悬浮胶囊重构：4 个独立按钮 → 单体玻璃胶囊（.ultraThinMaterial + 柔阴影 + 左右 12pt 边距），去掉独立圆背景和贴边分割线，仍为 VStack 兄弟不改成 overlay；BUILD SUCCEEDED |
| 2026-07-23 | `Models.swift` / `AIAMigrationPlan.swift` / `AppPersistence.swift` / `CloudSyncManager.swift` / `RecordsViews.swift` / `FoodDetailView.swift` | **对话A** FoodEntry 新增 fiber/sugar/sodium/waterIntake/baseFiber/baseSugar/baseSodium 7 字段；schema v7→v8 轻量迁移；同步 push/pull 透传；展示页从占位符改为真实数据 |
| 2026-07-23 | `Models.swift` / `AIAMigrationPlan.swift` / `NutritionLibrary.swift` / `FoodMetaStore.swift` / `RecognitionTypes.swift` / `RecognizeService.swift` / `ChatView.swift` / `RecognitionSaver.swift` / `云函数/recognize/index.js` | **对话A** FoodMeta 同步加 fiber/sugar/sodium；FoodRef/FoodPayload 加同字段；ChatView 透传到 FoodEntry（6 个创建点 + 1 个 update 路径）；云端 SYSTEM_PROMPT_TEXT/IMAGE 加 fiber/sugar/sodium；FN_VERSION `20260723b-foodmacros`；**新增饮水意图识别**（`parseWaterIntake`+`createWaterIntake`），支持「喝了X升/ml/杯/瓶水」+「喝水」自动建 `waterIntake=ml` 的 FoodEntry |
| 2026-07-22 | `RecognitionSaver.swift` / `ChatView.swift`（对话C） | 修复「识别失败未拦截 0 值入库」：在 `autoSave()` 和 `saveFromResult()` 两条路径加全宏量零守卫，拦截空壳食物记录；commit `20affb5` |
| 2026-07-22 | `AppDelegate.swift` / `ContentView.swift`（对话C） | 首页加载优化：首次同步延迟 0.3s（让首页先再渲染）+ 撤回 header 同步状态指示器（静默同步）；commit `1df51e5` + `6389d0f` |
| 2026-07-22 | `RecognizeService.swift` / `云函数/recognize/index.js` / `原生代码脚手架/.../index.js` / `package-recognize.sh`（对话C） | 多账单识别改走视觉模型：客户端移除多账单本地提前胜出（fall through 到 `recognizeResilient`）；云端视觉 prompt 新增规则13 输出 `bills` 数组、FN_VERSION 升 `20260722e-sensenova`、两份 index.js 同步一致并重新打包（zip gitignored 需手动部署） |
| 2026-07-22 | `云函数/recognize/index.js` / `agentTools.js` / `agentSchema.js` / `agentPrompt.js` / `package.json` / `AIA/AIA/AgentChatRequest.swift` / `docs/` / `test/`（对话A） | 云端聊天 Agent 接入（商汤只读 v1）：新增 4 个 function-calling 只读工具（查账单/饮食/健康/汇总），`handleAgent` 循环≤5轮+降级回 context-only；`mode:"agent"` 与 `mode:"chat"` 共存；FN_VERSION 升 `20260722k-agent-v1`；commit `9bfd446`（本轮未单独 commit 分工表，避免混入对话D 在途改动） |
| 2026-07-22 | `AIA/AIA/ChatView.swift`（对话C） | 优化阿宝回复自然度：①新增 `chatConfirmOpeners` 随机开场白池（记好啦/收到～/好嘞/搞定…），6 处确认消息（待办/账单/食物本地+云端/多记录×2）去模板化 + 类型 emoji 轮换；②上下文过滤同步改用 opener 池（`hasPrefix("记好啦")` 旧逻辑会漏过滤随机化后消息）；③底部问候只引用「今天到期+未完成+未来时间」最早待办，无未来待办则跳过，不再展示过期示例；④账单商户名清洗增加时间/饮食动词分组正则，纯餐次词（晚餐等）回退"其他消费" |
| 2026-07-22 | `AIA/AIA/ChatView.swift`（对话E） | 通用闲聊/元意图本地回复优化：新增 `replyForMetaIntent` 处理「你是谁/在干嘛/你能做什么/你好/谢谢/再见/好的」等，避免统一道歉式兜底；兜底文案改为能力示例；`isQuestionLike` 增加「嘛」疑问词；改动合入 `377fc62` |
| 2026-07-24 | `AIA/AIA/UIComponents.swift`（对话D） | ✅ 已完成·识别中浮层「返回」按钮入场动画：加 `@State appeared`；按钮 `.position(y: H*2/3 + (appeared ? 0 : 8))` + `.opacity(appeared ? 1 : 0)`，`.onAppear` 走 `withAnimation(.easeOut(duration: 0.35))` 上滑 8pt + 淡入；尊重 `AIATheme.motionReduce`（减弱动态效果时直接置位不动画）；BUILD SUCCEEDED，commit `e16983d`（+16/-1） | 已释放 |

## 6. Schema 变更闸门

- SwiftData `@Model` 字段新增 / 变更 → `AIAApp.schemaVersion` +1（见 `AIAMigrationPlan`）。
- 责任人：**待指定**（建议单一团队统一管，避免版本号打架、旧 store 白屏）。
- 流程：改前在 §4 声明 → 责任人审批 / 执行 → 全量 `xcodebuild` 验证。

## 7. 使用流程

1. **开工前**：读本表 §2 / §3 / §4，确认目标文件空闲。
2. **认领**：在 §2 填团队 + 状态 = 进行中；若动 §3 文件，在 §4 加锁定。
3. **完工**：commit 后，§2 状态改「已完成 / 空闲」，§4 解锁。
4. **冲突**：git 标冲突 → 后提交方解决 → 更新本表。

---
*本文件本身纳入 git 版本控制；各团队通过读取最新提交获取最新分工状态。*

## 8. 新对话开局提示（复制给每个新对话）

> 在每个新 WorkBuddy 对话的第一句粘贴下面这段，之后正常下需求即可，**无需懂模块划分**——模块 / 文件判断由对话自己完成。

```
你是这个 AI 助理 iOS 项目的开发助手。当前是多对话并行改同一份代码，请遵守：
1. 每次动手前，先读本项目根的 `多团队协作分工表.md`，重点看 §3（共享高危文件）和 §4（文件级占用看板）。
2. 分析你要改的文件清单；若其中任一文件已在 §4 被其他对话占用，停下并告诉用户“XX 文件正被[对话]占用，请换任务或等其释放”，不要硬改。
3. 若空闲，先把你的占用写进 §4（文件 | 你的对话标识 | 进行中 | 预计释放），再动手。
4. 完工并 commit 后，把 §4 里你的占用清除。
5. 涉及 §3 的共享高危文件，无论是否被占，动手前都在 §4 标注并提醒用户。
```
